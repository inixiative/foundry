{
  "name": "@foundry/agent-primitives",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "bun run --bun tsc --noEmit",
    "test": "bun test",
    "viewer": "bun run src/viewer/server.ts"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
