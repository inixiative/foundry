# S5: authoritative construction binding provenance

Continue in this same Foundry/native Fable session. The long-term goal and
CORE-002/003/004/005 remain unchanged. Astra is provisional lead, now count5
after supervisor acceptance of current-admission audit exclusion; next checkpoint6.
Your deadline latch has independently passed all17 native cases at
`../agent-session/.qa/s0-cross-2026-09-07T03-53-31.502Z/report.json`; no capture
authorized until Astra reviews the corrected recorder.

You own only a narrow Foundry provider/adapter provenance correction plus focused
tests and a dedicated handoff. Astra is concurrently doing read-only provider
review and a causal-learning diagnostic. Do not edit its diagnostic, the sibling
recorder/engine, memory selection, shared ledgers or independent acceptance tests.
No native probes, restarts, live binding/settings/profile changes, credentials,
dependency installation, commits or detached workers. Use normal code edit tools.

Supervisor independently reproduced two failures with real adapter construction
and in-memory stores, replacing only native start/send (spawn forbidden):
`fixtures/harness-qa/acceptance/native-model-binding-provenance.test.ts`.
Report `.foundry/qa/2026-09-07T03-57-07.053Z-G5/report.json`: one positive pass,
two failures. Existing nine model/provenance cases are still required unchanged.

1. An auxiliary with only preserved legacy coding history actually starts a
   fresh text-only-v1 session, but provider profile claims it resumed the legacy
   binding because getExternalSessionId falls back while createSession does not.
2. Between preliminary provider lookup and adapter creation, a changed lookup
   can make profile claim fresh while the session actually resumes a binding.

Make the profile derive from an authoritative construction binding snapshot or
fail closed before dispatch when identity changed. Avoid another unsynchronized
lookup or special-casing only the reported auxiliary path. Preserve diagnostic
lookup APIs as appropriate, compatibility of other adapter callers, existing
legacy histories, warm profile refusal and requested-versus-acknowledged model
distinction. Pending/failed binding lookup may not be declared fresh. No implicit
binding replacement or clearing. A narrow adapter contract is preferable to
guessing the effective binding inside the provider; inspect existing patterns.

Run all three independent model files (12 cases) explicitly via harness-check G5,
focused adapter/provider tests, strict test types and baseline with stable source
fingerprints. Add your own negative/positive cases around both native adapters,
no native process required. Do not weaken independent assertions. Give a bounded
handoff with findings, actual changes, exact commands/reports and limitations.
Supervisor decides acceptance after Astra's opposite-model re-review.
