# Implement T1: callable tools owned by the live runtime

Prepared by supervisor, NOT dispatched. Start only after an explicit assignment
and verified terminal of your prior work. Preserve CORE-002/003/004/005 and
AS-001..005; the goal is usable native retrieval, not only a safer denial path.

Dispatch update: your learning/R1 review actual end_turn05:56:13.849Z is verified
and fully read. Supervisor accepts bounded R1 configuration ownership based on
source, independent15 model/2 cleanup cases and your opposite review. It is the
sixth accepted bounded Astra slice; provisional lead retained, next checkpoint8
or serious regression. Learning's cleanup-refusal interaction still needs an
independent follow-up and is not a rollout acceptance. Do not change review
providers to an HTTP fallback to declare native parity. Astra is still reviewing
your UI timeout read-only. BEGIN T1 implementation now, in the same Fable session;
these MCP files are disjoint. I0/I1 remains Astra's next implementation delivery.

Read docs/I-native-integration-review-and-plan.md T1/T2, actual mcp/server.ts,
mcp/cli.ts, MemoryToolAdapter, ToolRegistry and runtime ownership interfaces.
Current actual-SDK scope acceptance has seven cases. Four original failures cover
foreign project enumeration, absent project authorization, disposed cache access
and silently following a project change. A new case replaces the registered thread
object with the same id while the old object is undisposed: old authority must be
revoked, not continue serving old data or silently attach to the new runtime.
Positive current-cache updates and same-project summary visibility must survive.

Own mcp/server.ts, a narrow live-runtime authority module and focused SDK tests;
MemoryToolAdapter only if a missing interface is necessary. Do not edit providers,
sibling/session engine, learning/persistence, viewer UI, independent assertions,
shared ledgers, live config or dependency files. I0/I1 package and evidence adoption
is separately owned by Astra; T2 transport/native launch requires its admission
interface. No native probes, server restarts, package installation or publication.

Implement tools against the authoritative supplied thread/runtime, registered
project and generation, not a reconstructed CLI thread. Pin the authority when
created, validate before every operation and after awaited reads, revoke on
disposal/replacement/project change. Use existing public ownership APIs. If a
minimal additive runtime interface is essential, surface the exact contract before
editing that separately owned file. Tool arguments never supply authorization.

Make foundry_threads disclose only authorized live same-project summaries. An
unprojected owner can access its own context but does not gain unrelated unprojected
threads. No foreign IDs/tags/descriptions/counts or error details. Same-project
summary visibility does not grant another thread's private record contents.

Provide actual scoped retained-memory search AND get by record id using existing
MemoryToolAdapter/ToolRegistry and ownership-aware backend views. Preserve useful
existing query/conventions tools, distinguish all three domain parts, and return
record provenance, range/content hash and explicit missing/unavailable outcomes.
Do not pass a memory tool name in the prompt and call it retrievable. Test one
own record omitted from automatic selection but accessible through SDK search/get,
a same-project publication, another thread's private record and another project's
private marker; forged IDs and incapable backends must not leak data. Preserve
mandatory-context blocking until actual native bridge and policy are verified.

Keep initial native grant read-only. Do not expose foundry_signal as a successful
durable write until it has owned awaited journal/call provenance. Preserve any
explicit standalone/operator behavior separately rather than silently broadening
the native grant. Do not invent native IDs from SDK request IDs. Return a bounded
tool invocation contract ready for I1/T2 attribution, including operation, owner,
generation, start/result status and digest; missing native correlation is unknown.

Exercise actual SDK initialize/listTools/callTool with existing in-memory transport,
not just internal handlers. Add delayed disposal/replacement and publication/read
races, fresh committed knowledge visible without recreating server, same-project
positives, immutable previous results and explicit unsupported-memory behavior.
No model or credentials in these tests. Run unchanged seven scope tests RED then
green, scoped memory regressions, source and explicit test types/diff sequentially;
record unique report paths, exact counts and moving-source limitations.

Deliver docs/T1-live-scoped-tools-handoff.md with source interfaces, exact tests,
remaining native transport/correlation work and proposed bounded real tool sample.
No self-acceptance. Opposite review and supervisor independent SDK verification
precede T2 native use. The next integrated sample must retrieve an omitted owned
fact through the actual Foundry session for both engines; no permanent local-only
replacement for native tools, artifact inspection, model effort or continuity.
