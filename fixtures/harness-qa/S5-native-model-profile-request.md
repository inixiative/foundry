Continue in this SAME Fable native work session. Astra remains provisional lead
(four accepted slices, next checkpoint six) and is actively preparing the sibling
two-admission recorder OFFLINE. Own only this disjoint Foundry provider correction;
do not modify sibling code, the lead server/script, shared graph or independent
acceptance assertions. No new native probes, detached agents, restarts, package
changes, live configuration edits, credential changes or binding replacement.

Independent reproduction: native-model-profile.test.ts has three failures and
one positive control. Report .foundry/qa/2026-09-07T03-19-43.544Z-G5/report.json.
SessionBackedProvider.complete accepts opts.model but omits it from createSession
and the warm profile; it returns that requested model even though the adapter
uses its constructor default. Correct the actual model propagation through BOTH
Claude and Codex adapters and include effective requested model in the immutable
warm profile. A same-thread model change must fail before send/replacement, with
an actionable explicit-transition error. Default and identical explicit models
must reuse the warm session. Preserve tools=false enforcement, native binding,
timeouts, maxTurns and all previous failure handling. Do not weaken restrictions
or silently change saved classifier/router settings.

Add your own offline spawn/config tests for both production adapters, not merely
a fake-provider assertion. Verify explicit and fallback models reach actual argv
or first native MCP configuration, tools remain restricted, and changed profiles
cannot dispatch into an existing process. Distinguish requested configuration
from emitted native acknowledgment; an echoed model label is not proof it ran.
Preserve the existing persisted binding; if persisted model identity is unknown,
report the limitation instead of inventing confirmation or silently starting fresh.
Do not broaden this to a model catalog or central execution-budget UI in this slice.

Run all four independent native-model-profile cases, focused provider/adapter and
agent-selection tests, full baseline/types/diff. Return concrete evidence and
limitations for Astra read-only review and supervisor acceptance. Do not self-accept.

Context, not a causal claim: primary memory rollout and real same-session read-only
continuation succeeded. Exact native input fell from239046 to15062 characters,
selected memory from228942 to6364 including presentation. Generated knowledge is
still1326 chars and stale. Classify/route each took about15s. All owned auxiliaries
have --model fable, but saved classifier/router agent overrides also say fable,
so those facts alone do NOT prove model mismatch caused those delays. Source
start.ts falls back to rawProvider for domain/cartographer work. Keep the stale
learning and self-referential current-audit selection gaps explicit; investigate
their causes read-only only if time remains after the bounded provider correction.

Keep the full CORE-002/003/004/005 and AS-001..005 goal: three-part parallel
decoration, causal knowledge/lifecycle, historical provenance, native tools/events,
I integration/T callable scoped tools, session continuity/fork/rewind/subsessions,
artifacts/attachments/tags/inspection, latest models/efforts, native budgets and
subscription-independent logical work. This slice is not native or speed parity.
