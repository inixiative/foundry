Continue the SAME native Astra lead session for a bounded S1 correction in
../agent-session. Your prior native task completed2026-09-07T01:49:00.259Z,
Foundry turn_7ead687e-16f2-40c2-92f0-c419626c810c. Supervisor independently reran
83 ordinary sibling checks plus2 expected S4 failures and all7 unchanged Foundry
admission/recorder cases with stable cross-repository evidence. Source review
adds two correction requirements before Fable review; lead count remains3.

Read fixtures/harness-qa/QA-2026-09-07-S1-terminal-review.md and the unchanged
supervisor-owned fixtures/harness-qa/acceptance/native-terminal-evidence.test.ts.
The real retained429 terminal passes. Two SYNTHETIC missing/false is_error variants
fail because api_error_status429/terminal_reason api_error are ignored despite
subtype success. Do not describe those mutations as real captured protocol.
Match the established Foundry provider's error precedence: explicit native error
evidence cannot become completed. Preserve the available terminal reason/status
and distinguish truly unknown evidence from confirmed failure. Do not infer a
native failure merely from a local transport/RPC error.

The third failure shows mutable event/raw objects shared across returned results,
attempt snapshots and internal evidence. Prevent clients or observers from
rewriting retained evidence, including nested tool/raw payloads, tokens and
terminal metadata. Choose an efficient owned-data boundary, not repeated copying
of the full session history on every event. Preserve late updates on the original
admission and frozen historical result semantics. Add both-engine observer and
snapshot negative tests, retaining original errors even for rejecting observers.

Reproduce the three independent failures unchanged, fix production code and
targeted tests, then rerun all THREE independent files explicitly:
native-terminal-evidence.test.ts, native-turn-ownership.test.ts,
native-recording-safety.test.ts under Foundry fixtures/harness-qa/acceptance.
The older S0 cross-check runner covers only the recorder file, so do not claim
it covers all11 cases. Capture stable source fingerprints from both repositories.
Run sibling suite and source/script typechecks; preserve two expected S4 failures.

Use apply_patch for manual source edits. Do not edit Foundry production/shared
graphs or independent tests; Fable is actively correcting memory selection there.
No new agents, probes, native session forks, dependency integration/install,
credential changes, server restarts, publication or commits. Original recordings
and provenance stay immutable. Update your sibling S1 handoff with exact test
reports and honest capability gaps, then return for independent review.

Scope context: Fable hit installed CLI maxTurns25 and then resumed this same
session; actual tool activity is now verified01:53:32.524Z. It remains occupied
with memory corrections, so do not dispatch a review yourself or take over its
files. The artificial central turn-budget limit is recorded for later integration,
not part of this correction. Same full long-term goal and count4 checkpoint.
