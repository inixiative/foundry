# Correct current-knowledge observer ordering

Prepared by supervisor. Dispatch only after your current observer/inspection implementation turn is actually terminal; do not duplicate or interrupt it. Count stays5, full goal unchanged. Astra owns closed learning capacity; do not edit its runtime/provider/persistence source.

Supervisor read your observer-inspection handoff and visually inspected your eight-check green browser evidence at04:57:22.231Z. Your final combined rerun is affected by Astra's in-progress runtime changes, not a reason to change its files or keep repeating a known broken fixture. Return honest scoped evidence and await the combined source revision.

Independent scripts/check-knowledge-observer-order.ts reproduces a new blocker in store.js through actual viewer/store/browser and a controlled HTTP knowledge endpoint, no model or learning runtime. After initial revision0, one revision1 request is held; a second response displays revision2; the older request then succeeds with HTTP200 and replaces revision2 with revision1 in store and panel. Report:
.foundry/qa/knowledge-observer-order/2026-09-07T05-08-45.084Z-f90f42ad-189a-4231-9881-de9dcce892dd-G4/report.json
Source unchanged f886cc310774bfcd39d54cb6c8cdec79639856b9d03846ac7d099e61577e0fce. Actual old200 delivery and exactly one newer request are checked. Earlier05:07:08 apparent pass is invalid: the held request timed out and retried. Do not cite it as acceptance.

Own the same UI store/inspector files and focused tests only. Add explicit request/selection ownership so older same-thread successes or failures cannot overwrite a later observed state. Cover A-to-B-to-A selection, runtime generation/epoch changes, pending queries, unavailable states and manual/event-driven refresh. Do not guess latest from arrival time or a single revision across runtime generations. Keep immutable historical delivery/input separate from current state.

Also verify late history responses after thread switch cannot write into the foreign active thread, preserving completed-unsaved/legacy/streaming data. No native replay, replacement bindings or unbounded polling. Leave the independent ordering script unchanged; add tests that exercise actual store behavior, not only summary helpers.

Run the independent ordering script and original8 browser checks once the runtime source is coherent. If the learning fixture remains temporarily broken, verify this separate ordering fixture and report the exact missing combined check. No self-acceptance or primary restart. Append a correction section to QA-2026-09-07-fable-observer-inspection-handoff.md with hashes, commands, screenshots and gaps. Supervisor and opposite-model review decide acceptance.
