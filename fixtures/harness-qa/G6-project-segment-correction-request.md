# Close the three configuration metadata gaps

Continue SAME Fable session after actual end_turn13:23:30.617Z, matching durable
final and complete G4-preflight-only-handoff.md read. Your preflight delta is now
owned by Astra for read-only review; do not edit the runner or its tests here.
Leadership checkpoint eight is accepted for bounded SOURCE only. Astra remains
provisional lead; the full original goal and UI backlog remain unchanged.

Read docs/G6-segment-opposite-and-candidate-readiness.md in full. Parent reproduced
the independent scratch file unchanged: 2 pass, 5 fail, 15 assertions. Correct all
three findings through existing configuration policies:
1. Preserve optional segment and fields.segment origin/strategy through project
   resolution for inherited, overridden and project-only layers. Preserve absent
   legacy metadata and provider text.
2. Validate global AND project layer segment declarations before load/save/patch
   publication. Invalid null/string/object values must not mutate live config or
   the prior persisted file. Valid and absent controls must work.
3. syncFromHarness must copy a present construction segment for a newly inserted
   layer; do not overwrite an existing configured definition or add absent fields.

Own config-resolve.ts, config.ts and focused configuration tests only. If another
production boundary truly needs change, report the reason before extending scope.
Preserve reviewer scratch assertions and add checked-in regression coverage. Run
the exact scratch file, new tests, prior generated-segment/lifecycle/config tests,
types and diff; record scoped before/after hashes and honest failing commands.
Do not relabel historical artifacts, change provider text, infer ownership from
metadata, change layer construction semantics, or weaken validation to pass.

Append your G6 handoff with findings corrected, exact evidence and remaining
limitations. No staging/install/capture, live restart, native adapter/provider,
sibling, credentials/account/binding changes, commits or other-model dispatch.
After terminal parent will verify and Astra will review this correction before
one integrated immutable candidate. The next product slice remains lightweight
history, lazy immutable detail, pagination, safe cache and artifact navigation.
