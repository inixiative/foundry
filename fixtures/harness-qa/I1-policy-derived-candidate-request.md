# Build a reproducible policy-only candidate from the last passing snapshot

Continue in the SAME Fable Foundry/native session after verified end_turn
08:39:29.742Z. Supervisor read your full I1-command-policy-handoff.md, the full
command policy and ten tests. The policy has bounded source approval pending
independent installed checks. Both old native failures stay FAILED. Same full
goal/count6-next8. Astra continues T3; do not touch its runtime/MCP/runner files.

Your latest candidate snapshots unfinished T3 and has two real baseline failures.
Do not waive those failures or mutate that candidate. Avoid waiting for all of T3
or rebuilding a moving root simply to verify this independent policy correction.

Prepare a NEW derived source directory using reviewed collect/freezeCopy helpers:

- Base: .foundry/qa/adoption-6b0a0aad-0506-4745-8629-64dfa10ea947/foundry.
  This immutable C2 has independent baseline1232, focused55 and runner-failure4
  passing reports. Existing source fingerprint92951679354192a59b3c37f156e952d3e8863e42ad09a7a80959d44e9add87c9.
- Overlay ONLY scripts/native-sample-guard.ts and
  packages/foundry/tests/native-sample-command.test.ts from your immutable
  .foundry/qa/adoption-0ce4f59c-a0c6-4d3c-aad4-78760006aa18/foundry.
  Expected hashes respectively97ddfc8f08886f609926dc5084dcd32b788fcf0f937405de2f52f6f05cafcf60
  and672dd9b4dd3c265764030cc070a46523b80e0504ad9b50875adf0f0be100b625.
- Preserve the old sample runner8799fa5db677d07e210596c62343880a206c2b3b824b29f2e2f465df651f5b1e
  and all other allowed base source bytes. Do NOT copy root's new retrieval edits.
- Use base candidate's reviewed sibling mirror as native source, verify source
  digestf2c2f8f274f13230c767bf6a876e27c482f56621d6b6112007d48488be71f67f.

This explicitly authorizes controlled derivation/staging, not edits to any prior
candidate. Use a new unique directory; only allowlisted source, never node_modules,
live state, tapes, native artifacts or credential files. Write a structured
derivation manifest containing exact base/overlay paths and per-file before/after
hashes. Verify the full allowed-source diff is exactly those two files. No git
commit, fake HEAD provenance, ad hoc dependency edits or generic filesystem copy.
Programmatic copying of verified frozen bytes through the existing staging helper
is artifact assembly; any manually authored helper code must use apply_patch.

Run existing prepareNative against the NEW derived source (it accepts explicit
root/sibling/parent), creating another new installed candidate with both reviewed
scripts-disabled installs, explicit Prisma generation and installed-package
verification. The changed dependency/lock artifacts are those generated by the
existing installer, separate from source derivation. Never modify root/live install.

Run full baseline, exact previous18-file sample gate (now142 cases), strict owned
script/test types and source/manifest verification inside the new candidate.
Retain failed candidate/report and all old successful/failed native evidence.
No actual model capture, native replay, restart, agent, account/binding/config
change, publication or root/sibling/candidate commit. Report any genuine remaining
blocker rather than weakening a test. Append docs/I1-command-policy-handoff.md
with exact derivation/installation manifests, paths, results and fresh-ID unexecuted
Claude sample command. Supervisor independently checks and runs the real sample.

This isolates the already-reviewed sentinel correction; it does NOT accept T3,
deploy old T2 as current, or narrow the full long-term objective. Broader native
retrieval/learning/tools/inspection/lineage/pooling/latency work continues separately
in the Astra lead. Finish this bounded candidate and return for actual acceptance.
