1. **High — mandatory rules can be omitted without usable retrieval.** [file-memory.ts:150](</Users/agreenspan/Library/Mobile Documents/com~apple~CloudDocs/Desktop/inixiative/foundry/packages/core/src/adapters/file-memory.ts:150>) replaces oversized pinned rules with “read … with the memory tool.” However, [session-backed.ts:90](</Users/agreenspan/Library/Mobile Documents/com~apple~CloudDocs/Desktop/inixiative/foundry/packages/foundry/src/providers/session-backed.ts:90>) does not install Foundry’s scoped retrieval handlers in native sessions. My in-memory adapter check confirmed tool definitions are not forwarded. The supervisor’s subsequently added required-retrieval regression also fails: a 26,086-character requirement is omitted and execution succeeds. **Correction:** supply mandatory content fully or explicitly block before provider execution when retrieval is unavailable. Native scoped retrieval needs separate implementation and evidence; shell access is not equivalent.

2. **High — failed refresh can mark wrong-focus content warm.** [context-layer.ts:430](</Users/agreenspan/Library/Mobile Documents/com~apple~CloudDocs/Desktop/inixiative/foundry/packages/core/src/context-layer.ts:430>) restores the previous state without checking the current focus. Reproduced read-only: warm alpha → start another load → change focus to beta → reject load. Result: `focus=beta`, `state=warm`, `content=selected:alpha`. [hydrateDelta:709](</Users/agreenspan/Library/Mobile Documents/com~apple~CloudDocs/Desktop/inixiative/foundry/packages/foundry/src/agents/flow-orchestrator.ts:709>) then skips warming that layer. **Correction:** retain old content/provenance but leave it stale whenever its recorded focus differs. Test failures during both the initial load and a focus-triggered retry.

3. **Medium — selection policy lacks runtime validation.** [file-memory.ts:443](</Users/agreenspan/Library/Mobile Documents/com~apple~CloudDocs/Desktop/inixiative/foundry/packages/core/src/adapters/file-memory.ts:443>) merges unchecked overrides; project settings accept raw source configuration. Reproduced `maxEntryChars:-2` producing range `[0,-2]`, and JSON-valid `budgetChars:"invalid"` allowing 10,910 selected characters with `exceeded:false`. **Correction:** validate numeric types, finite integral bounds and kind arrays before accepting configuration/loading; reject invalid policies without replacing working configuration.

4. **Medium — browser fixture cleanup is broken.** [browser test:69](</Users/agreenspan/Library/Mobile Documents/com~apple~CloudDocs/Desktop/inixiative/foundry/packages/foundry/tests/browser/memory-selection-inspector.test.ts:69>) calls nonexistent `cleanup?.()`; [completionFixture:47](</Users/agreenspan/Library/Mobile Documents/com~apple~CloudDocs/Desktop/inixiative/foundry/packages/foundry/tests/helpers/completion-persistence-fixture.ts:47>) exposes `close()`. Successful runs leave store/thread/temp resources unclosed; browser launch also occurs before `try`, leaving earlier resources unprotected on launch failure. **Correction:** typed `fixture.close()` and cleanup covering partial setup and cleanup failures. This blocks calling that test repeatably cleaned up; it does not invalidate the supervisor’s separate visual evidence.

**Recommendation: reopen the bounded slice.** The six acceptance cases and baseline pass, and I inspected the controlled provider input and historical Selection screenshot: convention, relevant tail, ranges and selected/omitted counts agree. Scope filtering, retained audit data, ordinary restore mismatch and manual-set provenance handling have coverage.

The 6,000-character budget is deliberately soft: it excludes summary/header overhead and permits unlimited aggregate pinned content. Ten valid 20,000-character pinned records produced 200,727 characters with an explicit conflict. That is documented behavior, not a strict total-input bound or performance result.

Next ready work is the narrow G3/G7 correction above, followed by independent regressions and historical UI verification. Withhold primary rollout until those pass. Any later rollout should preserve audit/journal/binding snapshots; rollback code/configuration without deleting evidence or replaying unknown work.

The layer provenance fits the instructions/domain-knowledge/thread-knowledge model. Native retention, tags, broader artifact/attachment inspection, subsessions, fork/rewind, model/effort parity and subscription-independent continuity remain open. No files were edited, no review was dispatched, and I did not approve S1. Lead count remains **3**.
