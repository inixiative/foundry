# Integration dispatch and observer ordering

Supervisor,2026-09-07. Previous goal turn: progress, with independent learning,
model and browser failures, revised causal contract and native cross-review.
Full objective unchanged; count5/checkpoint6, no rollout.

## Lead review and assignment

Astra integration review ended04:54:43.815Z, native turn
01a07a29-b5aa-7242-ae4b-210e560466de. Supervisor read the complete
docs/I-native-integration-review-and-plan.md, including both-engine Foundry samples,
typed provider-to-journal path, scoped authenticated tools and no-data-loss rollback.
Bounded sibling explicit-compaction and actual construction-binding corrections
are accepted based on source, independent21 native/14 earlier model cases and
opposite-model review. This does not close mutable configuration collections
(new15th case RED), real native compaction, model switching, adoption or capacity.

The I0/I1 integrated delivery, I2 learning reconciliation and T1/T2 runtime bridge
are ready for implementation after their blockers. Do not reduce the milestone
to packaging alone. Preserve expected S4 failures:176 ordinary plus2 expected,
not178 ordinary acceptance. R1 collection ownership must be corrected; typed
native outcomes must reach provider/executor/trace/journal/viewer; actual tools
must bind the live authority, not a reconstructed MCP thread.

After actual review terminal, G3-closed-capacity-correction-request.md was sent to
the same Astra lead: Foundry turn_73b16973-83e3-42b6-b763-8a702f26ee39,
HTTP200/matching start; native task_started04:57:14.032Z, turn
01a07a3a-4425-7d21-8833-1eea0ed4000b. It owns runtime/learning and narrow provider
capability/lifecycle work. Fable owns viewer UI; avoid overlapping provider/adapter
edits when assigning R1. No additional native captures authorized.

## Report isolation

The actual04:33:06.747Z collision mixed two check runs. Supervisor changed
harness-check.ts to use scripts/qa-run-directory.ts: validated timestamp/gate,
UUID and atomic nonrecursive mkdir for each run. Trailing gate labels remain
compatible with existing discovery. Directory reuse fails instead of overwriting
logs. An intermediate mkdtemp naming put randomness after the gate; corrected
after observing an active reviewer's *-G3 discovery. Historical paths unchanged.

Three qa-run-isolation.test.ts cases pass:32 same-timestamp/gate allocations with
independent reports, repeat allocation preserving old evidence, invalid label/path
refusal. Initial04:58:15.971Z-G7-T1fAJp passed tests/types/diff. The final naming
run05:02:12.416Z-854d918f-44af-48ab-bf96-9f303f46d2e5-G7 passed3 tests but failed
combined types on Astra's in-progress _pendingReviews removal and capacity field
addition. Do not call that whole gate green. Explicit strict runner/helper/test
types pass. Atomic paths solve collisions, not concurrent-source staleness.

## Actual MCP scope

native-tool-scope.test.ts now has6 SDK cases. Report
.foundry/qa/2026-09-07T05-00-23.010Z-G5-UqEdk3/report.json:2pass/4fail.
Live owned-layer updates and same-project summaries pass. Cross-project metadata,
unrelated unprojected enumeration, private cached access after disposal and silent
project reassignment of existing authority fail. Standalone strict types pass.
These linked-transport SDK cases preregister T1 owner/lifecycle requirements; they
do not prove native callable availability or authentication.

## Browser progress and ordering blocker

Fable's QA-2026-09-07-fable-observer-inspection-handoff.md exists; existence is not
terminal. Its original independent browser report04:57:22.231Z has8 positives,
including external refresh and historical pending/zero-wait inspection. Supervisor
visually checked that historical label. Later combined checks encounter Astra's
incomplete runtime source. No UI acceptance or deployment yet.

New scripts/check-knowledge-observer-order.ts uses the actual viewer/store/browser
with controlled HTTP snapshots, no learning runtime or model. It observes0, holds
one revision1 request, displays revision2, then receives the older successful200.
The current code regresses to revision1 in both state and visible panel.

Authoritative RED report and after-older-response.png:
.foundry/qa/knowledge-observer-order/2026-09-07T05-08-45.084Z-f90f42ad-189a-4231-9881-de9dcce892dd-G4/report.json
Store hash unchanged f886cc310774bfcd39d54cb6c8cdec79639856b9d03846ac7d099e61577e0fce.
Exactly one old and one new request, actual old200 delivery verified, no browser
errors; standalone strict script types pass. Earlier05:07:08 apparent pass is
invalid: the held request timed out at10s and retried. Corrected fixture uses60s
idle budget, avoids a screenshot while holding the response and verifies delivery
and no masking retry. Owned server/database/browser are closed.

G4-knowledge-order-correction-request.md is prepared, not dispatched. Send after
Fable's current UI task actually terminates. Preserve request/generation/selection
ownership, historical evidence, bounded refresh and no replay. Recheck original8
browser cases plus ordering, storage/history and native inspection. No need to
repeat known failures from another owner's active partial edits; verify independent
boundaries and run combined gates when that owner returns a coherent revision.

## Subsequent native terminal and correction admission

The original Fable UI admission did terminate, but without an assistant end_turn:
Foundry's durable agent row records `Native terminal error (error_max_turns)` at
05:07:45.193Z, immediately after its public tool result05:07:45.179Z. The provider
constructs this error from the native result subtype; it is not an observer timeout.
The last tool reported37 tests passing with a source-stale combined report at
05:06:52.809Z-a7bffe63-c984-44c6-8d89-501b52c93c0d-G3. No test child remained.
Empty live buffer alone was not used as terminal proof. This is another real
25-turn harness-cap limitation, not evidence against Fable's planning quality.

After that verified native terminal, supervisor dispatched the separate ordering
correction on the preserved binding: Foundry
turn_f369f2b8-728b-4b57-b474-79f2efa4b666, HTTP200/matching start; actual same-native
tools verified05:20:41.981Z. G4-knowledge-order-correction-request.md is now active,
not merely prepared. The previous UI task must not be replayed. Current iteration
adds explicit latest-request ownership; await actual terminal and independent QA.
Supervisor's05:24:39.712Z ordering rerun crossed a store edit and is stale, not a
verdict on the corrected revision. Keep the stable05:08:45 RED as the baseline.

## Latest independent learning and lifecycle evidence

All16 independent learning cases now pass, including the four closed-capacity
regressions, with source/types/diff stable at
.foundry/qa/2026-09-07T05-18-17.102Z-ecaff695-f683-4cba-a954-929fff120c88-G3/report.json.
Fingerprint0be25135d95137e928d60ef43b822781add492d8607b92abda027f8879d583cc.
Astra's implementation remains active pending its final handoff and Fable review;
this is not wholeG3/native capacity acceptance or deployment.

New native-review-cleanup.test.ts tests the real adapter/provider over controlled
streams, with separately controlled kill requests and process exit. One native
review terminal is known completed, but releaseIdle calls kill, deletes local
maps and returns `released` before exit occurs. Independent RED at
.foundry/qa/2026-09-07T05-21-35.358Z-b1e227bf-6bb5-4057-aedd-a13a06106779-G5/report.json:
1fail/1pass; active unknown review correctly refuses cleanup without a kill.
The negative permits either a truthful requested/unknown result or waiting for
owned exit; it does not require immediate completion. Strict standalone types
pass, no native process/model. This narrow cleanup-provenance finding must enter
the opposite review. A kill request is not proof of physical resource release.

Also review the new release call after every review queue drain: it may eliminate
warm review reuse even while the runtime remains active. That is an unmeasured
capacity/latency concern, not a claimed benchmark regression or permission to
retain resources forever. Compare with a bounded idle/disposal policy during I2
native verification; do not equate memory-map removal with owned process exit.

Final suffix-compatible QA runner check passes all3 isolation cases, combined
types and diff at05:23:00.197Z-f0a71370-96f2-43e7-9684-6f54412169f1-G7. Inspect its
source-change marker before combined-release claims. The runner correction does
not increment Astra's accepted-slice count. Full native/tool/account/artifact/
inspection and measured no-loss objective remains unchanged.

## Learning return and next lead admission,05:37Z

Astra correction has actual task_complete05:34:04.384Z. Supervisor read the full
docs/G3-closed-capacity-correction-handoff.md. It now keeps idle resources attached
until owned subprocess exit, with no new call admitted during unresolved cleanup.
Its final baseline1161 and focused82 reports are implementer evidence pending
opposite review, not acceptance. Count remains5/checkpoint6.

Supervisor independently ran31 cases at
.foundry/qa/2026-09-07T05-35-28.407Z-80a1e0dc-0d90-478c-9af6-9e7720fe9e8f-G3/report.json:
12 independent learning,17 package domain-learning and2 independent cleanup;
all pass with types/diff and stable hash
6f9ddf43a72b792be9ae9295059006939ab9905585e1278cecd29cf0a3d9ea75.
The initial next-assignment wording incorrectly called this all16 independent
learning plus2 cleanup. It selected the package domain-learning file, not the
four-case acceptance file. Preserve that original journal text and this correction.
The subsequent actual16+2 run passed assertions/types/diff at
05-37-01.339Z-57ed98cd-0d6b-451a-985c-d8559560c52f-G3, but source changed during
checks: stale, not a stable combined acceptance. No assertions were weakened.

With Astra's prior native work actually terminal and provider ownership released,
supervisor dispatched R1-configuration-collection-correction-request.md through
the existing Foundry stream API: HTTP200/matching start,
turn_5d3677c7-1a9b-4858-a6df-609133b99daf. Actual native task_started05:37:05.151Z,
turn01a07a5e-c074-7550-93bc-7d72fb22598c, same native session. Scope is detached
immutable configuration snapshots for both adapters and focused regressions,
preserving learning cleanup and bindings. No I installation or probe authorized.
Fable is still on the disjoint UI ordering correction (public tool05:34:57.008Z).
After actual terminals: opposite reviews, independent browser/SQL/provider gates,
then bounded I/T integration and real both-engine sample work. No duplicate sends.

CUA located the existing lead tab17, but its rendered history still stopped at
the prior04:38 review request. This is stale-tab observation, not proof that the
new frontend is loaded or new work visibly streamed. The lead is actually running
inside Foundry, while live browser synchronization remains a separate acceptance
requirement. Preserve the two native identities and the full long-term goal.
