# Correct project ownership at the HTTP construction boundary

Your E1 turn completed with matching native/durable terminal and empty live buffer. Parent read the full handoff. Continue toward the unchanged CORE-006 graph and full native-harness goal.

Parent regression packages/foundry/tests/configured-project-routes.test.ts now reproduces TWO actual HTTP route failures using saved ConfigStore, production configuration-enabled factory/runtime and ProjectRegistry: creation and existing non-native fork return a project-P thread but runtime.owner.projectId is undefined and the globally enabled compatibility expert remains active despite project P disabling it. Direct factory control in both tests succeeds. Exact RED: .foundry/qa/2026-09-08T06-55-40.425Z-fb0071a0-7037-400a-b134-f7b412af4c9d-G3/report.json (0pass/2fail/12assertions). Earlier two reports were fixture setup errors, not product evidence.

Own a narrow correction in packages/foundry/src/viewer/routes/runtime.ts and docs/E1-project-route-correction-handoff.md. Pass resolved project identity to the factory before construction in both routes. Inspect adjacent ownership paths for the same issue, but do not expand edits without a concrete finding. Preserve explicit native fork 501 refusal; this is not native-fork implementation. Do not weaken or edit the parent test or earlier acceptance fixtures. Run the parent regression, existing thread route/ownership/lifecycle tests, configured-experts and original configured-domain-registration test, plus strict types and diff check. Record exact commands, RED/GREEN evidence and final hashes. No manual post-construction owner mutation.

Parent owns the new test and independent verification. Fable is receiving read-only E1 review with findings only in its new review document. Existing E1 source is otherwise frozen; no source overlap. No browser slot needed. No installs, candidate freeze, native sample, live-server restart, settings/account/binding/shared-browser change or commit. Installed/native L remains next before E live lifecycle acceptance; do not claim hot editing or Herald delivery is implemented. Keep the full goal and countTEN unchanged.
