# Lead correction: owned configuration collections before native integration

Post-dispatch evidence correction: the05:35:28 report ran31 cases:12 independent
learning,17 package domain-learning and2 independent cleanup, not all16 independent
learning. The separately rerun full16+2 at05:37:01.339Z passed assertions/types/diff
but crossed concurrent source edits and is stale. Original dispatch text remains
in the journal; neither report accepts the slice or changes this R1 assignment.

Continue in the SAME Astra Foundry/native session. Your learning correction has
actual task_complete05:34:04.384Z. Supervisor read the complete
docs/G3-closed-capacity-correction-handoff.md and independently reran all16
learning plus2 native cleanup cases with source/types/diff stable at
.foundry/qa/2026-09-07T05-35-28.407Z-80a1e0dc-0d90-478c-9af6-9e7720fe9e8f-G3/report.json.
Fable is still correcting viewer ordering; opposite review of your learning
correction follows its terminal. Count remains5/checkpoint6, no self-acceptance.

Close the separately reproduced R1 blocker from your
docs/I-native-integration-review-and-plan.md. The production adapter exposes its
internal observedConfiguration array. A caller can append a forged owned-binding
fact and cause the next completion to claim a native model never emitted. The
unchanged third independent native-model-evidence-ownership.test.ts reproduces it;
your final G5 check retained this ordinary failure, with older14 cases passing.

Own only Foundry providers/session-adapter.ts and narrowly necessary
session-backed.ts, related model/configuration types and focused adapter/provider
tests, plus docs/R1-configuration-collection-handoff.md. Your previous provider
lifecycle work is handed off; preserve it exactly except for inseparable fixes
which must be explained. Do not touch Fable's viewer files, learning/persistence,
sibling, independent fixtures, shared ledgers or dependency/settings files.

Return immutable detached snapshots at the public configuration boundary for BOTH
Claude and Codex adapters. Preserve immutable fact objects and the collector's
ability to append legitimate later evidence. Distinguish unavailable evidence
from an owned empty collection; do not fabricate bindings, model or effort.
An older returned snapshot must not grow or change after a later real event.
Test push, splice, index replacement, saved snapshot retention, foreign observers,
both engine paths, and later legitimate own-binding configuration visible on a
new read. Reuse existing owned-data helpers, avoid copying full event history on
every lookup or introducing a new provider abstraction.

Run the independent failure RED unchanged before editing, then all15 model cases,
both native cleanup cases, targeted provider/adapter tests and source/strict test
types/diff sequentially with exact report paths and stable fingerprints. Use
controlled streams, not new model calls. If Fable's simultaneous UI edits make
whole-tree evidence stale, record that limitation rather than claim acceptance.
No native probes, agents, server restarts, binding/credential changes, installs,
commits or publication. Manual edits via apply_patch; do not weaken independent
assertions. Hand back for opposite review and independent acceptance.

Keep the next delivery concrete: I0/I1 reviewed local package AND end-to-end typed
outcomes, T1 authoritative scoped tools, T2 actual native bridge, I2 owned learning
reconciliation, then bounded both-engine Foundry sample work and reviewed rollout.
Do not run those probes or install a package in this R1 correction. The full
CORE-002/003/004/005 and AS-001..005 goal remains unchanged, including three-part
decoration, artifacts/tags/right-panel inspection, native lineage/subsessions,
models/efforts, the central25-turn cap and subscription-independent continuity.
