# Correct explicit native compaction evidence before adoption

Continue SAME Fable session after verified end_turn04:21:17.676Z. Supervisor read
your full continuation/I-T handoff and accepts the bounded two-admission native
evidence after independent record joins and hash checks. This is not full S1/G5,
native retention or Foundry adoption. Count5/next6 unchanged.

Your repeat-init finding is independently reproduced in two cases:
`fixtures/harness-qa/acceptance/native-compaction-evidence.test.ts`, stable RED
`.foundry/qa/2026-09-07T04-20-00.582Z-G5/report.json`. Both fail; strict standalone
types pass (ES2023). First replays the TWO actual sanitized init envelopes from
the new Claude capture through production sibling ClaudeCodeSession, which emits
one false session_compact. Second synthetic positive control emits an explicit
compact_boundary then init and gets TWO compaction events instead of one; foreign
boundary must not adopt a different binding or invalidate this owner.

Own only sibling Claude compaction/evidence boundary, related focused tests and
dedicated handoff; cross-check allowlist may add this seventh independent file.
Do not edit Foundry providers/runtime/journal/startup, independent tests, shared
ledgers, original or new recordings, package versions/dependencies or lockfiles.
Astra continues owned learning implementation. Use apply_patch for manual edits.

Remove unsupported inference that repeated init alone proves compaction/restart.
Preserve every init as immutable, appropriately unowned lifecycle/configuration
evidence with its binding; no attribution to the next admission. Explicit supported
compaction boundaries must still emit one accurate event with native provenance.
Do not infer compaction by merely comparing model/tools/config payload differences.
Keep foreign/duplicate/unknown ownership diagnostics and first-init preservation.
If an actual explicit protocol boundary can legitimately duplicate, handle only
correlatable duplicates, never discard a later genuine compaction by text equality.
No new native probe is needed: replay actual retained init envelopes and synthetic
supported positive/negative boundary cases. Do not rewrite historical captures
to pretend their earlier normalizer already had this correction.

Run sibling focused/full suites and source/script types, all SEVEN independent
native files explicitly (21 cases, including frozen recordings), with both source
manifests. Foundry types may remain temporarily incomplete during Astra's source
work; record test passes separately from a failing combined gate, do not fix its
files or endlessly rerun unchanged failures. Return exact evidence and limitations
for Astra opposite-model review and supervisor acceptance before I adoption.

Corrections to review wording: Claude dedicated native turn IDs remain unknown;
distinct admission/result UUIDs are not native turn IDs. The supervisor DID NOT
inspect raw MCP stderr, so its meaning is unknown, not privately verified benign.
Future rollback must preserve all newly journaled data, not restore an old snapshot
over newer records. Publishing/version0.2.0 is not authorized or required for
local adoption; propose a reviewed local dependency mechanism separately. The
existing MCP cli builds another thread in another process: adding IDs alone may
not expose the actual running thread/hot cache, so T still needs authoritative
runtime scoping and exact tool provenance, not merely a new launch argument.

Full CORE-002/003/004/005/AS-001..005 goal preserved: I/T, live events, usage/cancel,
scope/recovery/three-part learning, artifacts/tags/attachments/right-panel,
fork/rewind/subsessions and subscription-independent continuity. No new agents,
native probes, installs, credentials, live changes, restarts, commits or publication.
