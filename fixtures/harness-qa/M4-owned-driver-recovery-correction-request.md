# Correct the independently reproduced recovery loss and finish the handoff

Your1a6e076a ended with native result subtype error_max_turns, not a timeout of
parent observation: matching durable agent error1788848582241, execute trace error
and empty live buffers revalidated. The native transcript ends after your successful
06:23:02 read of docs/M4-owned-driver-parent-review.md. No final handoff was written.
This is a NEW continuation in the SAME Foundry/native session, not replay of the
previous work. Preserve partial source and retained real/controlled evidence.

Parent traced the cap: your exact live Claude command has --max-turns25; the
installed registry agent-session0.1.0 defaults null/undefined to25. Sibling source
already supports explicit null/unbounded, but is not installed in this server.
Do not try to patch the process, raise limits ad hoc, change profiles or restart
the server. The parent records this actual capacity loss for installed adoption.
Keep this correction focused so it can finish within the current legacy session.

You already read the parent review. Its P1 is now demonstrated by the finalized
fixtures/harness-qa/acceptance/owned-browser-late-recovery.test.ts, unchanged by
you. Stable RED0pass/1fail/4assertions at06:20:07-ca8c3472; strict types PASS.
The fixture closes its simulated child/server/socket during teardown; no real
process or OS signal. Kill refused, no exit, reachable host socket deleted after
the recovery wait. Fix that ownership loss and the analogous initial rejection/
host-loss paths. Observation expiry is not resource release. Expose unresolved
and late recovery through the minimal existing lifecycle/error contract; retain
the actual owner/endpoint until its outcome is known. Do not merely keep a temp
directory with no way for a caller to inspect/reconcile the resource. No new broker.
Audit the global process-exit scratch deletion too: it cannot erase the only
recovery path of an unresolved browser. Never blanket-delete directories matched
by a shared prefix; remove only your exact recorded resources after reconciliation.

The packaging regression is now independently GREEN1/8 with root types/diff:
.foundry/qa/2026-09-08T06-20-32.193Z-f4ae8b30-a240-4f9f-bfb4-0486dd20bf59-G3/report.json.
Keep this unchanged. Parent read your real evidence owned-driver-real-lCo14r:
graceful actual child45394 exits0; stalled-close actual child45421 exitsSIGKILL.
Those positive cases do not cover P1. Parent matching host/probe inventory is empty;
your public cleanup report is retained. Reconcile your exact resources; no broad
process scans for signalling or shared/user Chrome actions.

Ownership stays only driver, host, driver test and docs/M4-real-browser-driver-handoff.md.
The verified helper and parent fixtures stay unchanged. Astra2d3e707d is ACTIVE
on config/runtime/factory/startup source; do not touch its files, runner/composition/
checker, native transport, journal, UI, tickets or old artifacts. No background
agents. Use apply_patch for manual code edits. No installs/candidates/model
sample/live server/account/binding changes or commits.

Run the parent recovery and packaging tests, focused controlled driver/helper
regressions and strict types. Reuse retained real evidence when unchanged scope
warrants it, and explicitly rerun affected real behavior if your fix changes it.
Finish with a concise full handoff: source hashes, commands, failure/positive
evidence, resource outcomes, integration contract and genuine remaining gaps.
Do not spend the turn re-reading unrelated history or re-running broad suites
that cannot cover this defect. Native expert learning and configured lifecycle
remain the main product milestones, not complete from this driver correction.
