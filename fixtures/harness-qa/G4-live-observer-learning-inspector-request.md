# Correct live observation and learning inspection

Your read-only learning review ended04:45:35.008Z and reopened the slice. Supervisor is reproducing queue/lease findings and will return those to Astra after its current review terminal. Do not edit learning runtime, provider, sibling or persistence implementations. Count stays5/checkpoint6; all original goals remain.

You now own a bounded UI implementation in viewer/ui/store.js, conversation-state/inspector-data/detail-drawer and focused browser/unit tests. Narrow viewer route changes are allowed only if needed to expose existing durable data, not change learning ownership or journal semantics. No native probes, dependencies, servers/restarts, live config/binding changes, publication or detached agents. Use apply_patch for manual edits. Do not edit independent scripts/assertions or shared ledgers. Write a dedicated handoff.

Read QA-2026-09-07-learning-browser-and-cross-review.md and scripts/check-owned-learning-viewer.ts. Authoritative RED: .foundry/qa/owned-learning-visual/2026-09-07T04-43-19.771Z/report.json. A connected observer receives three owned WebSocket events yet misses externally submitted completed work without reload. Turn Context omits existing pending-learning delivery metadata. Six historical input checks pass at333/390/1440. The first earlier pending test was a false positive matching its turn ID; use the corrected final script unchanged.

Implement real conversation reconciliation on relevant owned events and reconnect/visibility transitions as appropriate. Preserve central active/inactive thread identity, local-only legacy and completed-unsaved evidence, streaming partials, durable message/turn IDs and deduplication. Do not replay work or replace owned message state with late foreign responses. Bound/debounce work; avoid repeated full-history fetches on every token and do not trigger native calls. Keep currently active stream output and scroll ergonomics intact.

Add a usable Turn Context rendering of immutable historical delivery/learning metadata: prior committed revision, pending/closed/failed state when recorded, zero added wait, and ownership/provenance availability. The selected trace is not the current runtime. Read matching durable message metadata if delivery is not in the trace; do not retroactively alter the trace or pretend current learning state was historical input. For current thread/layer inspection, make the existing knowledge endpoint accessible with domain review status, queued count/identities, latest committed revision, job ownership and clear requested-versus-acknowledged configuration limits. Keep instructions, domain knowledge and thread knowledge separately inspectable. Unknown old data stays unavailable, not zero/complete. Closed/non-admitted schema may be extended by Astra; handle explicit unknown future statuses without assuming pending.

Add focused tests for late fetch after thread switch, inactive thread events, deduplication, browser storage failure, local completed-unsaved output, no duplicate submission, selected historical snapshot unchanged after new learning, and unknown/missing historical metadata. Run corrected independent browser script unchanged plus existing memory-selection and storage/history regressions. Actual browser screenshot checks at333/390/1440, no incoherent overlap or horizontal overflow. Record all commands and stable-source evidence, not self-acceptance.

Use the bundled Playwright dependency via FOUNDRY_QA_PLAYWRIGHT as in existing QA commands. No change to production primary/lead servers. Return fixtures/harness-qa/QA-2026-09-07-fable-observer-inspection-handoff.md with implementation, tests, remaining gaps and instructions for independent reviewer. Do not spend the assignment only making labels green: the operator must genuinely see externally admitted Foundry work and inspect current versus historical knowledge.
