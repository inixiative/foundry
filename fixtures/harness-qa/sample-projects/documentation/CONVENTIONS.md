# Documentation Conventions

- Use sentence case for document headings.
- Commands must be exact, runnable commands in fenced code blocks.
- State what is persisted and what is only in memory.
- Never describe an unverified recovery path as automatic or guaranteed.
