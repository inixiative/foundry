# Preserve configured decision policy in the native loop composition

Your native pilot turn_a1b48208 is terminal: task_complete07:45:00.738Z, durable1788853500745, empty buffer. Parent read your entire L-native-fable-pilot-handoff. The failed original remains immutable, no retry or native acceptance is authorized. Continue the full goal with a narrow source correction in this same Foundry session.

Own scripts/domain-loop-composition.ts, a NEW focused test for the initial main Harness policy, and docs/L-main-harness-policy-handoff.md. Your pilot showed classifier/router configured and budgeted but never invoked: initial Harness only sets default executor while scoped Harness uses normal configuration. Reproduce this with controlled provider calls through the actual composition HTTP entry path. Correct initial policy construction using existing production configuration semantics, including enabled/disabled behavior, and prove it matches subsequent scoped Harness behavior without duplicating a divergent policy framework. Preserve main project/thread ownership and observed classification/route evidence. Do not weaken or alter existing tests to accept skipped roles. If faithful wiring exposes other controlled fixtures assuming zero decision calls, report the incompatibility and make only evidence-based explicit fixture changes after parent coordination.

Fable simultaneously owns ONLY DomainLibrarian advice protocol/validation and new tests. Parent owns packages/foundry/tests/domain-advice-protocol.test.ts, graph/ledger and independent audit. Do not edit Fable or parent files, providers, runtime, factory, routes, core, journals, protected old tests/checkers, immutable captures, installed packages or live state. No browser, new native model pilot, accounts, restarts, binding changes, commits or process kills. Preserve deadlines, tools/effort/model/call ceilings and unknown leases.

Also provide a read-only design analysis of pre-hook local expiry versus global admission closure based on the failed pilot and existing completionLifecycle ownership contracts. Distinguish local timeout, actual terminal provider rejection and unknown occupancy. Do NOT alter the budget latch/provider timeout in this task; that policy requires a separately reproduced boundary and review. Do not assert that the parallel advice protocol defect explains the measured native latency.

Return source hashes, RED then stable GREEN controlled behavior/type evidence, changes and unresolved limits in the handoff. This remains source preparation; no M4/L acceptance, release or count advance.
