# Implement the next native adapter and bridge boundary

DISPATCH READY11:39Z: parent freeze COMPLETE, both installs and installed package
independently verified. Candidate adoption-694bcdf8-4a78-43e1-a566-795f5c084aeb
is immutable; Fable will run read-only installed SDK/browser checks there while
you implement in ROOT/sibling only. R0 and reference source reviews accepted,
count7-next8. No review is still active on your owned root source. Historical
preparation conditions below have been satisfied. Proceed with implementation.

PREPARED, NOT DISPATCHED. Use same Astra/Foundry/native session only after actual
idle-correction/reference-review terminal and full handoffs, and reconcile Fable
R0 review findings first. Same full goal/count6-next8. This executes R1/R2 from
docs/G4-native-recovery-lead-handoff.md, not another general architecture study.

Readiness update11:36Z: both actual handoffs are complete and parent read full
opposite reports. Corrected R0 source960b... accepted as seventh bounded Astra
implementation slice, next checkpoint8/serious regression; full goal unchanged.
Fable reference source also accepted separately (does not increment your count).
Fable F1 handoff-staleness condition is resolved by your11:25 append, which it
missed in its earlier read. Other F2-F8 requirements must inform this integration:
local validation versus transport RPC labels, owned automatic-respawn preflight,
typed server-request replies/refusals (never unanswered requests), public refused
status, native interrupted/usage semantics, and required returned configuration.
Parent reproduced schema generation from installed Codex0.153.4 binary hash
a30ec314bbd0e3721632234d07db7c99855db3b9f1e32dbe8c791947f07e7629; fresh bundle
matches d3eace08... exactly. See QA-2026-09-07-reviewed-native-candidate.md.
One separate reviewed candidate is being prepared for bounded Claude verification;
do not modify it, its archive, source manifest or frozen dependency installations.
Only begin after supervisor confirms freeze complete. No native captures here.

Own the existing Foundry Codex adapter/provider/native-launch/configuration
surfaces and needed sibling Codex configuration/tool-event seam, plus focused
tests. Coordinate explicit ownership before editing bridge/transport/guard files
if their contract truly must change; prefer their existing owned lease APIs.
Do not undo Fable's reviewed public reference contract or replace engines.
Preserve current MCP/Claude defaults and all live sessions while adding explicit
app-server selection for isolated reviewed work. No silent migration, unknown
lease adoption or clearing bindings. A native thread/session-tree id is not a
Foundry logical thread id, an admission id, a tool call id or a fork id.

R1: expose deliberate app-server selection through production configuration,
adapter construction and immutable requested/observed provenance. Use stored
binding on cold construction, exact original prewrite owner registration and
required native idle state before turn writes. Keep native model/effort/defaults
and all tools; report unsupported or unknown settings honestly, never silently
weaken them. Read the installed protocol's supported options, not host UI labels.
Separate owned exit/bridge cleanup from native terminal/RPC/local observations.
Preserve text-only/auxiliary policy refusal unless actual native enforcement is
implemented and tested; do not use a weaker implicit fallback to make it green.

R2 tool path: pass the original owned bridge's structured configuration into
native thread/start AND thread/resume, preserving other native tool configuration.
No arbitrary user config/credential scraping or raw private configuration in
evidence. Establish exact thread-scoped mcpServerStatus/list tool readiness before
a tool-capable turn; missing/failed/foreign readiness must not quietly degrade
the session. Rotate per-process capability/endpoint through current lease APIs;
stale or foreign bridge calls stay refused. Use installed-schema MCP ThreadItem
events to retain server/tool/arguments/result/error/call-or-item identities
truthfully, with no guessed join or private/hidden payload pass-through. Preserve
public text/output streaming; unknown usage stays unknown. The existing guard
must join actual owned bridge receipts, not accept mere tool-name mentions.

History and recovery: native resume preserves native conversation history; keep
historical input/artifacts/trace snapshots immutable and separated from the new
admission's events. Implement the minimum honest history/status inspection needed
for resumed work through existing viewer/journal APIs; verify exact pagination
method/schema names before use. Do not label same-thread continuation as native
fork or a client-only clone as a native subsession. Record remaining full history,
fork/rewind/subsession work explicitly in the unchanged long-term graph.

Verification: focused RED-to-GREEN production adapter/provider/route/journal and
owned SDK tests for fresh/warm/cold/Foundry-restart settings, absent/active/foreign
native status, no duplicate/replay, late native/RPC/transport/cleanup, exact tool
readiness and outputs, no cross-project leakage and durable binding failure.
Parent recovery/outcome12, learning6, references4 and old independent ownership/
cleanup/provenance fixtures stay unchanged. Add tests matching new public APIs,
not a mock that starts the bridge itself by bypassing the native config boundary.
Opposite review and exact immutable package/candidate provenance precede native
capture; avoid repeated broad baseline/staging while scoped failures remain.

Produce docs/G4-native-adapter-bridge-handoff.md with implemented interfaces,
scope hashes/tests, remaining gaps and runnable isolated acceptance entrypoint.
Extend the existing opt-in disposable sample runner only after the real adapter
works, keeping plan separate from execution. Native sample authorization remains
separate: fresh tool turn, known terminal/owned exit, new process same-binding
resume plus refreshed scoped tools, next tool turn, native/journal/old-fresh
browser evidence and timing. No work/review session as test target. No model
probes, live restart, root installs, credentials/accounts/binding changes,
commits or publication in this source assignment. Use apply_patch. Full causal
learning/artifact/tag/inspection/pooling/model-effort/latency goal remains open;
no self-acceptance or count advance.
