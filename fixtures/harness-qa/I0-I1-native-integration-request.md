# Lead delivery: adopt native outcomes into working Foundry sessions

Prepared for explicit supervisor dispatch after R1 acceptance and prior native
terminal; required learning/UI corrections below are part of this integration
delivery, not an excuse to postpone all package/outcome work. Continue as Astra
lead in the SAME session. Do not repeat architecture
planning: implement the reviewed docs/I-native-integration-review-and-plan.md
I0/I1 delivery, with checkpoints and actual disposable Foundry sample runners.

Dispatch update: your G4 review actual task_complete06:03:11.856Z and Fable
learning/R1 review actual end_turn05:56:13.849Z are verified; both complete
handoffs read. Supervisor accepts R1 as your sixth bounded slice, retains the
provisional lead, next checkpoint8 or serious regression. Earlier cleanup defect
was supervisor-found and you corrected it; do not repeat the review's self-found
attribution. Owned learning remains subject to the cleanup-refusal interaction
below and real native verification. The broader UI slice is REOPENED, not accepted.

Fable is actively implementing T1 from T1-live-scoped-tools-request.md in the
same primary session, Foundry turn_f31b9283-2782-4e9b-8e22-f73f18365e5c. Preserve
its MCP/tool files. BEGIN this I0/I1 implementation now. Complete product behavior
and usable sample runners are required, not another architecture-only handoff.

## Necessary corrections at the integration boundaries

Own the four narrowly related corrections in your docs/G4-observer-inspection-
review.md while extending those same outcome/observer paths: complete nested
message comparison; actual thread/turn-owned journal-change notification for
pre-dispatch failure (including inactive owner); malformed knowledge history
validation with recovery; partial fixture setup and bounded cleanup/failure
reporting. Keep original120s timeout evidence, cause unresolved; your stable40
rerun does not erase it. Parent observer-metadata-reconciliation.test.ts now
independently fails on metadata-only native reconciliation, with unchanged-cache
identity positive:06:03:41.021Z-b8133ac5-04aa-4b0b-a06e-1f73a66a512c-G4. Preserve
frozen delivery while updating new authoritative evidence. Do not edit that test.
The request-ordering/no-store correction itself holds; preserve its browser cases.

Fable review's learning cleanup interaction needs explicit no-send/admission
evidence: a review refused while its owned process exits must be durably deferred
or genuinely queued until eligible, not mislabeled an executed reviewer error.
The current error record DOES carry job/evidence/reason in source; verify rather
than repeating a claim that all evidence is absent. Do not classify every settled
HTTP error as non-admission: a completed503 was actually sent. Use the new typed
admission capability, preserve original errors, and retain pending/late ownership
without adding serial central wait or replay. Native MCP learning settlement still
requires I2; no forced HTTP fallback can establish native parity. Surface capability
limitations honestly until I2, preserving explicit provider/model selection.

Full CORE-002/003/004/005/AS-001..005 goal stays intact. Own native package staging,
provider/adapter and additive core executor/trace metadata, factory/runtime/journal
projection, narrowly needed UI outcome inspection and focused tests. Fable's T1
MCP authority/retrieval modules are disjoint; agree on typed owner/admission
interfaces without overwriting them. Supervisor owns independent tests/shared
graphs. Preserve every unrelated dirty change and both running work bindings.

## Package and compatibility checkpoint

Implement fail-closed staging of reviewed sibling package.json, src/**/*.ts and
license only, with hashes, symlink/path/collision checks, frozen source manifest,
tar-entry verification and controlled destination. Exclude recordings, .qa,
private native tapes, .foundry, credentials and unrelated files. Never edit
node_modules or adopt a moving sibling symlink. No publication/version invention.

Authorize packaging and dependency resolution ONLY inside new isolated review
directories, not the current root's installed runtime or live data. Stage the
reviewed DIRTY Foundry implementation with an explicit source manifest; HEAD alone
omits the accepted work. Use an immutable file tarball and minimal lock resolution,
scripts disabled, then a second frozen-lockfile install and actual package import
path/content check. Write reusable staging/adoption utilities in source. Retain
the generated reviewed artifact/manifest and propose exact root manifest/lock
changes for separate rollout; do not switch the running sessions' dependencies.

If an additive sibling interface is required to register logical ownership before
native write, implement the narrow existing-engine change with offline tests and
regenerate the frozen artifact from that reviewed candidate. Do not add an engine
or bypass the recorded native ownership/terminal semantics. Record separately any
new source that still needs opposite review before live model use.

## End-to-end outcome checkpoint

Make unchanged native-adoption-boundary.test.ts pass against production provider
and the actual installed candidate, not relative sibling imports. Carry immutable
typed admission/native session/thread/turn and local/RPC/transport/terminal evidence
through provider, executor, trace, factory, HTTP/SSE and durable message/history.
Register dispatch/review job and runtime owner/generation before native write;
never infer identity by message text or the most recently active work.

Keep normal completed output after later local, transport, guard or SQL failure.
Persistence failure is completed-unsaved, not a false native failure/durable ACK.
Unknown stays unknown, legacy compatible. Retain original errors and partial text
for genuinely failed native work. Late events append to the original admission
without mutating old observations or attaching to a subsequent turn. Add scoped
public native events and tool begin/result identifiers to the existing pipeline;
no global session bucket leaking projects, hidden reasoning or raw secret fields.
Expose sufficient owned settlement to unblock I2 without replaying unknown work.

Address the repeatedly observed central25-turn capacity loss in this delivery.
New independent native-turn-budget.test.ts is1fail/2pass at05:49:01.014Z: actual
installed class/provider imposes --max-turns25 when the operator requested no
coding cap; explicit48 and auxiliary1/tool restrictions pass. Make the central
no-cap policy explicit and native-supported, preserving explicit operator budgets,
auxiliary limits and legacy standalone behavior where possible. Do not merely
raise25 to a different hidden ceiling, strip arbitrary native argv or restart a
live process to apply it. Test supported unset/unbounded representation, invalid
values, clone/resume preservation and request-versus-enforcement inspection.

## Test and real-work readiness checkpoint

Test production routes and real candidate classes over controlled streams:
typed failure/contradictory legacy success; native success then local/RPC/SQL
failure; soft timeout versus terminal/physical occupancy; wrong/duplicate/late
evidence; write/observer failures; completed-unsaved browser recovery; profile
and immutable provenance; prewrite registration; retained bindings; model/effort
unknowns and no hidden central cap. Preserve all existing independent assertions,
26 memory cases,16 learning cases,2 cleanup,15 model cases and21 sibling/native
cases. Report source and packaged fingerprints, expected S4 failures separately.

Build an opt-in disposable Foundry runner for BOTH engines: one isolated production
viewer/runtime/journal and controlled sample project per engine; exactly two
read-only sentinel admissions on one native process/binding, known first terminal
before second, no retry on unknown. Record actual package, prepared input,
tool/result/terminal/trace/journal linkage, visible and fresh-browser history,
requested/observed settings and owned cleanup. Dry-run/offline guards execute now;
REAL MODEL EXECUTION requires supervisor inspection and separate bounded dispatch.
The sample is part of this delivery, not an indefinite packaging-only milestone.

No primary/lead server restart, live binding/account/config change, native probes,
new agents, commits or publication. Rollback must keep every new journal event,
knowledge revision and binding, with backward-readable unknown-lease guards; never
restore an old database or silently return to unsafe registry timeout behavior.
Return docs/I0-I1-native-integration-handoff.md with implemented checkpoints,
exact commands/results, unresolved facts and the ready real sample command.
Supervisor/opposite review decides acceptance and rollout. T1/T2 native retrieval,
I2 causal learning and actual both-engine work immediately follow, with full
artifact/tag/right-panel, lineage/subsession, effort and subscription goals intact.
