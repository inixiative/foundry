# Complete a connected, user-operable historical expert inspector

Continue the full CORE-006 objective in this existing Foundry lead session.
Your04a41bd3 task is terminal10:08:08.416Z/durable1788862088434/empty. Fable's
d4e0be4a is now terminal end_turn10:32:31.993Z/msg_011CeqpVTjB6puaMU7PYizhc,
durable1788863552106/empty. Parent verified those terminals and read FULL
docs/L-phase-boundary-repair-handoff.md and its code/tests. Fable is idle; you own
the source and owned browser slot. Parent will only read during your measurements.

Read docs/L-phase-browser-parent-review.md first. Parent independently verifies
55tests/371assertions/package types/diff with stable source in G3
10:35:21.343Z-2ffccddc. The three original boundary regressions now pass unchanged.
Do not revert those repairs or claim full LI from this bounded gate.

Complete the user's actual workflow, not just a report with a green flag. Own
scripts/phase-history-browser-qa.ts and focused necessary viewer/runtime phase
notification fixes/tests. Read existing owned browser and viewer setup patterns.
Use real runtime/HTTP/file-backed journal and controlled responses with the actual
app, not a separate panel mock. Do not modify owned browser drivers, LS/copy code,
native transport/model/account/install/live bindings, or protected assertions.

## Required experience

- At desktop and390px mobile, navigate through visible app controls. On mobile
  use Workspace views Chat/Inspect tabs to bring the conversation/inspector into
  view before ordinary Playwright locator clicks. No page.evaluate clicks, hidden
  button invocation, force:true, private globals or signal manipulation. Read-only
  DOM measurement is fine. Verify required text is visible and correctly scoped,
  not merely present somewhere in allTextContents. Scroll to the actual target
  sections; screenshot what the user can inspect and test overlap/overflow.
- Keep a selected historical inspector open while original guard and post-review
  calls are pending. Release those original controlled calls, with no new message,
  page.goto, reload or re-selection, and verify their exact durable outcomes and
  the expert's own revision appear through the real connected update lifecycle.
  Include failed/malformed/not-admitted outcomes without all-clear. Do not add a
  central learning wait or overwrite the old message's frozen inputs to refresh it.
- Send only Continue for the next central turn. Check the exact revised expert
  understanding is consumed in provider/injection evidence and visible inspection.
  Retain all three owned parts separately from phase protocol and shared evidence.
- Actually render configured-writer quarantine/refusal, legacy absence,
  uncorrelated guard history, and reopened original input after current instructions/
  domain cache change. Check precise guard finding counts/status instead of the
  previous generic Findings fallback. No fabricated event/turn/native ownership.

## Make the QA runner trustworthy

Use explicit127.0.0.1 ephemeral servers with the real WebSocket upgrade/event
subscription pattern (see scripts/native-domain-loop.ts server setup). Resolve
unintended HTTP400/console/reconnect noise rather than blanket-ignoring it. Expected
injected failures need scoped explicit expectations; all unexpected page/console/
request/required-capture failures make both report.passed false and exit nonzero.
No pass from failed assertions or missing screenshots. Test the report/exit rule
with small pure checks, using an exported helper only if it genuinely simplifies
the runner. Do not replace product checks with static source-string assertions.

Reserve a new output directory exclusively for each run. Existing attempt logs,
SQLite, screenshots and reports are immutable, even when failed. The old author
reused the directory and removed runtime data; acknowledge that retained logs are
not full old artifacts. Do not recover/recreate those as if original. Record source
hash, actual command, browser acquisition/close/exit and server/journal cleanup.
Wrap all construction/acquisition/test/report failures with the existing owned
cleanup mechanism. Never signal a bare PID. Earlier run4 PID40719 is currently
absent by parent read-only ps, but had no retained exit acknowledgment.

No CUA or Mac lock bypass; use owned headless Playwright at the known runtime path.
No user's browser, live4400/4407 restart, new model call, pilot/candidate installation,
process kill, account/binding change, count/release advance, commit or schema reset.
No timeout increases or retries until green. A changed-code correction may justify
a fresh retained run with the original failure kept; explain it. Bound iteration
to actual faults, and hand off an unresolved failure rather than manufacturing a
passing scope. Do not run another broad full-suite matrix.

Deliver docs/L-connected-inspector-handoff.md with findings first, actual screenshots
and exact stable-source checks, first-failure evidence, ownership/cleanup and open
items. Do an opposite review of the latest phase corrections while touching these
paths, especially full original correlation and write-failure truth; keep current
source tests and parent contracts intact. The full native learned-next-turn,
durability/recovery/subsessions/tools/lineage/artifacts/tags/latest-model fidelity,
Herald and matched Astra/Fable latency/capacity requirements remain on the graph.
LS follow-up is prepared separately; do not run sampling or copy diagnostics here.
