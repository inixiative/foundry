# LW completion: preserve sender terminal truth across live snapshots

Continue in this existing Astra4407 session. Your LW task is terminal, full handoff
read, but independent parent review found a reproduced blocking data-preservation
bug. Read docs/L-live-watch-parent-review.md and the unchanged new parent
packages/foundry/tests/live-watch-terminal-contract.test.ts. Read the prior
L-live-watch-repair-request.md; its full watchability, isolation, ordering, evidence,
cleanup and no-live-adoption requirements remain.

You own the sole source/test/browser slot again; Fable remains spend-limited and
parent stays read-only. Reproduce the protected RED unchanged, then repair the
actual source precedence. A sender's terminal outcome is independent of whether
its journal write succeeded. Cached active snapshots must not overwrite its full
answer, change its local execution back to running, erase failure or lose rich
browser-only evidence. A later bounded terminal snapshot must not replace a full
sender result with truncated preview. Keep late native/RPC activity separate and
truthful; do not drop every later original event or hide pending/unknown capacity.

The production _persistLocal path always merges cached live state after its SSE
terminal patch. Test that actual order, not just merge against an already terminal
snapshot. Add controlled actual-browser sender coverage: hold an active watch
snapshot, receive a full >32768-character completed answer with journal failure,
then let terminal snapshots/durable reconciliation catch up; verify full output,
local completed/unsaved status, rich evidence and no replay. Also cover original
terminal failure and late unknown/RPC handling. Use the actual composer and ordinary
visible UI controls, real routes/native controlled provider/journal and production
store. If delaying snapshot response via a test server/browser route, retain real
response bytes and explicit ordering proof; never inject fake positive UI state.

Preserve the prior external-sender observer scenario and55/23 browser coverage,
all P02a/P02b and original parent contracts, actual owned cleanup, stable source,
strict/package types and old timeout budgets. The new permanent parent test is
protected and must remain byte-identical; add separate controls as needed. Keep
new/touched state logic readable enough to review precedence explicitly; do not
bury complex ownership decisions in compressed one-line conditionals.

Retain every attempt in a fresh exclusive directory with exact command/PID/exit
receipts. Inspect actual screenshots. No fake or mislabeled engine/native proof,
private reasoning/tape scraping, account/model/binding change, package install,
candidate mutation, live4400/4407 restart, commit, native model experiment or CUA
retry. Actual owned Playwright fixtures are permitted. Earlier combined32/7/4skip
is still failed and unrelated; do not relabel it with a focused pass.

Write docs/L-live-watch-terminal-repair-handoff.md with causal diagnosis, source
semantics, protected RED/GREEN, actual browser sender/observer ordering evidence,
cleanup, final hashes and remaining gaps. Complete the task for independent review.
The user's actual preserved live server remains unadopted until a separately
reviewed immutable installation and native/live plan; do not claim these source
tests changed what the preserved process loaded.
