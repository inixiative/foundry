# Parent authorization: one integrated immutable candidate

2026-09-07 ~13:56Z. Parent accepts the bounded preflight source plus corrected
portable tests after Astra's source review, Fable's final opposite review
end_turn13:53:12.888Z, matching durable final and complete review read. G6 metadata
correction independently accepted after Astra13:47:42.334Z/full review and parent
46focused/7scratch/4productionroute checks. Root baseline320core/1044Foundry/30gates
passes types/diff at13:51:48, one explicit historical opt-in skip. Separate actual
historical opt-in passed35combined tests and original capture hashes still match.

Authorize parent execution of exactly ONE prepare-native-adoption.ts --execute
using root foundry, sibling ../agent-session and existing .foundry/qa parent, after
its read-only plan and script hash check. The existing orchestrator alone stages
one seven-file native archive, freezes allowlisted dirty source, creates unique
adoption directories, runs scripts-disabled first and frozen-lockfile second
installs plus explicit Prisma generation with no engine, and checks both imports.
No root install or source manifest widening. Prior candidates/captures immutable.

Reviewed production runner6419617e58d47715a458eec42026f2ab948863fe65bec2f12250e57a1e547996;
portable tests9418b5d4/02bc7a9f; config-resolve065d19dd/configd6538c59.
Both native actors now own read-only/scratch work. No production movement is
authorized during this freeze. Record exact new run/source/native/package/lock
hashes and verify copied source before using candidate. After freeze, parent may
assign disjoint root UI implementation while frozen checks continue.

This authorizes candidate creation/install verification ONLY, not rollout or
model capture. Next: complete source/package/lock/archive provenance, baseline,
installed SDK/recovery suites in both copies, real --recovery-preflight with no
model work, then SEPARATE bounded native recovery authorization. If an install
fails, preserve its unique report and investigate; do not mutate an old candidate
or silently repeat the orchestrator. No live4400/4407 restart, database restoration,
account/credential/binding changes, commits or native/model work. Full goal and
count eight remain unchanged; a candidate is not capability/latency acceptance.
