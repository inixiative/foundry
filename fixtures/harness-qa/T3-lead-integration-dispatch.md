# Lead delivery: corrected transport review, then native bridge integration

Prepared for dispatch only after your current I1 path correction has an actual
native terminal. Continue as Astra lead in the SAME Foundry/native session with
the full CORE-002/003/004/005 and AS-001..005 goal, count six/next checkpoint eight.
This is implementation, not another architecture-only milestone. Fable is now
read-only reviewing your frozen path-correction candidate; its T2 work is handed
off and no longer concurrently edited. Supervisor owns actual new Claude capture.
Do not run samples, restart or change root installed dependencies under this task.

## Opposite review of the corrected T2 boundary

Read docs/T2-live-transport-handoff.md Corrections4/5 and actual MCP source/tests,
your prior docs/T2-opposite-integration-review.md and the prepared
fixtures/harness-qa/T3-native-bridge-integration-request.md in full. Fable actual
Correction5 ended08:05:54.550Z. Supervisor independently ran all68 MCP cases with
420assertions/types/diff, stable3960a464685776a6ac8d8c496101020d13b285de2ef000919c842c34753846c0:
.foundry/qa/2026-09-07T08-06-49.493Z-2a119a9e-e8dc-42ce-b8f0-b23a2a17c879-G5/report.json.
This includes unchanged25 independent tests and all43 focused tests; initial
disposal/accounting1pass2fail evidence remains in the prior QA section.

Verify shared close-entry revocation of primary and SDK-removed authorities,
detach-only disposal-hook cleanup preserving valid late reads while the bridge
is open, explicit pending/completed/failed close accounting, original late records
and immutable launch descriptors. Check that close owns the COMPLETE cleanup
chain if SDK DELETE already removed a session from live before transport.close
settles and then server.close starts: one snapshot of pending promises may miss
subsequently enqueued close work. This is a supervisor source-level question, NOT
a reproduced defect; use a held transport/server pair to settle it if needed.
Do not assume all production cleanup promises always settle simply because the
default fixtures do. Unknown/pending is distinct from completed and native cancel.

Record a bounded opposite verdict in docs/T2-opposite-integration-review.md before
starting integration, preserving prior findings/evidence as history. If a remaining
blocker is reproduced, you now own the smallest necessary MCP correction and its
focused tests as part of this integration delivery; do not weaken independent
tests or claim self-acceptance. Supervisor/Fable will review these changes. Avoid
another idle handoff cycle for a small inseparable lifecycle correction. If it
would require a materially different security/ownership design, report that exact
blocker and continue dependency-ready implementation without native activation.

## Implement T3 against the current production components

Follow the full contract in T3-native-bridge-integration-request.md and your T3
contract in the opposite review. Deliver actual provider/adapter/factory/runtime
wiring, owned per-process launch configuration, prewrite admission association,
public tool argument/result journaling and existing inspector projection. No new
session engine or alternate harness. A bridge lives with the owned runtime/native
process; it is not recreated on each warm turn. Auxiliary sessions never receive
its capability. Preserve unrelated central tools/config, model/effort and explicit
unbounded central budget; no silent warm-binding replacement or unknown replay.

Use the existing engine launch seams. Verify exact local installed CLI help/schema
before adopting flags, without creating a model session or changing saved MCP
settings. If a narrow additive launch option in the sibling is inseparable, own
that option and both-engine offline tests, preserving all previous behavior and
recording precise source/export hashes for opposite review. Do not publish or
edit installed node_modules. Fresh isolated candidates may be staged/installed
using the reviewed existing scripts only; never mutate old candidates or archives.

Freeze per-operation association at start and distinguish registered admission
context from actually observed native call/item IDs. Do not infer joins from the
latest turn at completion. Journal public results/digests and separate per-record
pending/committed/failed persistence status; preserve immutable old observations,
late history, failed setup and publication failure without execution replay. Add
sanitizer coverage before any new launch configuration could be recorded.

Prove actual-class controlled provider/HTTP/SSE paths, real SDK proxy, one bridge
across two admissions, auxiliary refusal, foreign/stale owner, warm incompatibility,
registration/write/observer failures, native terminal/RPC split, completed-unsaved
journal failure and old/fresh inspector reconciliation. Preserve existing I1,
T2, memory/learning and browser assertions. Add the opt-in both-engine omitted-fact
retrieval sample described in T3-native-bridge-integration-request.md, but do NOT
execute it; supervisor/opposite review will authorize actual capture promptly.

I2 owned-learning settlement follows T3; preserve its exact-owner lease interface
and hard-expired commit latch. Do not make central work wait for background review.
Keep native cancellation/stream/usage, artifacts/attachments/tags/right-panel,
fork/rewind/subsessions/lineage, subscription pooling and measured capacity/latency
requirements in scope. Basic sentinel success does not satisfy these requirements.

Return docs/T3-native-bridge-integration-handoff.md with implementation, exact
controlled checks, candidate source/installation provenance, remaining gaps and
ready real sample commands. No root install/live restart, accounts/credentials/
binding changes, model probes, additional agents, commits or publication. Preserve
all dirty work and prior journals; rollback keeps evidence and unknown leases.
