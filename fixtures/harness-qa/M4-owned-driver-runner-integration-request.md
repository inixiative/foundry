# Integrate the verified owned driver and preserve late recovery evidence

Your03c0397a completed native end_turn2026-09-08T06:37:29.682Z with matching
durable final1788849449788 and empty live buffers. Parent read FULL final
docs/M4-real-browser-driver-handoff.md and the262-line driver implementation.
Independent narrow stable driver/helper/source gate:
.foundry/qa/parent-owned-driver-final-72o3D9/report.json
30pass/224assertions,2explicit real-browser skips, strict types/diff pass,
all8owned files unchanged. Parent P1 recovery and packaging regressions are GREEN
unchanged. This accepts the bounded driver correction, not installed/native work.
Retained author real evidence vCumWX covers graceful and actual owned fallback
on final driver5aade87d and hostc5e2bd6b; no parent real rerun is claimed.

New task: integrate this driver into the existing native-loop observer/reporting.
You now own scripts/native-domain-loop.ts, narrowly needed native-loop-proof.ts,
scripts/check-native-domain-loop.ts, required-source names ONLY in
scripts/domain-loop-composition.ts, focused native-domain-loop tests and
docs/M4-owned-driver-integration-handoff.md. Do not edit the verified driver,
host/helper/their tests or parent fixtures. Report an unavoidable helper contract
dependency before changing those files. No second runner or broker.

Astra2d3e707d remains ACTIVE on E1 config/resolver/factory/runtime/startup and its
focused tests. Do not edit those, native transport/journal/UI/tickets/package/lock
or the parent-owned scripts/check-native-adoption.ts. Parent added actual installed
turn-budget argv checking there (6tests/26assertions plus actual legacy rejection/
sibling pass), not a deployment. Account for source movement in broad gate results;
do not claim a stable global baseline from moving E1 files. Preserve all dirty work.

Replace bare chromium with createOwnedPlaywrightDriver for the real browser path;
controlled injection stays explicitly gated. Include driver and host in the frozen
required source/fingerprint closure and execute focused owned-driver/parent safety
tests in the existing checker. Preserve original launch10s/readiness8s budgets and
the real old/fresh1440/390, disconnected readiness, module/refusal, failure ordering,
immutable report and journal reconstruction behavior. No relaxation of assertions.

Integration is MORE than one substitution: helper acquireOwnedBrowser wraps the
entire driver launchWithProcess in launchMs. If that outer observation expires,
its later rejection branch currently emits only launch:late and discards the error.
Capture recoveries at the driver invocation boundary so a late
OwnedBrowserHostUnavailableError, or original launch error with recovery, is still
registered and inspectable even after the outer helper rejected. Use existing
runner hooks/owner tracking with bounded status snapshots, not serialization of a
live object or a callback that can discard the owner's references on write failure.

Retain original failure before cleanup. Store recovery status and exact owned
endpoint, child identity/exit evidence and cleanup outcome. Reconcile boundedly
without silently dropping unknown work, repeated launches or arbitrary PID signals.
Host loss is NOT Chrome exit: unresolved/host-lost must remain failed/unresolved,
not cleanup success. Late authoritative evidence may append checkpoints/progress
but cannot rewrite the original failed report into PASS. Preserve recovery access
through constructor failure, before an observer object exists.

Add focused controlled runner tests for immediate recovery failure, outer launch
expiry followed by a late recovery-bearing rejection, refusal then authoritative
late exit, and known exited versus host-lost outcomes. Assert zero native admissions
on failed construction and unchanged original final report after late evidence.
Do not weaken the parent artifact verifier to accept failed reports. Also run a
normal isolated real-browser observer scenario through the new driver and retain
its exact host-child exit evidence; no other actor currently owns Chrome.

Use apply_patch for manual edits. Retain failed runs and exact resource cleanup.
Keep the task focused; the live legacy session still has the25-turn cap. Finish
with changed hashes, commands/evidence, bounded limitations and an integration
handoff. No installs/candidates/native model sample/live server/account/binding/
shared-browser changes or commits. The next objective remains actual installed
unbounded central tools + native expert learning, then live configured experts.
