# Deliver usable long history and historical inspection

IMPLEMENTATION AUTHORIZED after your actual opposite-review end_turn13:53:12.888Z,
matching durable final and complete handoff read. Parent accepts the bounded
preflight-test correction. G6 configuration correction is also independently
accepted. Parent created the ONE immutable adoption-524ce5d1-0612-4128-88a0-b3af6bcb9466
candidate at13:56, verified407source files (405unchanged plus2intentional package/
lock edits), seven installed native files in both copies, archive and proposal.
The first real no-work installed preflight passed13:58. Candidate baseline/SDK/
native recovery remain parent's work; NEVER modify either frozen candidate copy.

Continue SAME Fable session and implement this root UI/persistence slice now;
do not stop at another proposal. Own viewer UI/message-history/detail routes,
the minimal LocalSessionStore index/detail/pagination queries and focused tests.
Do not change native adapters/providers, core lifecycle, configuration segment
work, staging, sample runner or sibling. Astra's G7 diagnosis is read-only and
parent verifies the frozen candidate, so coordinate only if another boundary is
genuinely required. Root changes do not retroactively alter candidate evidence.

Read QA-2026-09-07-long-history-parent-review.md and your complete browser baseline.
Parent viewed3screens and confirmed source matches. Keep18actual PNG count, avoid
claims of completed large mobile/offline scenarios (large run stopped at reload),
and do not call the asynchronous browser-byte observer's0 a zero-byte payload.
The large mixed snapshot exceeds quota; don't assume it occupied storage first.
Use fresh unique output directories for ALL after-runs, preserving baseline tests,
reports and screenshots. Build explicit after-acceptance assertions, not edits
that turn assertions of broken baseline behavior into feature passes.

Read CORE-002/003/004, QA-2026-09-07-history-payload.md and the live-inspection
semantics QA. The goal is an actual usable session, not just a smaller response.
The observed 100-message Fable history is 17,369,507 bytes, mainly metadata, with
100 optional-cache notices in a fresh browser. These are unequal live histories,
not a model comparison. Preserve every server record and all browser-only work.

## Delivery contract

Use existing SQLite journal, routes, conversation state and inspector patterns.
Add a lightweight bounded message index and stable cursor pagination, with lazy
owned turn detail for complete provenance, native events/tools and artifacts.
Choose an explicit compatible API contract and document it before the source edit;
do not silently make existing full-history consumers receive partial detail.
Preserve stable message/turn/thread/trace identity and semantic status in summaries.
Durable, interrupted, failed, completed-unsaved, native outcome unknown and pending
are distinct. Missing detail must not imply empty detail or lost work.

Current LocalSessionStore.messages hydrates nativeHistory/nativeTools per row,
including user rows that cannot use them. The route also returns all thread tools
at top level. The new index must not hydrate hidden full detail just to discard
it on serialization. Detail remains retrievable through existing or additive
owned routes. Never delete, recompute or relabel recorded injection artifacts.
Trace snapshots must remain the historical selection, not today's layer cache.
The existing Prisma fallback must not silently claim empty history on failure.

Update initial load, reconciliation and older-page UI together. Pagination must
actually reach the oldest record, tolerate concurrent appends and repeated cursor
requests without gaps or duplicates, reject malformed/cross-thread cursors, and
retain request ownership when switching threads or selecting older details.
Do not merely raise the100/200 defaults or eagerly download the entire history.

Separate optional durable index caching from transient browser-only evidence.
Legacy unidentified browser history, completed-unsaved output, late metadata,
partial output and browserFailureEvidence cannot be discarded because a server
row exists. A summary is not a replacement for uncommitted detail. A saved older
snapshot does not mean a changed row is saved. Quota/security/serialization failure
keeps the old copy and current in-memory evidence; show actionable scoped status,
not100 repetitive notices for durable rows. Do not strip model/provider input.

Historical detail must be navigable from messages: main message, each decoration's
instructions/domain/thread parts, exact prepared provider input, provenance,
native public events/tool inputs/results, associated artifacts and selected turn.
Render readable wrapping on desktop/mobile rather than raw unbounded JSON only.
File/artifact links must use explicit authorized workspace ownership and existing
safe file-serving patterns; no arbitrary local-file exposure, hidden reasoning,
credentials or unrelated native tapes. Unsupported native history/fork/subsession
features remain explicit gaps, not falsely represented by local copies.

## Verification and scope

Build deterministic long-history fixtures in disposable workspaces from the
release-notes, documentation and tool-workbench sample projects as appropriate.
Include >300 messages, overlapping turns, multiple threads, large injection/native
detail, legacy browser rows and completed-unsaved/error evidence. No new model
call is required for these browser/index tests. Preserve existing failure-storage,
completion, reconciliation, inspection, thread and journal tests; add regressions
for every boundary above, durable reopen and old/fresh browser behavior.

Measure exact initial/older/detail payloads against equivalent seeded histories,
not unequal live engines. Use real production routes/SQLite and Playwright at1440
and390 widths, click through older turns and artifacts, retain and inspect PNGs,
assert no overlap/page overflow, no missing text and no duplicate work dispatched.
Quota-denied, offline, delayed/out-of-order and inactive-thread cases are required.
Separate controlled transport/browser evidence from actual native capability.

Deliver docs/G6-history-index-and-inspection-handoff.md with API/cache choices,
exact source hashes, commands/results, before/after payloads, screenshots and
remaining scope. No live4400/4407 restart, root install, credentials/accounts or
bindings change, model probes, staging/candidate mutation, commits or other-model
dispatch. Full three-part lifecycle, native recovery/learning, models/pooling,
session lineage and matched decision-latency work remain on the original graph.
