# Diagnose the installed prewrite refusal failure

Continue SAME Astra session after actual G7 task_complete14:15:10.370Z/native
01a07c25-35c8-7403-ac7d-cdef21defdf8, matching durable final and COMPLETE G7 diagnosis
read. Parent retains the bounded D1 proposal for later disjoint implementation;
do not implement G7 now. This installed-check failure gates the actual native
recovery experiment. Fable continues root UI work; don't interrupt or overwrite it.

You may diagnose, then make a narrowly evidenced ROOT test/fixture correction and
deterministic regression if a fixture defect is established. No runtime source,
frozen candidate, sibling or UI edits in this assignment without coordination.

Integrated immutable adoption-524ce5d1-0612-4128-88a0-b3af6bcb9466 was created once
after all opposite reviews. Both real no-work installed preflights pass with full
source/native/proposal identity. First-copy full baseline passes. The subsequent
installed SDK/recovery/bridge/outcome/learning suite produced82pass/1fail/702assertions,
stable3e9415345680d7018fb9010f1081816d3b0e442b40525f60a8c1979bafbfee88:
foundry/.foundry/qa/2026-09-07T14-00-51.894Z-13ee0f62-de7c-4746-82ca-06a601c84027-G5/report.json
inside that adoption. Failure is bridge.test.ts's "mcp: rejected prewrite registration
prevents SDK tool operation and native write", MCP error-32000 Connection closed.
No actual model work occurred; these are real installed classes with controlled
native transports and an actual SDK stdio proxy.

Parent isolated rerun of that exact case passed1/5assertions; a second run with
scratch delay-proxy-start.ts (150ms delay before fixture Client.connect) also
passed1/5. Neither resolves the original failure; do not pass it by retry count.
The scratch preload lives in .foundry/qa/parent-installed-prewrite-20260907 and
does not modify frozen files. Candidate lock diff changes only the agent-session
dependency/archive entry; no evidence of an unrelated SDK upgrade in this stage.

New second-copy result: direct five-file run81pass/2fail/684assertions. Both failures
were browser setup ENOENT because the direct test command did not create .foundry/qa
and bridge.test.ts:103 mkdir(output) assumes it exists. Parent created ONLY that
QA-output directory, not source, then both exact browser checks passed2/18assertions.
The original failure was preserved; this explicit setup correction is not a runtime
fix. Artifacts beside adoption report: second-installed-sdk.log and
second-browser-after-output-setup.log. No claim of one clean83case rerun yet.

Parent added scratch SDK lifecycle instrumentation (connect/close timestamps and
error codes only, no arguments or secrets), then reran all five original first-copy
files:82pass/1FAIL/698assertions. This time refusal passes, but "mcp actual viewer:
old and fresh inspector reconcile tool evidence without another native write"
times out at30000ms. SDK client28 connected14232ms, no close event before timeout;
Bun reports killing one dangling CONTROLLED test process. This does not identify
the Playwright phase or prove a production bug. sdk-lifecycle-diagnostic.log beside
adoption report retains all events; scratch trace-proxy-lifecycle.ts is under
.foundry/qa/parent-installed-prewrite-20260907. Do not claim its diagnostic preload
is an uninstrumented release pass or native/model execution.

Include both instability modes in diagnosis: locate exact browser wait/cleanup
phase with per-step bounded diagnostic evidence and screenshots on failure, and
ensure teardown runs even after setup/observation rejection. Original browser
test sets8s timeout only on its old page; fresh page defaults and whole-test30s
can hide the precise failure. Do not increase timeouts to manufacture a pass.

Inspect actual fixture lifecycle: Client.connect starts asynchronously in fake
native spawn, early initialize/tools-list replies are emitted immediately, and
close() awaits proxyReady before closing clients/runtime/journal. A proxy-ready
rejection could therefore escape teardown or skip later cleanup, but that is a
source hypothesis, NOT established cause. Establish the failure phase and whether
registration-specific refusal assertions pass before teardown. Preserve any
genuine SDK/startup error rather than suppressing it as expected without proof.

Reproduce with controlled SDK/transport ordering and full-suite interactions,
record exact installed source/test hashes and all failures. Check ownership,
zero native write, zero tool operation, the registration-specific error and full
cleanup of every owned fixture handle. If purely a fixture handshake/cleanup
defect, make a narrowly justified ROOT test correction plus deterministic regression;
do not weaken runtime assertions or modify either frozen candidate. Any production
change needs explicit scope coordination (Fable owns root UI/journal only).

Report docs/G5-installed-prewrite-failure-handoff.md with actual cause, source versus
fixture distinction, exact corrected hashes and commands, cleanup observations
and implications for the immutable candidate. Parent must decide/re-authorize any
new candidate after correction/review; no silent repeat of prepareNative. Never
modify existing reports, run model probes, restart live services, change account/
credentials/bindings or retry unknown native work. Native recovery capture remains
unapproved until installed checks are explained and satisfied. Full goal retained.
