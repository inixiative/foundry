Continue as the provisional Astra lead in this same native Foundry session.
Your memory review must be actually complete before this request is dispatched.
Fable's read-only S1 review completed02:16:40.518Z and reopened the bounded source
slice. Read fixtures/harness-qa/QA-2026-09-07-fable-S1-review.md. Supervisor
independently reproduced its two Medium findings as THREE ordinary failures:
fixtures/harness-qa/acceptance/native-historical-evidence.test.ts, report
.foundry/qa/2026-09-07T02-19-18.861Z-G5/report.json (both engines relabel completed
attempts after idle transport EOF; Claude drops unowned startup init evidence).
The test's standalone strict TypeScript check uses targetES2023 for findLast.

Own only ../agent-session source/tests/recorder scripts and its S1 handoff. Do not
edit Foundry production, independent fixtures or shared graph files. Fable owns
memory corrections. Preserve dirty changes and the entire CORE-002/003/004/005
goal, including full native tools/streams/artifacts/lineage/subsessions/model effort,
three-part decoration and subscription-independent logical work continuity.

Fix historical transport semantics: a later idle/session transport failure must
not retroactively mark already settled unrelated admissions failed. A still-owned
unresolved admission must retain accurate transport failure and late evidence.
Add active second-admission and buffered-terminal-before-exit variants so the fix
cannot hide an actual transport failure on the current work.

Preserve unowned Claude startup/lifecycle/late native evidence in session history
without assigning it to the next admission. Preserve foreign/unknown identity
diagnostics without adopting foreign binding or resolving current work. Match
the contract, not simply the current MCP implementation. Keep raw event safety,
immutable retained data, duplicate rejection and correlation truth intact.

Triage the review's Low items explicitly. Fix observer-failure diagnostics and
the incorrect Claude unrecognized-terminal localFailure=rpc label if they belong
to this same evidence boundary. Do not introduce a lossy history cap as a quick
fix for blocked-send growth; retain it as a measured retention requirement with
an honest bounded/durable design proposal if outside this slice.

Run the new three cases RED before editing, then all FOUR independent native
files explicitly (14 cases total), sibling suite, types and cross-repository
fingerprints. Extend the cross-check allowlist for the new file without changing
its assertions. Existing11 cases and all13 historical recordings stay intact.
Do not call expected S4 failures capability acceptance.

No native probes, package integration/publication, commits, credential or binding
changes, server restarts or new agents. The subsequent two-admission recording
node remains next, not a reason to silently fold new native work into this fix.
Provide exact evidence and a bounded handoff for supervisor QA/Fable re-review.
Count remains3 until independent acceptance; no self-approval.
