Continue this SAME native Fable session after your actual end_turn03:28:10.296Z.
Supervisor retained the matching response in QA-2026-09-07-fable-model-profile.md
and read your dedicated QA-2026-09-07-fable-native-model-profile.md handoff.
Original four independent propagation/profile cases now pass, with types/diff:
.foundry/qa/2026-09-07T03-29-28.843Z-G5/report.json. The provider slice is NOT
accepted because new inspection metadata has independently reproduced defects.

Task A, bounded correction in your existing provider files/tests only:
native-model-provenance.test.ts reproduces arbitrary tool/unknown raw.model being
labelled native acknowledgment, a foreign binding's init model being attributed
to this session, and a failed binding lookup being swallowed and reported as a
fresh session. First report03:29:12.460Z-G5 has three failures and one positive
recognized-config control. A fifth case also covers the pending-lookup profile:
reserve concurrency ownership immediately, but do not call an unresolved binding
fresh before its lookup settles. Both independent files now total nine cases;
do not edit their assertions. The initial fixture type-import resolution issue
was corrected by deriving its type from SessionAdapter, without changing tests.

Use explicit supported protocol/configuration envelopes and same-native-binding
correlation for model acknowledgment. No arbitrary raw.model scan and no borrowing
unowned/foreign evidence. Missing identity/configuration remains unknown; do not
remove the positive capability merely to make negative tests pass. Existing own
tests that modeled an arbitrary raw.model as acknowledgment should instead use
actual supported configuration envelopes, explicitly labelled synthetic. Do not
claim this makes installed Claude emit the init event it currently drops. Preserve
lookup error or unknown provenance instead of fabricating fresh. Profile mismatch
must still refuse before send/replacement, including concurrent creation. Keep
the four original profile cases plus the five new provenance cases.
Run all nine, both adapter test sets, baseline/types/diff; report exact results.

Task B after that correction: READ-ONLY opposite-model review of Astra's OFFLINE
recorder preparation in ../agent-session/docs/S1-continuation-preparation.md,
scripts/s0/continuation{,-plan,-record}.ts, sanitize.ts and tests/s1-continuation.test.ts.
Supervisor inspected these and reran sibling163 passes (161 ordinary +2 expected
S4 failures) at .qa/s0-2026-09-07T03-21-40.126Z/report.json, plus all14 independent
native cases at .qa/s0-cross-2026-09-07T03-22-35.278Z/report.json. Both plan modes
ran successfully. Verify single owned process/two sends, exact native terminal/
tool joins, first failure/unknown blocks second, no replay, redaction, immutable
historical recordings, exclusive output paths, checkpoint failure and attached
owned cleanup. Unknown timeout is not cancellation. Source pass is not native
continuation or retention proof. Return a separate accept/reopen recommendation
for this preparation with concrete blockers before real capture is authorized.
Do not edit sibling files or launch any native probe yourself.

Astra is active on G3-current-admission-lead-request.md in core/context/dispatch/
memory and Foundry flow/runtime/inspection, NOT provider files. Do not duplicate
that work or review its unfinished implementation. Both production servers stay
on old provider/backend builds; no restart, live settings/profile/binding changes,
credentials, dependencies, agents, commits or publication. Supervisor owns graph
and independent acceptance. Concurrent source edits can make baseline evidence
stale; rerun or report the limit instead of claiming stable acceptance.

Same complete long-term goal, Astra provisional lead count4/next6, Fable reviewer.
No source/native/latency/retention parity or self-approval. Keep response bounded;
separate provider correction from recorder review so their verdicts are explicit.
