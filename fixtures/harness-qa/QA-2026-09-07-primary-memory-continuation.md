Privately: nothing further is needed. The version result, the ledger tails and the injected memory of this very turn are in hand, and the request is read-only. Report follows.

FOUNDRY_MEMORY_PRIMARY_OK 1.3.14

**Actual tool result.** `bun --version` in the repository returned 1.3.14. No files were edited, no agents, probes, restarts or binding changes.

**Evidence the correction is live in this process.** The memory layer injected into this turn carries the new selector's own accounting: a "Relevant to this message" section with windowed excerpts and character ranges, and a "Not injected" summary reporting 351 audit records withheld with the complete log of 356 records retained, budget 6000 used 5934. That is the bounded selector running in primary 4400, not a description of it.

**Ledger state versus the generated per-domain thread knowledge.** Both domain summaries still describe the 22:37 post-rollout smoke and the G5 audit recommendation about the Codex app-server engine. That is history, not today's decision. Stale or missing current-work facts:

- Accepted Astra lead count is 4, with the next routine checkpoint at 6. The summaries carry no count.
- Memory selection went through four corrections today. The independent gate is now seven acceptance files, 24 cases, last passed at `.foundry/qa/2026-09-07T02-57-41.563Z-G3`. Not self-accepted; supervisor decides.
- S1 in the sibling passed 14 independent cases with both Medium findings closed. The sibling package is still not integrated into Foundry; the registry 0.1.0 remains in use.
- Astra's final memory review and offline two-admission recorder preparation are active as of the latest ledger bullets. Not complete, not to be duplicated.
- CORE-004 records the central native turn-budget gap: the installed engine defaults to 25 turns and startup does not override it. It also notes the journal still drops structured native terminal fields until S1 is integrated.
- Supervisor's browser QA covers the mandatory-context refusal path at three widths, and the pre-rollout measurement showed raw memory at 96.1 percent of system input. No post-rollout measurement is recorded yet.
- The earlier idle checkpoint is explicitly no longer a restart clearance; fresh native terminal and history evidence is required after the latest work.

**Next two dependency-ready work items, Foundry-owned, not executed here.**

1. **Central execution-budget visibility.** Surface the native turn budget and its source in configuration and the right panel, per the CORE-004 gap, without mutating the running session profile. Depends on nothing in the sibling.
2. **Selection self-reference and duplicate audit tightening.** This turn's relevant section selected the current request's own dispatch record twice, once each for the classifier and router payloads. Exclude the in-flight turn's own audit records from relevance retrieval and collapse duplicate payload copies. Small, core-owned, testable through the existing seven files.

Gated, not ready: sibling integration (I) and a real scoped native retrieval bridge (T) wait on S1 acceptance and the recorder node. This report claims no native retention, retrieval, speed or capacity parity.
