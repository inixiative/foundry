# Correct the brittle sample command policy without weakening ownership

Continue Fable in the same Foundry/native session after your verified path-review
end_turn08:18:20.293Z. Supervisor fully read the review and accepted its bounded
source verdict. A NEW authorized capture has now completed and exposed the
anticipated liveness gap. Same full goal/count six-next8; Astra continues T3
implementation and must not be interrupted or have its MCP/runtime files edited.

New evidence, immutable candidate C:
.foundry/qa/adoption-6b0a0aad-0506-4745-8629-64dfa10ea947/foundry
Run C/.foundry/qa/i1-pathfix-claude-20260907t0819, actual08:18:35.689Z to08:18:57.709Z.
Parent exec38357 exited0, returned passed=false. Report reason
sample-command-shape-failed, verification rejection command-shape; all native/
HTTP/local/transport/persistence checks true. Actual one Bash command:
cd "<EXACT OWNED ABSOLUTE PROJECT>" && bun --version && cat sentinel-1.txt
with output1.3.14 and FOUNDRY_FIRST_SENTINEL. No foreign path, write or extra tool.
Native result/completed/local resolved; two committed messages and one trace;
one spawn/write, second admission withheld; owned exit143 after known terminal,
no cleanup failures and unchanged project. Independent readonly replay of the
existing ownership guard passes. This NEW run stays FAILED, as does the older
absolute-path run. Read only its selected public SQLite evidence/report.

Implement a bounded operation policy rather than another isolated observed-string
patch. The authorized work is version inspection and reading the exact sentinel,
with harmless directory setup to the SAME owned project. Cover normal equivalent
literal spellings coherently: optional exact-owned cd (including --), version/read
in either order, separate calls or safe conjunction, relative/./ and exact absolute
owned sentinel, optional cat --, and safe quote/escape forms. Preserve original
MCP argv, actual process/admission/owner/terminal checks and explicit failures.
Do not change the task to an easier fixture, force a different model, weaken
output/evidence checks, auto-retry, or retrospectively turn a failed run green.

Prefer a proven parser if available and appropriate, with explicit rejection of
expansion/substitution rather than assuming undefined variables are harmless.
Supervisor checked root require.resolve for shell-quote/bash-parser/tree-sitter-bash:
none is installed. No root install or dependency/lock changes during Astra T3.
A small closed operation grammar built from safe literal owned operands is an
acceptable alternative; it must not become a general hand-rolled shell parser,
strip shell syntax, split inside quotes, or accept arbitrary absolute paths.
Explain the conservative supported language and refusal limits in the handoff.
Do not chase a single new spelling per native attempt.

If considering shell-quote, inspect its actual source and pinned-version safety
before adopting it later. Its official docs describe variable interpolation, not
a shell authorization boundary, and it has published parsing-complexity advisories:
https://github.com/ljharb/shell-quote
https://github.com/ljharb/shell-quote/security/advisories/GHSA-395f-4hp3-45gv
No new parser dependency is approved by this request; no eval or execution of
observed commands during validation.

Add offline positive/negative coverage spanning BOTH retained command shapes and
the normal operation variants above, plus foreign/prefix/traversal paths, dynamic
substitution/env, globs, redirects, pipelines, backgrounding, extra operations,
malformed quotes/argv, hostile filenames and control/size/operation-count bounds.
Exercise accepted literal commands in disposable non-model Bash/zsh fixtures and
prove no extra markers/files, without weakening existing security assertions.
If an existing assertion rejects newly allowed benign syntax, preserve its old
evidence and explain the deliberate policy change; independent ownership/terminal
assertions must stay unchanged. Keep report rejection diagnostics precise.

Own sample guard/runner and focused sample tests plus a dedicated
docs/I1-command-policy-handoff.md only. Root sample helper is no longer Astra-owned;
his T3 request uses separate retrieval utilities. No MCP, native provider/runtime/
sibling/startup/UI/shared-ledger edits, no model probes or new native sends.
Use apply_patch for manual edits. Preserve all frozen candidates/reports.

Run focused tests, explicit strict types and unchanged independent ownership/
projection/budget/actual-class routes. Build a NEW isolated two-install candidate
using existing scripts and exact manifest policy, full baseline and sample gates.
Record source fingerprints and concurrent T3 limitations. Supervisor reviews the
policy/source and tests before a separate new capture; do not self-accept.
No root install, new agents, account/config/binding changes, restart, publication
or commits. Keep T3/I2 and full tools/artifacts/inspection/lineage/latency goal open.
