# P02a completion: delivery proof, runner integration and honest inspection

Fable turn_0fe544e5-5b62-412d-81ed-78d5be6dc254 is terminal: native end_turn
12:42:07.187Z, matching durable1788871327286, empty buffer. Parent read FULL
docs/L-expert-delivery-revision-handoff.md and the new regression, proof and
inspector projection. Parent viewed the actual mobile race screenshot. The
positive controlled race is useful, but P02a is NOT accepted yet.

You are the sole root source/test/owned-browser owner; Fable is idle. Read that
handoff, the previous P02a request, and the full pilot02 handoff. Keep the chosen
contract: immutable assessed inputs AND an explicit owner/version-bound snapshot
of actual central delivery. No review wait or discarding newly committed knowledge.

## Independently reproduced verifier gaps

Parent added packages/foundry/tests/expert-delivery-proof-contract.test.ts.
SHA2560b2b52dc725ad99ea961fed9e47e8b80ebc571aa8298183aa855be58a8d09893.
It constructs a REAL runtime/factory/executor turn with controlled providers;
the unchanged positive control passes. All six corruptions are wrongly accepted:
foreign assembled-layer thread owner; included:false; assessed revision999 unrelated
to the participant; forged assessed hash; advanced relation with no revision advance;
and replacement user input that merely quotes the knowledge strings.

Stable RED1pass/6fail/13assertions:
.foundry/qa/2026-09-08T12-48-09.230Z-71f2a11b-1f6f-49f2-9829-3f507fa38f73-G3/report.json.
Strict tsc for this fixture passes. The earlier12:46:44-33dd112b RED is retained;
parent removed an unsupported Message.timestamp property to fix its test typing,
without changing any assertion. Keep the final parent fixture bytes/assertions
unchanged. Add coverage rather than weakening the positive or six negatives.

deliveredParticipants currently checks injection.threadId only, ignores the
assembled layer's threadId/included flag, does not join assessed hash/revision to
the frozen participant or validate relation arithmetic, and proves delivery only
with text.includes(content). Fix actual provenance validation, not the test. Bind
delivered layer/owner/domain/version and intended assembled block to the initial
provider-boundary evidence; quoted text in the user's message is not an injected
expert block. Handle duplicate/missing records, foreign project/thread/domain,
invalid/negative revisions, unknown relation and prepared-only/legacy evidence
honestly. The inspector's deliveredUnderstanding currently trusts a single ledger
row without these joins; it must not confidently label invalid evidence delivered.

## Incomplete end-to-end integration

The native runner is unchanged: scripts/native-domain-loop.ts imports and uses
recordedParticipants(delivered) at its desired/content-distinctness check, report,
privacy check and historical browser proof. Its report still says
source:immutable-message-participants. For a real assessed0/delivered1 A2,
hasCommittedDelivery can be true while desired contains two empty assessed strings,
so the next equality check still fails. With unequal stale strings, later checks
would instead inspect the wrong versions. scripts/check-native-loop-artifacts.ts
also equates delivered report revisions/hashes with assessed participants.

Finish the runner, report, artifact checker and actual browser contract together.
Keep assessed and delivered proofs distinct, use immutable actual delivery for
delivery/privacy checks, and inspect BOTH at the appropriate historical controls.
Never substitute currentAtObservation for recorded delivery or alter old pilot
reports. Legacy failed captures stay legacy and failed. Add an actual controlled
full-runner held-review race that reaches delivered verification, unrelated B,
artifact audit and reconstruction, with later current state changed, not just a
helper test. Preserve the native CLI's installed-package verification/no-test-adapter
boundary and original model/tool/process/call/deadline ceilings.

The first Fable test uses identical bytes across two experts, but does not exercise
same-content revision increments within one expert. Add that case so revision
identity cannot silently collapse to content hash. Audit ContextLayer version marks
on every content mutation (set/warm/compress/restore/clear/clone), invalid marks and
owner boundaries. Do not leave a stale version mark after unversioned source work,
including same-content rewrites. Preserve generic focusable-source ownership and
initial input capture on failed sends/tool followups.

## Verification and scope

Retain new failing controls before corrections. Run parent protected fixture plus
focused existing core injection/layer, expert phase/ownership/recovery, native
loop/proof/artifact and connected inspector scopes at unchanged budgets, strict
touched types and diff checks. Extend the real connected browser fixture and verify
positive advanced/same-content revisions and honest invalid/legacy states through
visible desktop/mobile controls. Each attempt gets an exclusive retained directory;
no hidden DOM clicks, forced controls, suppressed request/console errors or reload
in place of live updates. Use the assigned owned headless browser, never bypass a
macOS lock or touch the user's other tabs. All acquired handles must settle/close
with original receipts; no inferred exit or native cancellation.

Own docs/L-delivery-contract-completion-handoff.md. No native pilot03, source capture,
root install, live server restart, candidate edit, model/account swap, pooling,
commit or count advance. Parent remains read-only during your source/QA slot.

P02b remains next, with a concrete parent investigation now available at
docs/L-pool-occupancy-parent-investigation.md. It identifies logical-key activity
being attributed to an already released original session, but no reproduction or
fix has run. P02c startup400 diagnosis also remains open. Do not absorb those into
this task or forget them. Full audit pagination, LS, native B/privacy/recovery,
E/Herald, both-engine tools/subsessions/forks/lineage/artifacts/tags/latest models
and matched fidelity/capacity/latency remain required. Finish P02a end-to-end;
parent independently reviews before the remaining nodes and a new native pilot.
