# Bind an actual owned browser child without bare-PID fallback

Your03335935 completed native end_turn18:49:14.686Z, matching durable final
1788806954711, empty live buffers. Parent read FULL corrected handoff and verified
helper2428c8b0... plus your tests and its independent identity fixture:17pass/
135assertions,1explicit browser skip, strict types and stable source, report
.foundry/qa/2026-09-07T21-01-58.035Z-2126d3af-c1ea-4479-ad51-1fe7df407f75-G3/report.json.
The parent fixture only widened its async occupant annotation to string; every
safety assertion remains. The bare-PID and unknown-liveness defects are corrected.

The actual Playwright driver still supplies no acquired child, so forced cleanup
is unavailable. Safe refusal is necessary but is not the full desired reliability.
Find and implement a small real binding for the existing optional launchWithProcess
capability without changing that verified helper or creating another model engine.

You own ONLY new scripts/owned-playwright-driver.ts, a minimal companion host
script if genuinely required, packages/foundry/tests/owned-playwright-driver.test.ts,
and docs/M4-real-browser-driver-handoff.md. Do not edit owned-browser.ts or its
tests/handoff, the parent fixture, runner, diagnostics, composition, checker,
vendor/package/lock files, live state or any existing artifact. This is a distinct
driver task, not continuation of the already-final helper03335935. That helper
must stay stable so Astra can finish its current integration independently.

Inspect supported bundled Playwright and installed Node APIs first. Prefer existing
launchServer/process/remote-connection facilities. If a separate Node host is
necessary because Bun's same-process connect stalls, keep the host limited to
one browser lifetime and the existing Playwright protocol, not a new general
browser broker or arbitrary command runner. Record the architecture and evidence
before implementation. Use installed runtimes; no dependency install or network
research/download is necessary for the initial local API/protocol investigation.

The acquired resource must retain the actual child's authoritative exit event
and operate on that child handle (directly or through its exact owning host),
not a PID/start-time/name check followed by kill. A host exit or lost connection
is not evidence that Chrome exited. No global spawn monkeypatch, private model
handle, shared browser profile, process scan or arbitrary PID signal. Preserve
10s launch/8s readiness and all browser/context capabilities used by the runner.
No replacement launch on timeout. Late construction, RPC, host and Chrome outcomes
must keep their distinct ownership and cannot claim false exit confirmation.

IMPORTANT scheduling: Astra9adab6e2-70a3-491e-8d75-227fe99b0c2e is ACTIVE in
4407/foundry-astra-lead on current runner integration/real Chrome checks. Begin
local source/API review and controlled driver tests now. Do not launch Chrome
or a browser host until its exact original turn is terminal, its durable matching
agent final exists, live buffers are empty, and its final handoff is read. You may
observe that dependency through the existing Foundry APIs and only public terminal
events in its owned tape:
/Users/agreenspan/.codex/sessions/2026/09/06/rollout-2026-09-06T20-15-59-01a07901-d93d-7903-9c9d-09e5e7136573.jsonl
nativePID43025; do not print reasoning/unrelated content, interrupt, replay, or
send a task to Astra. This new driver task does NOT block its current integration;
the unchanged helper is already terminal. Keep the dependency acyclic.

After that slot is free, run bounded real tests against fresh isolated local
resources: normal launch/page/close, a controlled graceful-close stall with actual
owned fallback and observed exact child exit, and lost-host/late-acquisition
handling. Test protocol/ownership faults with controlled resources, never a real
PID reuse experiment. Retain failures and original handles, no retry-count passes.
Helper/driver proof is not runner integration or installed/native acceptance.

If a supported binding requires a materially broader component or cannot preserve
ownership, record the concrete evidence and smallest viable design instead of
inventing a fake handle or claiming the safe-unavailable path is complete. This
is a proportionate reliability correction, not permission to redesign the app.

Return exact hashes, source/strict tests, real capability evidence, cleanup of
each acquired resource, remaining limits and minimal runner integration contract.
No native model/sample, package install, candidate, saved-server restart, account/
binding/credential/shared-browser changes or commit. Native local expert proof,
configured expert lifecycle, then scoped Herald remain the graph's sequence.
