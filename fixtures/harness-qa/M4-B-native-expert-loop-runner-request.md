# M4-B: implement the disposable native expert-loop runner

Continue after M4-A e62a5922 completed native01a07c87-598a-7111-a41d-9c578f84ec9b
at15:54:55.296Z, matching durable final, empty live buffers and complete handoff
read by parent. Parent read all startup/helper/tests and is independently rerunning
the72-case scope. This does not yet accept installed or live startup. Fable owns
the reproduced M1 evidence navigation race and read-only opposite M0 review.

The next dependency is executable native local-expert learning, not more unrelated
guard suites or Herald. Implement the missing opt-in runner and its controlled
tests now, WITHOUT executing native samples or creating installed candidates.

Use the existing production ThreadFactory/ThreadRuntimeManager/viewer/SQLite and
SessionBackedProvider/Claude/Codex adapters and current native admission ownership.
Reuse existing native-sample/adoption/QA-directory machinery wherever its actual
contract fits. Do not add another engine, journal, knowledge store or mock a real
provider on the native path. Keep M0 helper untouched while Fable relies on it.
Separate import-safe plan/composition from explicit native execution; existing
frozen candidates and the two running work servers stay unchanged.

Parent has added the real sample at
fixtures/harness-qa/sample-projects/contact-migration. Existing
scripts/prepare-harness-project.ts contact-migration creates a fresh uncommitted
local repository. Three baseline tests pass; explicit bun check-migration.ts
intentionally fails on its starter. Native agent must implement the feature,
preserve baseline/checker, add regression coverage and emit report.md. Every local
checker attempt retains a new closed SQLite database and source-hashed report.
Parent validated a separate reference-only copy, NOT native proof. Never run the
sample on either parent's QA control copy or seed knowledge from that solution.

## Composition and scenario

Support explicit central engine/model/effort and explicit Claude domain reviewer
profile, with no silent provider/model substitution. First proposed pilot may use
Claude/Fable central and Claude/Fable experts; Astra central follows with the same
architecture/testing roles. Native tools, requested central context/effort and
existing configured decision/guard policies remain present. Do not claim Codex
text-only reviewing is supported. Enumerate actual central, pre-hook, post-hook,
decision and guard roles/admission ceilings in the plan before executing anything.

Create explicit fresh project/thread identities A and unrelated B in isolated
state. Architecture/testing each own their configured instructions, domain
knowledge and empty initial thread understanding. The native central actually
edits/tests the prepared contact project through its native tools; no finite
controlled migration tool is substituted on the native path. Post-hooks consume
actual completion/tool evidence, not a planted successful summary.

Use M4-live-domain-loop-adoption-handoff.md's bounded A1/A2/(if needed A3)/B1
protocol: at most4 central admissions, at most one original post review per domain
per eligible completed observation, and no competing same-domain review. A1 is
the task; A2 is only Continue after actual central terminal/capacity allows it;
A3 only if A2 preceded eligible commits; B1 unrelated. Enumerate extra pre/decision/
guard requests, not just four central calls. No new startup self-test by default.
Bound stopping of NEW admissions separately from observing occupied originals.

Retain central/pre/post prepared inputs, task outputs, native public tool/call
records, original review ownership/outcomes, exact durable revision/CAS evidence,
historical input, next-turn delivery and project artifacts. Private structured
review reason remains owned audit only; never capture hidden native reasoning,
credentials or unrelated tapes. Preserve requested versus observed model/effort.
Display this isolated session through the real viewer so the user can watch the
later authorized run; provide its URL and inspection/report locations.

Pending review must not add a serial wait; a next turn can use last committed
knowledge with pending status. Do not manufacture native timing to force this
case. After actual commits, require next prepared input to contain each own expert
interpretation without repeated user instructions; unrelated B receives neither.
At actual all-terminal/settled cleanup, reconstruct from the same journal without
a new central admission and compare snapshots, evidence and immutable history.

## Gates and tests

An executable plan must record immutable source/native manifest and installed
package identity, new run/state/project identities and complete call budget.
Execution must refuse wrong manifests, reused owner/run directories and unknown
original occupancy rather than bypass admission or replay work. Use existing
manifest/guard contracts; no second validation framework. Required installed
central bridge paths must pass before a later native dispatch; unrelated G5 stress
does not justify substituting a tool-less or controlled central path.

Provide controlled end-to-end runner tests including production lifecycle,
explicit real-path provider injection boundary, uncommitted/pending versus learned
next input, failure/unknown ownership retention and cleanup. Use real sample
artifacts where useful, but label controlled providers as controlled. Keep checks
focused on this scenario rather than broadening into every outstanding graph node.
Plan/inspection tests must launch no native work; execution tests use controlled
adapters only. Source/strict types, stable scoped hashes and exact failure reports
required. No models, package installation or live account setup for testing here.

First checkpoint docs/M4-B-native-expert-loop-runner-handoff.md, implement, run
focused tests and finish the handoff with exact commands and the smallest next
candidate/installed check/native authorization boundary. File ownership is new
runner/helper/tests plus narrowly necessary existing sample guard integration;
no UI/cache/history, runtime/M0 semantics or startup rewrites unless an actual
blocking contract is first explained. No commit or self-acceptance/count advance.

This authorizes source/controlled runner work only. No candidate creation, install,
native sample, preserved server restart, credential/binding/account/config changes,
shared browser storage, Herald or subscription pooling. Parent independently
reviews before freezing and native execution. Full original goal stays active.
