# Independently review R1/R2 and the parent's recording replay

Continue the same Fable session after actual end_turn12:27:19.299Z, matching
durable final and complete guard handoff section7 read. Full goal/count7-next8
unchanged. Parent independently confirms corrected guards696b6945/2f12e3ed:
106 cases/427 assertions, types/diff pass, stable root88c01f85 in
.foundry/qa/2026-09-07T12-24-54.981Z-d2cacc85-98cb-4013-89a7-e628b3605731-G5.
Both actual saved Claude admissions pass current guards, original report/DB/WAL
unchanged. No new native call. Astra will opposite-review your final guard.

Your next assignment is READ-ONLY independent review, not implementation:

1. Read docs/G4-native-adapter-bridge-handoff.md completely and inspect its final
R1/R2 sibling adapter and Foundry provider/config/projection changes. Check real
schema/config acknowledgment, prewrite persistence, cold resume same binding,
fresh owned bridge inventory including pagination, truthful item-only IDs,
public tools/output, terminal eligibility versus occupied capacity, exact-owner
release/physical cleanup, and failure/no-retry behavior. Verify tests exercise
actual production classes and real SDK boundaries rather than bypassing config.
Existing native metadata/history summaries are not full history/fork proof.

2. Review parent's new scripts/check-native-retrieval-replay.ts and acceptance
native-retrieval-replay.test.ts. Is replay genuinely read-only, correctly joined
to actual admitted/public evidence, no new calls, refusal on missing/contradictory
evidence and preserved original capture? Parent three failure tests7assertions
and explicit strict script types pass. Reproduce both saved actual admissions
using that entrypoint; independently challenge assumptions, not just green output.
Recording is the immutable adoption694bcdf8 capture already reviewed. Never edit
it, its guards, package, DB/WAL/SHM or report. Use read-only SQL or disposable copy.

Astra currently owns scripts/native-foundry-sample.ts, source checker and recovery
sample tests under turn_859bf9d5-c643-430d-99c8-33dbfd5f3e01. These are MOVING:
exclude them from a final stable-source verdict, do not edit or run its changing
capture/checker. Direct offline production test with explicit source preload is
allowed; no native CLI invocation. Report the optional completionLifecycle type
errors previously seen as pending moving-runner evidence, not stable core failure.
If adapter/provider files move during your review, report before/after and narrow
the verdict; don't claim a moving tree is accepted. Add scratch QA findings/tests
only under a fresh .foundry/qa review directory, no owned source/fixture changes.

Output docs/G4-adapter-and-replay-opposite-review.md with severity-ranked findings,
exact file/line references, scoped hashes, commands/results and explicit native
and installed-candidate limits. Do not self-accept, advance count, stage/install,
restart live services, touch accounts/credentials/bindings, commit or run models.
Retain all full goal gates: native continuity/learning, historical inspection and
artifacts, tags, lineage/forks/subsessions, models/effort/pooling and matched latency.
