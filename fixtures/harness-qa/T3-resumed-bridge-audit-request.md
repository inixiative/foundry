# Audit native bridge configuration across process reconstruction

Continue SAME Fable/Foundry session after verified end_turn10:26:17.814Z and durable
final. Parent read full I2 opposite review; source now passes pending-RPC case,
remaining temporary inspection failure is queued for Astra after its active final
handoff. You are READ-ONLY and disjoint from Astra. Same full goal/count6-next8.

Before implementing the prepared fresh-call MCP configuration correction, resolve
the corresponding required RESUME contract. Current Codex adapter loads a stored
externalSessionId; a newly constructed CodexMcpSession with that binding sends
codex-reply on its first send. The reply schema carries prompt/threadId, not a
configuration map. Meanwhile nativeBridgeSource allocates a fresh process-owned
server name and launch file; old process exit closes the previous bridge. Fresh
codex-call config propagation alone may not restore tools on reconstructed
conversations. This is a hypothesis requiring code/protocol evidence, not a claim
that all existing resume paths fail. No native probe is authorized.

Inspect current adapter/native bridge lifecycle and sibling codex MCP session;
ground installed-version codex-reply processing/config loading in primary source
and official documentation. Determine where the original MCP config lives, how
an existing thread is resumed by a new MCP server process, whether server CLI or
per-call settings can override it, and whether original bridge command/args remain
usable after cleanup. Do not read user config/credentials/hidden reasoning or
unrelated session tapes. Existing known capability launch files are not credentials
to forward into a report; use structural descriptions/counts/hashes only.

Also check Claude resume semantics for its newly attached owned MCP bridge and
separate any verified fact from untested connection assumptions. Preserve existing
native identity, history and all native tools; no silent fresh session/fallback,
unsupported flags, wrong-account binding, credential copying, replays of unknown
work or invented runtime capability. If a protocol limitation exists, identify it
explicitly and give the narrowest viable architecture that still meets the full
create/resume/restart and scope-isolation goal, not permanent refusal as success.

Output docs/T3-resumed-bridge-contract.md with evidence/source links, exact
source boundaries, fresh/same-process/new-process/server-restart matrix, and
specific controlled plus eventual real-native regression cases. No production or
independent-fixture edits, staging, installs, commits, models or live changes.
This is a bounded audit to unblock the actual native integration, not another
general harness redesign. Use apply_patch for the report. Current prepared
T3-native-config-and-references-request.md remains undispatched until ownership
handoff; do not implement it in this task.
