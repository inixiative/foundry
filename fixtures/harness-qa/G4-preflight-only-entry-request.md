# Expose the real recovery preflight without admitting native work

Continue SAME Fable session after actual review end_turn13:13:33.757Z, durable
final and complete docs/G4-recovery-runner-opposite-review.md read. Parent accepts
the reviewed recovery source boundary as eighth bounded Astra slice and retains
Astra provisional lead. This does not accept an installed candidate or native
retention. The full goal and queued G6 UI work are unchanged.

Your review correctly requires a positive real preflight before capture, but
--recovery-plan only prints a plan and recoveryPreflight is private; the only
production entry calls it then continues toward a native turn. Implement the
small missing verification entry now, reusing exactly that existing preflight:
an explicit --recovery-preflight command (or equally clear separate read-only
entrypoint) that validates the same engine/plan, installed manifest, complete
source/proposal and protocol constructors, returns the actual preflight evidence,
and NEVER creates a browser, dispatch, native process, tool call or binding.
Do not duplicate the validation logic or introduce a bypass into capture mode.
Exporting the existing function plus a thin command branch is sufficient if it
meets those requirements. Error/missing arguments fail closed with nonzero exit.
Label modelCalls0 and distinguish --plan from verified preflight in help/output.

You own only this narrow runner preflight/CLI path and focused preflight tests.
Astra is currently READ-ONLY G6 metadata/candidate reviewer, turnf5068657; it is
not editing the runner. Parent will coordinate the explicit delta review and
candidate freeze after your final handoff. Do not touch core/G6 metadata, native
adapters/providers/guards, stage/install scripts, sibling, live services or any
existing candidate. Existing capture behavior and all original tests retained.

Offline tests must prove command/refusal cannot start a browser/process or write
model work, preserves the original manifest/candidate/capture, and exercises the
REAL preflight rather than stubbing it in the production branch. It is fine that
actual positive installed proof awaits the newly built candidate; report it as
missing, not as simulated success. Do not run model CLIs or build a substitute
staging mechanism merely to fabricate a positive. Include exact command for that
first real positive check and retain root/missing-manifest negative evidence.
Run focused original recovery tests plus new tests and explicit script types/diff.

One evidence erratum in your review: the root does NOT resolve the live sibling.
Parent independently used createRequire(root/packages/foundry/package.json),
realpath and file inspection: entry is root/node_modules/.bun/@inixiative+agent-session@0.1.0/node_modules/@inixiative/agent-session/src/index.ts,
package0.1.0, Codex hashfeb1e41d8a25baace4bfcd559de2cd77bd6517ed6ec60398b424363e0e7be7f6.
Installed registry tree has6files, not a live sibling tree exceeding7. Your
fail-closed preflight result remains valid, but that explanation in section2 is
wrong. Append correction with the verified resolution; never modify installed
package or existing reports to fit the description.

Output docs/G4-preflight-only-handoff.md with exact delta/hash/commands, tests and
limitations; append the resolution erratum to your existing review. No staging,
install, model capture, live restart, account/credential/binding change or commit.
Parent will run the new immutable candidate's real preflight, installed checks
and separately authorized native experiment once reviews are complete. Keep the
full native/learning/inspection/history/artifact/pooling/model/latency goal intact.
