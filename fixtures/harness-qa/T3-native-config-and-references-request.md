# Integrate native conversation config and public discovery references

Ownership update10:53Z: Fable now implements ONLY sibling Claude normalizer plus
additive SessionEvent fields and sibling tests as turn_920cda01-965f-4513-af2d-ead2d9ebd5c4.
Do NOT edit those concurrently; consume docs/T3-claude-reference-handoff.md after
its actual terminal. Astra can own Codex source and Foundry integration after I2.
Parent native-app-server-recovery.test.ts adds controlled transport/input/cold-
resume/refusal regressions against the existing class; all four initially RED.
Installed schema verifies mcpServerStatus/list, ThreadResumeParams threadId
without client rollout lookup, TurnStartParams input array. Existing class
selects WS but reads stdio, sends string input and skips thread/resume for stored
IDs. Treat these as explicit G4/G5 prerequisites, not proof that changing only
one call will produce recovery. Plan native outcome/capacity/turn identity and
configured bridge verification too; keep existing engine identities explicit.

Preparation update10:48Z, still NOT dispatched. Before implementation, review
Fable's completed docs/T3-resumed-bridge-contract.md against pinned primary
sources and the existing sibling app-server class. Fresh per-call MCP config is
only a bounded fresh/warm fix, not cross-process recovery. Verify exact protocol
names and config semantics; propose the smallest executable existing-engine
resume/restart slice in the same full graph, without silently creating another
conversation or calling permanent refusal success. Stable tool naming is only
a proposal and needs ownership/collision/guard review before any change. Do not
expand this task's edit ownership into bridge/transport without explicit handoff.

Prepared next assignment, NOT dispatched while Astra owns active I2. Read actual
I2 terminal/full handoff and reconcile its independent eligibility findings first.
Keep SAME preserved Astra/Foundry/native binding. Same full goal/count6-next8.

Read docs/T3-actual-retrieval-diagnosis.md in full and the completed Fable
docs/T3-owned-discovery-handoff.md when available. Both actual C-final captures
remain FAILED/immutable. Fable guard implementation uses an explicit owned
toolReferences contract; it does not make sibling/provider projection exist.

Parent independent fixtures, unchanged during your implementation:
- acceptance/native-mcp-conversation-config.test.ts: actual production adapter
  with its real lease and controlled native process. Unlike the previous bridge
  fixture, it does not connect a proxy by reading spawn argv. First tools/call
  lacks owned mcp_servers configuration although spawn argv contains it. Three
  controls pass: no-bridge effort/reply binding, foreign and tools:false refusal.
- acceptance/native-claude-tool-references.test.ts: actual sibling Claude class
  drops typed tool references and fails to label unknown non-text output;
  provider projection also drops the proposed references. Text control passes.
Combined4pass/4fail28assertions, strict types pass, report
.foundry/qa/2026-09-07T10-13-29.078Z-fb6a6ef2-c997-48d2-8f54-956b5859c73d-G5/report.json.
Parent three relevant sibling hashes stable before/after; canonical QA records
full values. Root MCP case uses installed registry class; rerun inside NEW exact
candidate after sibling/adapter changes. Do not root-install to make it green.

Own bounded sibling Claude/Codex config/event types and Foundry core public
evidence/provider projection/adapter/native launch plumbing, after I2 ownership
handoff. Do not edit Fable guard concurrently. Preserve other dirty source.

1. Pass the exact owned bridge's structured command/args into the first Codex
   tools/call config, not only server spawn argv. Retain effort/model/native
   defaults and existing native engine. No arbitrary config scraping, env or
   credential forwarding, config-file edits, fallback or new session on reply.
   Verify dotted-key merge semantics in the installed-version primary source;
   first call/reply/no-bridge/foreign/auxiliary/restricted behavior is explicit.
   Launch evidence must distinguish spawn versus per-call configuration without
   publishing sensitive launch contents. Preserve original session binding.
2. Normalize Claude tool_reference blocks into bounded public toolReferences
   names, detached/frozen at existing ownership boundaries. Unknown/non-text
   payloads must be explicitly omitted, not indistinguishable from empty output.
   Retain text and tool failure/call identity. No raw/private envelope passthrough.
   Guard needs to distinguish fully represented references from unknown omitted
   content. Coordinate that exact metadata contract with Fable's final handoff.
   Add malformed, mixed, duplicated, foreign/late and oversized cases; do not
   silently drop unknown contents and then label discovery fully represented.

Require controlled production adapter/class first-call assertions and actual
SDK/HTTP/browser linkage in a NEW reviewed candidate. Existing eight independent
cases,15provenance cases and prior MCP ownership/normalization/cleanup regressions
remain unchanged. Record root versus installed candidate/sibling hash boundaries.
Do not claim actual native wire or tool availability from controlled streams.

Output docs/T3-native-config-and-references-handoff.md. After tests and opposite
review, separately authorize fresh actual retrieval on both engines with new run
IDs, native terminals, original-owner tool/receipt/journal/inspector evidence and
owned cleanup. This assignment alone authorizes NO actual model captures, commits,
root installs, live restarts, account/config/binding changes or agents. Keep full
learning, artifacts, tags, lineage/subsessions, inspection, pooling and latency
scope; these corrections are existing G3/G5 requirements, not new architecture.
