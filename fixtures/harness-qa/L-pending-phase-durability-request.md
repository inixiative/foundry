# Finish pending and late phase durability, then verify the historical UI

Your phase-history task69cd84e5 is terminal: native end_turn09:15:59.062Z,
msg_011CeqieuZzFMLmCEyNLgGRj, durable1788858959233/empty buffer. Parent read the
FULL docs/L-complete-phase-history-handoff.md and the implementation/tests. Continue
the full CORE-006 goal in this same Foundry session. Do not call LI complete yet.

Parent independently passed16tests/135assertions/6files, types/diff, stable source:
.foundry/qa/2026-09-08T09-23-50.373Z-75dd7c32-c43d-4aa3-b681-dd7db8b0dd0d-G3/report.json.
This covers your completed-call SQLite integration/projections, parent review
protocol, guard evidence, advice provenance and shared-state guards. It does NOT
prove pending call durability, late guard persistence, failed main turns, rendered
UI or a complete native loop. Your broad suites both failed and the top-level
command used a20s default timeout; those results are not a passing unchanged-default
regression gate. Preserve them, don't combine totals with later narrow passes.

Parent new protected packages/foundry/tests/phase-history-pending-review.test.ts
drives the real M0 factory/Harness/HTTP/runtime/SQLite path and holds both original
reviewers open before their soft deadline. Both providers have received exact
inputs, yet the owning journal contains no matching request records. Actual RED:
.foundry/qa/2026-09-08T09-24-36.703Z-eaf5ae63-905f-4e8f-bc29-a16c71631130-G3/report.json,
0pass/1fail/3assertions,27ms test/142ms command, stable source. Strict types pass.
Keep this fixture unchanged. Earlier G3 09:23:34.692Z-b23445ee failed because the
parent mistakenly opened a second exclusive writer; that was a fixture error,
corrected to read through the existing owner. Preserve both attempts distinctly.

Your completed-call fixture explicitly awaits learningSettled before completeTurn,
so it cannot certify the normal non-blocking path or survival of a process loss
before an answer/soft deadline. Its moved-source claims are also too broad: it
changes the template layer and own understanding, not configured instructions or
the phase protocol. The guard provider returns deterministic routing-shaped JSON;
accepting any of three statuses is not a positive completed-guard proof. Keep that
failure case, add actual successful and held guard controls. Do not weaken the
existing fixture or pretend its conditional assertion covers every outcome.

Finish the original phase-history contract:

1. Persist original owned invocation identity and exact phase input at the real
   call boundary, before waiting for its result, so an admitted/pending call remains
   inspectable after process loss. Captured/prepared/supplied, native dispatch,
   native terminal, local result and capacity remain distinct. Recording an input
   does not prove model receipt. A persistence failure must be explicit and cannot
   become a successful unrecorded call. Preserve exact prewrite/native ownership,
   CAS/eligibility, no replay, original clocks and model/tool/effort budgets.
2. Guards cannot depend only on the mutable _liveDispatches result-meta bucket.
   Persist their pending request and exact original observation owner; later result
   or failure must reconcile the original durable record even after the central
   turn is journalled or the live dispatch is gone. Never guess a join from tool
   name or the latest turn. Uncorrelated evidence stays explicitly uncorrelated,
   not silently discarded or assigned to another thread. Do not delay central work
   until guards/reviews finish just to make the history test pass.
3. Failed central turns and partial phase failures must retain inputs already
   supplied. Historical input stays immutable; outcome progress is separate, owned
   and append-only/reconciled through the existing journal. A thrown call with no
   observation cannot honestly be labeled not-sent solely because its request was
   not captured. Audit the Flow guard catch and pending request states accordingly.
4. Keep private phase inputs out of shared thread-state and central prompt. Shared
   state receives bounded attributed outcome metadata only. Do not duplicate full
   histories on each native update; reuse owned records/references in the existing
   journal where suitable, not a second middleware framework or durable store.
5. Complete actual historical right-panel verification after source checks. Use
   an owned isolated viewer/headless browser, desktop and mobile, with completed,
   pending, failed, legacy, long-input and late-settled history. Inspect what the
   UI renders, not only projection helpers. Preserve exact requests after cache,
   instructions and protocol versions really change. Retain screenshots, browser
   errors and cleanup for parent inspection. A green reported badge must not imply
   an all-clear when its domain checks failed. The Mac is locked: no CUA retry or
   lock bypass, and do not claim manual viewing.

Own the existing domain/flow/cartographer/runtime phase mechanisms and narrowly
necessary existing LocalSessionStore/KnowledgePersistence/types/viewer/UI files,
new author tests and docs/L-pending-phase-durability-handoff.md. Schema evolution
inside the existing journal is allowed only if genuinely needed and covered by
backward-compatible recovery tests; a second journal is not. Preserve prior capture,
protocol and lifecycle changes and all protected assertions/fixtures. No composition,
native transport/provider/driver, candidate/root install/live server/account/binding
or central tool/model/effort changes. Required cross-boundary changes must be
reported precisely before making them. No native model pilot or release/count advance.

Parent separately fixed generic agents/librarian.ts while your previous task ran:
hash44ea6408a59972f71bebc2f58be8f28725629555cb37cf9a2763d3fa49fe1e0a.
Read FULL docs/L-shared-state-guard-parent-handoff.md. It prevents derived guards
from appearing as duplicate actual tools and retains truthful shared status without
private inputs. Its protected thread-state-guard-provenance.test.ts passes in the
independent16test gate. Preserve/review it; do not attribute this drift to yourself.
Your handoff listed composition79f6bd2f before and after; don't claim movement
without an actual intervening fingerprint. Label exact observed source changes.

Astra dbb45337 is ACTIVE preparing NEW test-only copy-boundary instrumentation;
it may run bounded diagnostic self-tests, not integrated/timed/browser workloads.
Do not edit its new files or interrupt it. Your isolated browser QA can follow your
own source tests; coordinate exact-order performance diagnosis with parent later.
Use original test defaults, retain failures and source fingerprints, and don't rerun
until green. Stop for independent review with explicit remaining gaps. The full
native learning/E/Herald/both-engine recovery/lineage/subsessions/artifacts/tags/latest
models and matched capacity/latency objective remains intact.
