# Stage the current verified local-expert loop for native work

Your connected-inspector task bd6d56c5 is terminal native task_complete
11:24:38.081Z, matching durable1788866678101 and empty live buffer. Fable d4e0be4a
is idle after its verified10:32:31.993Z terminal. Parent read FULL
docs/L-connected-inspector-handoff.md, both new tests, the full browser runner and
relevant production notification/selection/persistence/projection changes. Parent
independently passed72tests/469assertions/types/diff on stable source, G3
.foundry/qa/2026-09-08T11-28-11.147Z-4667257d-e426-4551-b3da-5d1b3fd58f58-G3/report.json.

Parent also executed ONE fresh actual connected browser run:
.foundry/qa/parent-connected-inspector-04f9ceb4-aa68-45aa-8964-ac800a8ecf27,
11:28:50.187Z to11:29:08.348Z, exact childPID2257 exit0,142checks/52requiredPNG,
zero errors,stable source,owned Chrome2278 graceful exit0 confirmed,all fixture
resources closed. Parent checked all required files exist, actual report/outer exit,
and viewed its mobile live-learned and fresh original-guard images, plus four of
your final mobile images. Original historical messages remain distinct from current
revision. This supports the bounded connected local workflow, not native acceptance.

The next material milestone is real native learned-next-turn work, not another
expansion of inspector fixtures. Full old review retrieval beyond the100-row audit
window remains an explicit open requirement, not waived. LS copy delays also remain
open; the prepared native stack sampling experiment is not authorized here. Neither
gap converts controlled results into native proof or permits a release claim.

You now own ONE fresh current-source isolated installation/readiness task in this
same Foundry session, no production changes. Fable remains idle. Parent will not
run concurrent tests, compilers, Chrome, copies or model experiments during your
readiness checks. Keep the complete CORE-006 scope and CountTEN unchanged.

Read docs/L-fresh-installed-diagnostic-handoff.md and the previous installed/native
failure handoffs before execution. Old adoption2cba7e42 and native pilot01 are
immutable and stale for current source; never patch or reuse them. Use the reviewed
existing prepare-native-adoption.ts --execute . ../agent-session .foundry/qa path
ONCE to freeze the current source/native archive and create both isolated installs.
Preserve scripts-disabled installs, frozen-lock second install and explicit
Prisma generation without engine. Capture original/frozen/installed manifests and
exact lock diff; extra dependency changes must be visible, not silently accepted.
Do not edit captured files, package versions or tests after freezing. Source drift
or installation failure ends this attempt for review, not another freeze-until-green.

Use the same reviewed installed-native bridge/fixture controls, then the six-file
native-candidate suite described in the previous handoff, with controlled transports
and real SDK proxies. Keep original default deadlines and assertions. Remove source
preload/overlay/browser/control flags; only the documented offline recovery seam
is set for those controlled tests. Do not dump auth or environment values. Record
actual argv/PID/exit/log paths and cleanup of the precise owned transports/proxies.
Failure/timeout is retained as failure; observe original handles through settlement
instead of replay, killing native workers or clearing bindings.

Run the current phase-connected and protected boundary tests against the installed
tree as well, ensuring the snapshot actually includes the source parent verified.
No Chrome required here: the root connected browser is verified, but installed
browser/native observation stays for the subsequent separately authorized pilot.
Typecheck the installed critical scope. Verify installed package identity before
and after in BOTH trees: central maxTurns null really omits the launch flag,
auxiliary1 retains its bounded text-only profile, standalone default25 is unchanged.
These are controlled launch checks, not claims about the old preserved4400 process,
which still runs its installed25-turn default. Do not restart it as a shortcut.

Produce non-executing native-loop plans/preflights for BOTH engines against the
fresh candidate with exact requested model/effort/tools/budgets and package identity.
No real model call, browser launch, source repair, optional fourth copy condition,
native pilot, account switch, live4400/4407 restart, preserved binding/data change,
commit or release. Do not run broad source suites or upgrade SDKs to chase a pass.
Native model execution remains the immediate next separately reviewed action if
these exact installed artifacts pass; this assignment does not redefine completion.

Deliver docs/L-current-installed-loop-readiness-handoff.md with findings first,
fresh candidate/native-manifest absolute paths, source/lock/package identities,
exact results and closed/unresolved owned handles. Include the concrete command
for the next once-only Fable pilot02 from the fresh first tree, plain Continue,
full central native tools/effort/unbounded turn setting, actual post-review and
historical evidence. Do not execute that command yet. Parent will independently
verify the second installed tree and assess the lock diff before native dispatch.
Keep original full E/Herald/tools/subsessions/recovery/lineage/artifacts/tags/latest
models and matched Astra/Fable capacity/latency on the graph, not replaced by this
installation or the connected controlled scenario.
