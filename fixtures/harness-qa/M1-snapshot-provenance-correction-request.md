# Continue M1/M2: preserve snapshot evidence independently of recent history

The prior Foundry turn1a9a4615 reached actual error_max_turns at15:41:39-ish;
durable failed row and empty /api/messages/live independently verified. Final
public tool result15:41:39.819Z completed. The sole direct native child observed
is the pre-existing Playwright MCP service, not unfinished build work. Continue
this same native session with a new bounded turn, not a replay of the old task.

Parent read the entire M1 handoff through checkpoint2 and the browser scenario and
new UI helpers. The15:39:54.044Z report contains10/10 assertions, no page errors or
non-analytics failed responses, cleanupFailures[]. Parent viewed desktop initial
expert, mobile current expert and mobile turn2 screenshots. This is promising
controlled-browser evidence, not whole M1 acceptance or real native learning.

Complete the existing work with these bounded corrections, in the same UI/test
ownership. Do not edit parent acceptance, runtime, M0 helper, startup/provider,
local-store schema, live settings/bindings, candidates or native dependencies.
Astra is implementing M4-A startup composition separately.

## Confirmed independent regression

fixtures/harness-qa/acceptance/expert-snapshot-evidence.test.ts drives real M0
factory/runtime/HTTP/SQLite: migration commits architecture revision1, later
Continue abstains, GET knowledge?limit=1 contains only a later abstention but the
durable snapshot still has the original dispatch evidence with messageId
evidence-origin. knowledgeInspectionSummary drops snapshot.evidence; current
KnowledgeDomain derives committed evidence from latestLearned in the recent
history and therefore displays no committed revision even with revision1 present.

RED0PASS/1FAIL/12assertions, stable scoped source (sourceChangedDuringChecks=false):
.foundry/qa/2026-09-07T15-41-45.568Z-03108890-41ba-4b25-873f-f613333b872e-G6/report.json.
Strict test types with --allowJs pass. The test leaves original scenario artifacts
and closes its own resources. Do not weaken it or fabricate a historical record.

Preserve committed snapshot evidence in the inspector projection, and use it for
current revision provenance independently of the bounded history list. Distinguish
snapshot evidence, a matching historical learn outcome, and the latest review.
Do not attach an older/uncommitted/mismatched learned event to a newer durable
snapshot; missing before-revision or rationale stays unrecorded, not invented 0.
Add a real browser case where recent history omits the original learning event,
yet current revision evidence remains visible and the control resolves the right
owned turn. Avoid requiring the user to manually hunt/load pages for an evidence
link when the existing owned turn-detail API can resolve it directly; preserve
thread identity and legacy unavailable behavior.

## Two remaining inspection claims

Read M1-independent-live-observations.md. Current drawer still says no committed
OR pending knowledge when a legacy server does not report live state. Current
ExpertUnderstanding also equates absent domain entry with "layer is not a
configured domain expert here"; an older server with no snapshot/job cannot prove
that. Distinguish unavailable observation from actual configured/idle/empty data.
Preserve the passing initial configured expert case, historical segments and
independent cache/history regressions.

## Visual verification

The mobile turn2 screenshot is at the drawer top and shows historical metadata,
learning barrier and user message, but no expert participant. DOM assertions are
useful yet that PNG does not prove participant readability. Scroll the actual
panel to each expert's segments and capture them at1440 and390; inspect screenshots
for clipping/overlap and complete visible attribution. Keep initial and overview
screens too. Replace the large new instructional role paragraph with concise
role/scope metadata; keep the actual three parts prominent instead of a tutorial.
Do not change unrelated page styling or expand into a redesign.

Checkpoint before edits and after tests, run the unchanged parent regression,
focused inspector/legacy/cache/history tests and actual browser case, types,
fingerprints. Record all failed runs. Provide a complete final handoff before the
25-turn cap. No new native call beyond your owned work session, install/restart,
capture, configuration, candidate, credential or commit operation. Count remains
eight and acceptance remains independent.
