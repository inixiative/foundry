# Fresh isolated installed diagnostic for native L

Your diagnostics619b93ea finished: native task_complete07:13:17.558Z, matching durable1788851597566, empty buffer. Parent read FULL final handoff and actual code. Parent independently verified the integrated diagnostics plus parent Markdown file-URI correction in stable source report .foundry/qa/2026-09-08T07-14-16.987Z-4a6b54ab-a766-4c4c-9341-863366bf3bb4-G3/report.json, tests/types/diff all pass. Read docs/E1-markdown-source-parent-correction.md for the legitimate parent factory change to bf205874, not your edit. Parent accepts the bounded diagnostic correction; full native goal remains open.

I authorize ONE fresh isolated DIAGNOSTIC installation using the existing reviewed scripts/prepare-native-adoption.ts --execute . ../agent-session .foundry/qa. This freezes exact allowlisted source and the native package, installs first with --ignore-scripts and then independently with --frozen-lockfile --ignore-scripts, explicitly generates Prisma --no-engine, and checks both installed native package trees. Root installation, existing immutable candidates, live servers, bindings and databases must remain untouched. This is not rollout authorization or final-source acceptance: Fable915b05fc concurrently owns browser-order investigation and may change browser source. Label the captured source digest and any later drift honestly. Never patch the captured tree to include later changes. If freezeCopy detects movement, retain the failed artifact and report; do not loop stage attempts silently.

After successful installation, run the actual installed native candidate tests (controlled native transports, real installed package classes and real local SDK bridge processes) from the new first tree. Read docs/G5-installed-prewrite-failure-handoff.md first, then the actual current tests. Start with non-browser bridge/fixture controls, then the non-browser full native-candidate suite covering recovery-sample, app-server, outcomes, bridge, learning-settlement and fixture-lifecycle as applicable. Use FOUNDRY_RECOVERY_OFFLINE=1; UNSET FOUNDRY_QA_PLAYWRIGHT and browser flags so Fable's exclusive Chrome slot is untouched. No source preload or test overlay; the point is current tests and current production source together resolving the installed native package. Retain exact commands, exits, logs, before/after package/lock/source hashes, scoped diagnostic reports and owned cleanup. Verify upgraded checkNative reports null central -> no max-turns flag and auxiliary1 -> max-turns1 in both trees (controlled argv, not CLI admission).

If a test fails, diagnose its exact awaited boundary with read-only retained evidence. No production/test edits in this task, no changing assertions/timeouts, no copying root files into the candidate, no unowned process kills. Unknown original native work remains owned, never replayed. Native model calls and actual Claude/Codex model CLI admissions are NOT authorized by this diagnostic task. SDK fixture processes and the two isolated dependency installs are authorized. Do not fix Fable's shared runner/driver work or chase unrelated cleanup.

Only writable source deliverable is docs/L-fresh-installed-diagnostic-handoff.md; generated new QA/staging files belong under .foundry/qa. Return exact installed paths/manifests, counts and source movement limitations, and state whether installed central native-bridge prerequisites now pass. Do not self-approve the final native pilot or a live cutover. The next step after reviewed installed/browser evidence remains an explicitly supervised native expert-learning run with full central tools/model/effort, exact terminal/cleanup and later-input consumption. E live lifecycle, Herald, both-engine real project work, recovery/lineage/artifacts/tags and latency remain the same full goal; countTEN unchanged.
