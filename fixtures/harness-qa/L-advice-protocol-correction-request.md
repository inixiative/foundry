# Correct the expert advice response contract

Continue the full CORE-006 goal in this existing Foundry session. Your prior turn915b05fc has native end_turn, matching durable completion, and an empty buffer; its browser-order handoff is read. Parent owns independent verification. Astra separately owns only initial sample Harness decision-role wiring in scripts/domain-loop-composition.ts and a new focused test. No browser slot or native experiment is authorized for either source task.

The one real native pilot m4-native-fable-20260908-01 FAILED before learning; read docs/L-native-fable-pilot-handoff.md. Parent independently found that composition supplies broad domain instructions as advisePrompt, replacing DomainLibrarian's default response schema. Actual Fable pre-hook outputs were arbitrary domain JSON rather than layers/snippets/confidence. DomainLibrarian.advise silently accepts arbitrary parsed JSON as empty advice with confidence0.5. This is a proven protocol defect, NOT proof of the cause of the native 15s timeout. Do not solve latency by increasing deadlines, lowering effort/model, or treating this correction as a successful live loop.

Parent added protected packages/foundry/tests/domain-advice-protocol.test.ts. Baseline G3 .foundry/qa/2026-09-08T07-50-18.495Z-a846946b-6991-4730-a337-a87785df3d45-G3/report.json: 2pass/13fail/28assertions, schema omission and invalid outputs accepted; explicit valid abstention and null failure controls pass. Keep all its bytes/assertions unchanged.

Own a narrow correction in packages/foundry/src/agents/domain-librarian.ts plus new author focused tests and docs/L-advice-protocol-handoff.md. The response protocol must be supplied by the advice phase even with custom domain instructions, not require each caller to remember a schema. Preserve independently inspectable domain instructions/domain knowledge/thread understanding, exact historical input semantics, normal and fenced valid responses, explicit abstention and existing error propagation. Validate object shape, string arrays, finite confidence0..1 and optional boolean/string fields at runtime. Malformed responses must produce explicit bounded non-payload error, never plausible successful empty advice. Review downstream fixtures and integration before choosing a boundary; do not silently weaken existing assertions or replace domain instructions. An additional phase message is preferable to mutating the stored instructions just to satisfy a protocol, if coherent with current provenance. If exact historical phase protocol is not currently captured, identify that gap honestly rather than claim it solved.

Do not edit composition, configured-experts, runtime, providers, journal, graph/QA ledger, any parent fixture, immutable candidates/run, live servers/bindings/accounts or installed packages. Do not broaden to guard/review unless you report a demonstrated need to parent first. Preserve model/effort/tools/deadline and unknown-work ownership contracts. No model calls beyond your existing supervisor task, no native experiment retry, no installs, commits or process kills. Run focused advice, domain lifecycle and configured expert tests with types; report legacy fixture incompatibilities rather than disguising them. Finish a concise handoff with source hashes, actual stable test evidence and remaining limits for independent acceptance.
