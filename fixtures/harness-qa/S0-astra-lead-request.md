Continue as provisional Foundry lead in this same Astra native session, preserving
the full CORE-003 / CORE-004 / CORE-005 and AS-001 through AS-005 goals. Read the
latest ledgers and supervisor acceptance note before executing this assignment.
If G4's bounded correction is still under review, do not advance or duplicate work.

Dispatch update: Fable storage review completed2026-09-07T00:27:33.978Z, Foundry
turn turn_b191b46e-4106-4d48-9892-e29ce89ebce8, verbatim report
fixtures/harness-qa/QA-2026-09-07-fable-storage-review.md. Both bounded recovery
slices are independently accepted (count2), not full G4. Fable and supervisor
retain provisional Astra leadership after the scheduled checkpoint; reevaluate
after two further accepted slices or a serious regression. Add success-followed-
by-infrastructure-failure cases to your own future persistence/transport matrix.
The full goal and remaining recovery/memory/inspection/native gaps are unchanged.

Your proposed next node is CORE-004 S0: real recorded native protocol fixtures.
Lead this slice: confirm the graph and scope, make a bounded execution plan, then
implement it. Own sibling ../agent-session scripts/tests/fixtures/docs as needed,
plus a CORE-004 work-log entry. The sibling was clean before assignment. Foundry
still resolves registry agent-session0.1.0, NOT that sibling. No runtime src,
Foundry UI/provider/persistence, dependency/lockfile, engine-default or installed
node_modules edits in this slice. Do not add a fourth engine or publish a package.

Baseline: sibling bun test passes14 tests/35 assertions, all mocked Claude. Its
plain bun run typecheck fails because dependencies are not installed. This exact
read-only workaround passes from sibling cwd without installing anything:
bun ../foundry/node_modules/typescript/bin/tsc --noEmit --project tsconfig.json --typeRoots ../foundry/node_modules/@types
Include added scripts in an appropriate explicit typecheck; do not silently leave
the recorder untyped. Both existing classes expose an injected spawn seam.

Capture one bounded disposable successful tool-using session for Astra and Fable
through the existing CodexMcpSession and ClaudeCodeSession paths, not invented
event objects or a new engine. Use a new controlled sample directory for each,
with a known small sentinel file. Ask for bun --version and reading that sentinel,
then a short response. No repo edits by the probe, nested agents or other work.
Use current supported model configuration and preserve actual acknowledgment:
requested model/effort is not proof of the runtime's selected model/effort.
Record CLI/package versions, source revision, probe prompt and controlled artifact
hash, timestamps, owned process lifecycle, and native session/turn/item/call IDs
only when actually observed. Account identity/capacity remains unknown unless a
supported non-secret interface explicitly proves it; do not read credentials.

Record protocol evidence at the existing injectable transport boundary as well
as normalized SessionEvents, so dropped events and identity loss can be compared.
Never write raw reasoning, auth/session secrets, environment values, private
workspace content or unrelated native tapes into fixtures. Use structured explicit
allowlists before persistence, preserve correlation with stable redacted IDs when
needed, and test sanitization with adversarial nested fields and unknown types.
Unknown event shapes should retain safe schema/type information, not arbitrary
values. Do not include thinking/reasoning payloads in model-visible tool output.
If genuine protocol evidence cannot be captured, mark it missing; do not fabricate
a successful fixture or weaken a desired behavior assertion to match a bug.

Create offline replay/integrity tests against the recorded sanitized fixtures:
framing/chunks, actual terminal status, tool begin/result identity and output,
normalizer losses/duplicates, available usage fields, and explicit absent evidence.
Distinguish fixture-integrity checks from capability acceptance. Record regressions
for S1/S3/S4 rather than changing production parsers during S0. A completed local
send is not automatically native completion; capture the native protocol fact.
App-server remains experimental and is not the default for these probes.

Include one bounded check/recording of the existing experimental app-server path
as well, without changing defaults. Do NOT adopt the review's unverified assertion
that only app-server carries turn/item/status identities: inspect actual MCP and
app-server evidence. Current source uses a WebSocket listen argument with stdio
IO and may not match current turn/start schemas. Verify installed CLI help/schema
first, then record the actual existing-path result. A verified handshake/schema
failure is useful evidence, not a successful native-turn fixture. Do not silently
patch around transport incompatibilities in the recorder, invent an acknowledgment,
or turn the capture script into another engine. If source correction is necessary,
stop that path with explicit evidence and propose the smallest S1 follow-up that
will produce its successful fixture. The two existing active engines still require
their successful tool-using recordings for S0 acceptance.

Bound this enabling slice: at most one new probe per path (Claude, Codex MCP,
experimental app-server), plus deterministic offline sanitization/replay tests.
Target a20-minute execution window; report a partial result/next action if external
latency or a protocol issue prevents finishing, without calling it accepted or
abandoning an owned running process. No open-ended fixture expansion or automatic
retry. A failed app-server preflight need not prevent capturing working protocols.

No auto-retry of an ambiguous timed-out probe. Record its identity and unresolved
state, inspect only its owned process/native terminal evidence, and clean up only
owned processes once outcome is established. Keep concurrency/budgets bounded;
no primary4400 or lead4407 restart, no binding changes, credential copying,
detached agents, subscription switching, commits or publication. Disposable probes
are explicitly authorized for this slice; never use the work/review sessions as
test targets. Local timeout alone does not prove native cancellation.

Run existing sibling tests, new offline fixture/sanitization tests, explicit
typecheck and diff checks. Provide exact recordings/reports and a capability-loss
table grounded in protocol evidence. Propose the next implementation node with
owners/interfaces and independent tests. Preserve native fork/rewind/subsessions,
artifact/attachment/tag inspection, all three decoration segments, model/effort
parity and eligible subscription continuity in the larger graph. Do not self-accept
S0: return for Fable review and supervisor independent artifact inspection.
