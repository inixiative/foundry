# Native adoption readiness and independent regressions

Supervisor, 2026-09-07 approximately04:18-04:28Z. Previous goal turn: progress
(real two-admission captures, independent frozen-recording checks,14 model cases
and owned learning regressions). Full goal active, Astra count5/next6.

## Accepted bounded continuation evidence

Fable's read-only review completed04:21:17.676Z, Foundry
`turn_902932a0-589b-4195-8719-994d41f86908`. Full handoff read:
`QA-2026-09-07-fable-continuation-review-and-IT-handoff.md`. Supervisor accepts
the bounded real two-admission evidence after opposite-model review, independent
wire/admission/tool joins and frozen hash checks. This does not accept full native
S1/G5, context retention, cancellation, usage/capacity, subscription continuity or
Foundry adoption. Source engines in the captures remain sibling-only.

Correct review overstatements: only MCP has demonstrated distinct native turn
IDs; Claude has distinct admissions/result UUIDs and ordered-stream correlation.
No raw MCP stderr was inspected by the supervisor, so the576 bytes have unknown
meaning, not independently established benign content. MCP second-turn model
is not separately re-acknowledged; same-process continuation is an inference.
Future rollback preserves new journal data, not an old snapshot over newer work.
Publishing/version0.2.0 is neither required nor authorized for local adoption.

## Compaction integration hazard: two independent failures

`native-compaction-evidence.test.ts` first replays the two actual sanitized Claude
init envelopes from the accepted capture through the production sibling class.
A repeated init alone causes one session_compact event despite providing no
explicit compaction evidence. The second synthetic supported positive control
uses init -> compact_boundary -> init and yields two events instead of one.
Foreign compact boundary must never adopt or invalidate this owner.

Stable RED `.foundry/qa/2026-09-07T04-20-00.582Z-G5/report.json`: two failures.
Standalone strict ES2023 TypeScript passes. Actual envelopes are replayed without
model calls; the explicit boundary control is labeled synthetic. Historical
records will not be rewritten to pretend their normalizer had the correction.

Fable owns sibling-only correction after verified terminal:
`S1-explicit-compaction-correction-request.md`, Foundry
`turn_2771a53c-cd39-45c0-b665-7cd96f1002b2`, HTTP200/start. Actual native tools
verified04:25:25.060Z. Seven independent native files now total21 cases after
allowlist extension. No new captures, Foundry changes, package install or rollout.
Astra continues disjoint owned learning implementation, actual tools04:25:01.905Z.

## Typed provider adoption: two failures, one legacy control

New `native-adoption-boundary.test.ts` supplies the sibling's typed SessionResult
to Foundry's actual provider through a controlled adapter, without native calls.
Typed native failure is currently returned as successful text when the old Claude
raw envelope is absent; typed completed admission/terminal/local outcomes are
dropped from provider metadata. Legacy absence of native evidence correctly does
not become acknowledged completion.

Stable RED `.foundry/qa/2026-09-07T04-21-41.988Z-G5/report.json`: two failures/one
positive pass. Standalone strict types pass. This is a preregistered intended I
interface test, not a claim that the sibling package is already installed. Fixing
the facade alone will not complete propagation through execution/journal/browser
or late admission reconciliation. Keep completed-native/local-failure and unknown
occupancy cases in the broader adoption matrix.

## Real MCP call scope: one failure, one live-layer control

New `native-tool-scope.test.ts` uses the actual Foundry MCP server and the installed
SDK Client/InMemoryTransport protocol pair. No private handler invocation, fake
tool response, native model, network connection or package installation.

An actual foundry_threads call exposes another project's private description,
tag and thread ID from a mixed-project SessionManager. The bound owner belongs
to projectA; the returned private metadata belongs to projectB. The independent
positive control shows foundry_query can read the bound live thread's updated
layer, while its earlier result remains unchanged.

Stable RED `.foundry/qa/2026-09-07T04-25-04.487Z-G5/report.json`: one failure/one
positive pass. Standalone strict types pass. Scope enforcement is a prerequisite
to connecting this tool surface to a native worker. It is not a claim that the
currently disconnected native work sessions can exploit it.

Source inspection confirms mcp/cli.ts constructs a separate `mcp-session`, not the
active thread/runtime. It implements only inline/file/markdown loading and returns
an unsupported marker for other sources. Merely adding thread/project arguments
would not establish authoritative live knowledge, memory ownership/retrieval or
dispatch/call provenance. Use an explicitly owned runtime tool boundary; retain
text-only auxiliary tool restrictions. Required-context blocking must not be
relaxed until real scoped retrieval is demonstrated end-to-end.

## Next ready actions

After actual Fable completion, independently verify21 native cases and obtain
Astra's read-only correction review before I adoption. T needs authoritative
thread scope, authorization and actual call evidence; its mixed-project leak can
be corrected separately without touching active learning code. Before changing
dependency resolution, checkpoint and isolate the fixture/build boundary; preserve
active native sessions and new journals. No package publication or implicit
source/backend switch is authorized by this record. Stable combined baseline and
model review remain pending while Astra's learning implementation is active.

## Subsequent verification and cross-review

Astra learning ended04:35:30.941Z and Fable compaction ended04:29:54.561Z.
Supervisor now independently passes21 native cases,14 model/profile cases and
eight learning cases. Actual browser checks add six historical input positives
and reproduce two user-facing gaps: stale external-message observation despite
owned WebSocket events, and missing pending-learning inspection. Current review
assignments and exact evidence are in QA-2026-09-07-learning-browser-and-cross-review.md.
The previous next-action paragraph is historical, not authority to repeat either
completed implementation. No native capture, dependency adoption or rollout.
