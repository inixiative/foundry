# Complete the runnable native recovery experiment

DISPATCH READY12:13Z: original task actually completed12:11:50.548Z, matching
durable final and full handoff read. Parent final18independent readiness/recovery
checks pass on Codex0be222c234e858d963039a5bb4cbae9a5d7c4c923ceea8e061c9ea959d056bd8,
types/diff and stable rootb96d96ee1e5a43dac4eea4030e51545b314015bcd098aedc24cd149f97a9afd5.
The preparation condition below is satisfied. Proceed with this implementation,
not the guard edit requested in your last final: Fable owns that separately.

PREPARED, NOT DISPATCHED until original R1/R2 actual task_complete and durable
final, complete docs/G4-native-adapter-bridge-handoff.md read. Same Astra session,
full goal/count7-next8. R1/R2 remains unaccepted until integrated guard/source/
opposite review passes. Do not mistake a written handoff for actual terminal.

Ownership clarification: parent assigned ROOT scripts/native-retrieval-guard.ts
and focused guard tests to Fable as turn_9acb4ccf-123e-4491-964c-58f4877124ec.
Your prior async guard ownership question is resolved by that disjoint assignment;
do not edit those files. Parent new11item identity cases + old15provenance gave
25pass/1fail, exact item-only positive RED; Fable implements discriminated IDs.
You own existing sample runner, source checker and new focused recovery sample
tests only. Adapter/provider/sibling corrections require an explicit recorded
reason and bounded ownership; no broad refactor or frozen candidate modification.

Implement the currently non-executing recovery plan as an explicit opt-in capture
mode using the production Foundry viewer/runtime/provider and installed package.
No actual model call in this implementation assignment. All capture runs require
reviewed immutable candidate, exact manifest and separate supervisor authorization.
No root install, native/live restart, account/binding/credential change or commit.

Flow: preflight installed package/protocol -> exact prewrite owner -> fresh native
thread and scoped bridge inventory -> bounded real tool work -> known native
terminal AND local/RPC capacity available -> exact-owner release -> process exit
and bridge cleanup -> new owned construction -> thread/resume SAME persisted
native binding with refreshed bridge -> second bounded tool work -> durable old/
new evidence and existing/fresh desktop/mobile observer verification -> cleanup.
No implicit retry, migration of existing work bindings, third admission or process.
Use fresh isolated fixture project and capture identity; preserve all failure data.
Unknown outcomes stay attached to same handle, not retry/rebind/kill on timeout.

Strengthen the plan beyond independent tasks: also prove native conversation
continuity. A1 must learn a unique fixture nonce through actual native work. A2
must report that prior nonce after cold resume, while it is absent from A2's
prepared Foundry input and every A2 tool result. Separately require a new owned
tool operation after resume so answering from old context is not enough. Inspect
current thread-state/history assembly before designing this; detect any accidental
Foundry replay of the nonce and fail rather than counting it as native retention.
Do not strip normal production context to fake this condition. Fixture data and
second-turn tool policy can isolate the prior fact without weakening capabilities.
If the existing production context makes omission impossible, surface that exact
boundary with evidence and propose the actual correction, not a narrower claim.
Native history metadata should show the old native turn; full paginated artifact/
subsession hydration remains separate open work, never claim it from a summary.

Capture exact native thread/turn/item IDs, separate SDK receipts, actual start/
resume configuration, required owned readiness, prior frozen snapshots, prepared
messages, public tool inputs/results, persistence outcome and cleanup state.
No hidden reasoning/private config/credentials. Unknown effort/account/budgets
remain unknown. Retain requested full Astra model/effort/tools; no silent fallback.
Record timings by phase for later matched native latency QA, not p95/parity claims
from two turns. Current/historical inspector and artifact links must be usable.

Offline tests before capture: failed module/browser/setup creates zero processes;
registration/config/readiness/refused binding cannot write work; second phase
withheld on first failure or unresolved capacity; foreign release refused; exact
process/admission ceilings; partial/failing reports retained; prior nonce leakage
fails continuity check; wrong/missing recall fails; successful controlled path
uses production adapter and real SDK proxy with cold reconstruction; browser
history and cleanup failures remain failures. Keep existing Claude/MCP runner
and all original tests unchanged in behavior. Tests must not launch model CLIs.

After Fable guard actual terminal/full handoff, opposite-review it read-only and
run unchanged parent item/provenance/discovery/reference tests plus production
app-server SDK tests. Do not overlap active Fable edits or accept a moving hash.
Finish docs/G4-native-recovery-capture-handoff.md with exact files/hashes/commands,
guard-review verdict when available, honest open gates and next candidate steps.
No capture/staging authorization is granted by this implementation request.
