# Integrate the public reference contract into Foundry evidence

Continue the SAME Fable/Foundry/native session after actual I2 review terminal
11:02:36.993Z and durable final. Full opposite review read; parent accepts the
bounded source correction (independent5pass plus opposite49/type/diff), not the
full I2/native slice; count6-next8 remains. The low committed-capacity-expiry
audit label issue is retained for follow-up, not part of this edit ownership.

Implement Foundry projection for your completed sibling reference contract.
Own ONLY packages/core/src/native-evidence.ts, packages/foundry/src/providers/
native-evidence.ts and focused projection tests. Do not edit sibling sources,
runtime/librarian, bridge/transport/guard, UI or parent acceptance fixtures.
Astra owns active sibling Codex recovery source/tests and its architecture graph.
Its later opposite review must cover your authored normalizer AND this projection.

Read docs/T3-claude-reference-handoff.md and unchanged parent
acceptance/native-claude-tool-references.test.ts (3pass/1projection failure).
Add optional readonly toolReferences/toolOutputOmittedTypes to public evidence.
For tool_result only, preserve exact valid public reference strings in copied/
frozen arrays, including ordering and duplicates. Never forward raw envelopes.
Do not turn malformed arrays or unsupported labels into an apparently complete
discovery result: retain explicit omission when information cannot be represented.
Omission types use public allowlisted labels, not arbitrary private type values.
Preserve text/error/owner/admission/call and existing output-omission behavior.
Reference-only output is not retrieval success; the existing guard decides.

Add focused RED-to-GREEN malformed/mixed/duplicate/mutation/private payload and
non-tool-result tests. Run unchanged four parent reference cases plus independent
discovery-order/provenance tests and existing native projection/provider tests,
strict types/diff with scoped before/after hashes. Do not claim a moving sibling
whole-tree fingerprint as integration acceptance. No new archive/candidate until
reviewed source integration is ready; no native calls in this assignment.

Append projection contract, evidence, hashes and limitations to
docs/T3-claude-reference-handoff.md. Also clarify the I2 review browser wording:
parent has six controlled HTTP snapshot lifecycle renders (canonical QA), not
actual native learning/browser proof. Do not erase historical statements.
No root install, live restart, model probes, accounts/credentials/bindings,
commits, staging or source edits outside owned files. Use apply_patch. No
self-acceptance/count advance; full native/recovery/artifact/inspection/pooling/
latency goal is unchanged.
