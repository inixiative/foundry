# Complete middleware phase history and the post-work protocol

Continue the full CORE-006 objective in this Foundry session. The user clarified:
each domain expert owns instructions, domain knowledge and its OWN versioned thread
understanding. Shared thread records are attributed evidence; the Herald selectively
brings cross-thread evidence, never replaces each expert's interpretation. Preserve
this separation in every phase and every inspector surface. Pooling remains deferred.

Your guard task0e4071e3 has native end_turn08:45:33.360Z, durable1788857133417 and
empty buffer. Parent read the FULL guard handoff and independently passed56tests/
601assertions with types/diff and stable source in G3 08:49:53.920Z-79014a10.
This accepts the bounded guard source contract, NOT its persistence or UI.
The earlier advice capture had source proof only; JSON roundtrip was not an actual
database reopen. Do not repeat that as persistence acceptance.

Parent independently found a post-work protocol gap. The full schema exists only
in the default reviewPrompt; a custom reviewPrompt replaces it, leaving the user
request with only a knowledge-length hint. New protected
packages/foundry/tests/domain-review-protocol.test.ts is RED0pass/2fail/17assertions
on stable source, G3 08:51:53.615Z-fbcb2967. Focused strict types pass. The default
case checks that phase-owned protocol is independent of the instruction string;
the custom case proves the actual schema disappears. Preserve this fixture unchanged.
Supply the post-work phase's response contract independently, retain custom system
instructions verbatim, describe learn/replacement and abstain accurately, preserve
the current validator and bounded evidence without restricting fresh interpretations.
Capture the exact resulting request, not a reconstruction from today's protocol.

Finish LI across advice, guard, post-work review and cartographer routing as ONE
coherent existing-system increment, including guard outcome inspection. First read
the actual call/observation/storage/history/UI paths. Extend the current per-invocation
advice capture and existing owned journal/artifact mechanisms; do not invent a second
middleware framework, shared mutable lastRequest, separate journal or flattened
expert memory. Inspect existing source/tests before choosing the common types.

Required behavior:

- Each actual phase request records the exact role/content messages supplied to its
  provider, phase, domain or routing responsibility, provider identity, original
  thread/project/turn or review-job/call correlation, and relevant frozen revisions.
  Existing native evidence remains separate: supplied to provider is not proof of
  native receipt, execution, completion or released occupancy. Do not serialize
  credentials, callbacks, hidden reasoning or full unrelated tapes.
- Capture at the real call boundary, before await, with immutable invocation-owned
  data. Preserve early refusals/cold cache/programmatic/no-call, pending/deadline,
  provider failure, malformed response and missing historical evidence distinctly.
  Late results update only the correct original outcome, never rewrite old input
  or promote stale learning. Don't invent missing turn/call IDs by guessing.
- All three expert-owned parts remain independently inspectable. Shared thread
  evidence, original message/work/tool observation and phase protocol must also be
  inspectable as actual inputs, not silently folded into the domain's memory.
- Guards retain their explicit results and per-domain outcomes durably with the
  source observation identity. A failed or unattempted guard must never become a
  successful all-clear in storage or UI. Post-work history links request/outcome,
  original job, before/after revision and commit/discard status without changing
  admission, CAS, hard eligibility, cleanup or publication behavior.
- Restart/reopen must recover actual historical phase inputs and outcomes after
  live cache, instructions and protocol have changed. Old records without captures
  say not recorded; never reconstruct them from current source or settings.
- Complete right-panel historical phase inspection using the existing utilitarian
  UI. Keep original message and decorations separate. Avoid per-row whole-history
  duplication and new eager large payloads. Long inputs should be collapsed and
  inspectable; concise labels, stable desktop/mobile layout, no tutorial prose.

Scope: domain-librarian.ts, flow-orchestrator.ts, cartographer.ts, thread-runtime.ts,
their existing message/type exports, and narrowly necessary existing persistence /
viewer routes / inspector-data / drawer/UI modules. New focused author regression
and integration/browser tests plus docs/L-complete-phase-history-handoff.md. Preserve
all existing protected tests/checkers/assertions, parent fixtures, original failed
pilots/candidates and sibling native package. No edits to domain-loop-composition.ts
or its admission/policy tests, native transport/provider/driver, accounts/bindings,
root installs, live servers, budgets/models/effort/tools or CountTEN. Report a genuine
required boundary crossing before editing it. Parent shortened the advice drawer
labels after your earlier task; preserve that intent.

Verification must include unchanged parent protocol/guard/provenance fixtures,
new real call-boundary concurrency and timeout/failure controls, and an actual
SQLite write/close/reopen/history read with the real storage/runtime path. Use the
existing controlled provider for deterministic proof, not fabricated history rows
as the only integration evidence. Prove cross-thread/private-domain isolation and
that metadata adds no new central prompt content. Keep existing learning lifecycle,
native occupancy, original job/revision/CAS tests passing unchanged.

For rendered verification, use owned headless browser infrastructure against an
isolated viewer only after source/timing tests finish. Mac UI is known locked; do
not bypass it or claim manual viewing. Parent must independently inspect retained
desktop/mobile screenshots, successful and failed/legacy inputs, long content and
browser errors. Browser source currently has an unresolved order-sensitive failure;
retain real failures, exact source hashes and owned-resource cleanup rather than
rerunning until green or widening deadlines. No real model pilot, installation,
live cutover, process kill, account change or release acceptance in this task.

Astra6fa32af2 is terminal08:51:07.588Z/durable1788857467613/empty. Its full handoff
records a corrected queue but an integrated120pass/4fail/4skip run with setup stalls;
that run remains failed. The queued correction request was never dispatched because
Astra found/fixed it independently. Parent is verifying focused LA separately before
dispatching this request. No other source owner is authorized to edit your modules.

Keep evidence proportional: preserve exact failing and final commands, report the
files actually covered, actual persistence/browser limitations, source changes and
owned cleanup. Do not spend the task generating redundant audit reports instead of
finishing the usable lifecycle. Stop for independent review after implementation
and verification; full native learning, E identity lifecycle, Herald, both-engine
real work/recovery/lineage/subsessions/artifacts/tags and matched capacity/latency
remain on the graph and cannot be accepted from this controlled increment.
