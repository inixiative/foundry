# Implement native item receipt identity without fabricating call IDs

Continue SAME Fable/Foundry session after actual evidence-review end_turn
12:04:14.715Z, durable final and full report read. Parent accepts actual Claude
retrieval evidence bounded; same full goal/count7-next8. Your baseline erratum read.

Supervisor assigns YOU exclusive ROOT scripts/native-retrieval-guard.ts and its
focused tests for this correction. You authored the existing guard. Astra's
R1/R2 work currently leaves it unchanged while requesting ownership; parent
assigns this disjoint correction to you and will tell Astra on its next handoff.
Do not edit Astra provider/adapter/native-launch/sibling/native sample runner or
native-candidate/app-server.test.ts. Frozen candidates/packages/captures immutable.

Actual installed schema MCP ThreadItems carry native item ID, not a call ID.
Astra projects native thread/turn/item identifiers explicitly without inventing
call IDs. Existing guard refuses every such exact public receipt. Independent
new fixture native-item-retrieval-identity.test.ts has11cases: item positive RED,
9wrong/missing/mixed/duplicate identity negatives and legacy call control positive.
With unchanged15provenance cases:25pass/1fail44assertions, scoped strict types pass;
.foundry/qa/2026-09-07T12-04-51.639Z-e3c9de67-7aec-4043-a3bd-8cfdc8d429e8-G5/report.json.
Preserve every existing independent assertion; inspect full source and tests.

Implement discriminated native identity handling: keep call ID namespace and
exact native thread/turn/item tuple distinct. Never copy itemId into callId or
derive either from SDK IDs. Reject mixed, missing, duplicate, foreign or conflicting
identity; coherently foreign pairs must also fail against the admitted native
thread/turn, not just match one another. Preserve original owner/admission/full
receipt/args/result/digest/bridge/publication checks, one-to-one accounting and
fail-closed discovery. Add focused positive and adversarial coverage, especially
colliding item/call string values and all mutations that could accidentally relax
Claude/MCP call joins. Diagnostics should identify native item evidence honestly.
No synthetic native call IDs in reports. No broad exceptions for missing events.

Use focused harness/type/diff checks with stable guard hashes. All original
15provenance/3discovery-order/4reference/32guard cases must remain positive, plus
new parent11 cases and your adversarial additions. Actual previous Claude sample
is frozen evidence, not a retry target. Astra will opposite-review guard integration
before installed candidate/native app-server capture. No model work in this task.

Also append a precise correction to your I2 design in
docs/T3-reviewed-claude-evidence-review.md: native eligibility and physical/RPC
capacity are separate. The accepted parent native-learning-eligibility tests
explicitly allow exact-owned native-completed learning to publish while RPC remains
pending, with capacity still occupied. Your table requiring both native and RPC
settle before commit contradicts that contract. Re-read actual reconcile code;
do not restore blocking behavior or rewrite the prior evidence. The actual I2
experiment should include this distinction, with separately authorized bounded
controlled timing fault versus ordinary native success, not fabricated native
terminal evidence. Report title's read window through12:12 exceeds actual task
terminal12:04:14.715Z; correct timestamp accounting too. No I2 source edits here.

Output docs/G5-native-item-guard-handoff.md with source hashes, complete verification,
identity contract and explicit native limitations. Preserve full product graph.
