# Lead correction: usable learning capacity and durable closed-state evidence

Prepared by supervisor. Dispatch only after your current I-native-integration review/design task is actually terminal. Do not interrupt that work or run this as a duplicate. Count remains5/checkpoint6. Fable's learning review ended04:45:35.008Z and reopens the implementation; see QA-2026-09-07-fable-owned-learning-review.md. Full goal unchanged.

Supervisor independently reproduced the principal findings through production runtime and SQLite, plus actual OpenAIProvider talking only to a disposable loopback HTTP server. New learning-closed-capacity.test.ts: closed unknown occupancy is forever labeled pending; later non-admitted work is retained without bound and without durable refusal records; a completed stateless HTTP503 disables review of later distinct work. Original RED at04:49:00.273Z-G3 (3 cases); a fourth separates payload-retention assertion. No native calls or credential use. Preserve unknown native occupancy/no replay positives.

Own learning-config/domain-librarian/thread-runtime/knowledge-persistence/local-session-store and focused tests, narrow provider capability contract/startup/factory wiring if genuinely necessary. Do not touch Fable's active viewer UI/route implementation, sibling, independent assertions, shared ledgers, live settings or dependencies. No native probes, live server changes, agents, publication or installation. Manual edits via apply_patch.

Implement truthful closed/deferred/error states and bounded hot-cache retention. When later completed work cannot be admitted, preserve its owner and evidence link durably with an explicit reason, do not retain endless payloads or call it pending. Distinguish actual in-flight review from work that cannot run. Do not solve this by silently dropping evidence or declaring knowledge complete. Existing central turn records remain the recovery source; an explicit later reconciliation may act only with authoritative ownership/capacity evidence.

For stateless completed HTTP failure, permit the next distinct work admission without replaying the failed review. Unknown transport/native work must remain owned, with no hidden retry. Prefer explicit provider execution/settlement capability to guessing from arbitrary provider IDs; wrappers must retain or truthfully mark that evidence unavailable. Test completed API error, truly unknown native error, hard expiry, later terminal reconciliation and disposal/replacement ownership separately. I native integration remains the prerequisite for actual native terminal reconciliation, not an excuse to break stateless capacity.

Address rollout lifecycle/configuration findings: keep auxiliary phase settings faithful and explicit; do not silently default expensive reviews against the existing configured flow policy, or silently downgrade a requested review model. Expose the chosen phase provider/model honestly. Generated per-runtime review sessions need owned cleanup: never simply clear/rebind an active unknown worker and assume cancellation, and do not keep settled idle processes forever. Use the existing adapter/provider ownership surfaces where supported; explicitly defer unavailable native acknowledgment to I with a tested state and no parity claim.

Retain a durable audit of committed-then-publication-failed reconciliation before disposal, without allowing fallible observers to veto or corrupt the commit. Preserve completed output, old history and durable revision across reconstruction. Review manager/runtime lease consistency and reused runtime restoration; fix where necessary for these lifecycle behaviors, not unrelated cleanup.

Supervisor accepts the nonblocking contract clarification from both models: immediate requests use last committed revision and show pending only for genuinely runnable/in-flight reviews; first post-commit request sees new fact without repetition. The independent historical serial-barrier assertion will be replaced by supervisor with those two production-signal-based cases and original failure evidence retained. Do not change it yourself or mark it expected.

Run focused new regressions plus all independent learning tests, unchanged26 memory cases, source types/diff and baseline sequentially with stable evidence. No self-acceptance. Write docs/G3-closed-capacity-correction-handoff.md. Opposite-model re-review and supervisor SQL/browser/native gates follow; count6 is not automatic on source tests. Fable separately owns live browser observer/history/learning inspection under G4-live-observer-learning-inspector-request.md; coordinate by stable contracts, no shared-file overwrite.
