# Preserve guard failures and each expert's own thread understanding

Your advice capture f24be786 is terminal: native end_turn08:31:20.907Z,
durable1788856280972 and empty buffer. Parent read the full handoff and independently
passed66tests/558assertions/7files with stable source, types/diff in G3
08:35:20.569Z-3988bb2e. The new request captures actual per-invocation provider input
without merging the three owned parts. This is bounded source evidence, not rendered
or actual SQLite/restart/browser acceptance. Parent afterward shortened only the new
drawer copy into concise status labels; preserve it. No browser proof is claimed.

Parent now has four independent guard failures in protected
packages/foundry/tests/domain-guard-evidence.test.ts: a provider refusal, malformed
JSON and missing findings each return EXACTLY the successful no-findings result;
a learned domain interpretation is not sent to that domain's guard at all, although
common state and domain knowledge are sent. RED0pass/4fail/16assertions,15ms, three
stable source/test hashes: .foundry/qa/parent-domain-guard-evidence-rTDWbR/report.json.
Focused strict types pass. Keep all parent fixture bytes/assertions unchanged.

Own only DomainLibrarian guard result/input/validation behavior, the corresponding
FlowOrchestrator guard aggregation/emitted observation, new focused author tests,
and docs/L-guard-outcome-handoff.md. Preserve the completed advice capture and its
contract, including fresh interpretations (not excerpt-only), immutable request
attribution, three separate parts, and central-context non-injection. Astra6fa32af2
owns composition and its expiry tests; do not edit them. Parent's new queued-guards
fixture separately reproduces later-unsent requests falsely closing global admissions;
parent is holding LA acceptance until Astra handles it. Do not solve that here.

Required correction: make guard provider failure, malformed response and actual
successful no-findings distinguishable. Do not emit a completed all-clear merely
because a promise rejected or a JSON object lacked the findings schema. Preserve
successful programmatic/LLM findings and deliberate trigger/cold-cache behavior with
honest status. Surface failure distinctions in the existing guard report and emitted
observation, not only console text; do not claim persistence/UI coverage unless you
actually verify that path. Use bounded non-payload validation errors, validate findings
shape, and supply the guard phase's required response protocol even with custom domain
instructions. Preserve configured instructions as their own part rather than rewriting
them to carry response protocol. Include this domain's OWN frozen thread understanding
as distinct from configured domain knowledge and shared evidence. Ensure another
domain/thread's private state cannot enter its request.

Be precise about native admission: a provider refusal need not mean a model ran;
a failed local waiter need not mean native work stopped. Use existing structured
lifecycle classification when available and say unknown when it is unavailable.
Do not release, cancel, retry, replay or rebind native work from the guard path. Do
not change deadlines, models, effort, tools, budgets or post-learning/CAS eligibility.
Keep ordinary successful shapes compatible where possible; report any existing
fixture incompatibility rather than weakening protected assertions. Add targeted
checks for valid empty findings, actual findings, invalid schema, provider error,
scoped no-admission, private interpretation and emitted guard outcome visibility.

No production change outside the named modules, no new journal/framework, no review
lifecycle change, installed/candidate/live/account/binding mutation, native model,
browser, commit or process kill. Guard request historical capture and post-review/
cartographer capture remain later LI work unless a minimal shared record is strictly
necessary for this correction; state remaining gaps rather than broadening silently.
Rendered UI QA stays deferred while admission timings are under test.

Run focused guard/librarian/flow/advice-capture and middleware tests, strict types and
diff; return exact source hashes and resource/evidence scope in the handoff. Record
concurrent composition drift rather than declaring a whole-tree freeze. Full native
learning, historical phase coverage, L/E/Herald, both-engine recovery/lineage/tools/
subsessions/artifacts/tags and measured capacity/latency remain required. No count or
release advance.
