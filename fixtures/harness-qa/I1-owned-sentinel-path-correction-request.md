# I1 actual native sentinel path correction

Continue as Astra lead in the existing Foundry/native session. Same full goal,
count six/next checkpoint eight; no full acceptance, rollout or scope reduction.
Your prerequisite task actually completed07:50:00.019Z. Supervisor read the full
new handoff, both prerequisite implementations and opposite review, independently
verified the new candidate's installed seven files/prewrite contract, and reran
full baseline1220 plus128 integration/profile/startup cases. All passed with stable
candidate fingerprint86ab8bd6fffb0058766371e9b87b448e9eb5b8d9dadf18b6343ae2fc7bc706d5.
Those reports are in the new candidate at07-48-29.474Z baseline and07-49-41.052Z G5.

Supervisor then ran the reviewed bounded Claude sentinel sample in the new
candidate .foundry/qa/adoption-e7d4d6c5-2997-4e74-9f8b-70d85d683659/foundry.
Evidence beneath that candidate:
.foundry/qa/i1-supervised-claude-20260907t0750/report.json and state/sessions.sqlite.
Actual run07:50:26.710Z to07:50:53.620Z. First turn native completed/result, local
resolved, HTTP200, persisted trace/message/native journal, one owned Bash begin/
result with output1.3.14 and FOUNDRY_FIRST_SENTINEL. One process/one write;
second admission correctly withheld; actual owned exit143 after known terminal,
cleanupFailures empty and controlled files unchanged. Not native cancellation.
Observed model equals requested claude-fable-5-1; effort acknowledgment unavailable.

The run remains failed. Independent read-only replay of the EXISTING guard against
its selected public journal evidence gives otherOwnershipTerminalChecks=true and
sampleCommandsAllowed=false. Actual command was bun --version && cat followed by
the double-quoted ABSOLUTE path to this exact owned project's sentinel-1.txt.
No other path or command was executed. This is a demonstrated false negative in
the finite command allowlist, not permission to accept arbitrary shell syntax.

Implement a narrowly scoped runner/guard correction and tests. Supply the exact
owned project/sentinel identity to command validation, admitting equivalent safe
quoted absolute spelling of ONLY that sentinel. Preserve approved relative forms,
actual original MCP argv and all existing ownership/terminal/command guards.
Do not use a broad regex for absolute paths or shell normalization that strips
operators/substitutions. Prefer finite exact safe renderings or an established
parser with strict command/argument checks. Reject foreign/prefix-collision paths,
extra commands, redirects, substitutions and ambiguous/omitted arguments. Add a
regression anchored to the observed space-containing path and negative cases.
Keep generated paths shell-safe, including quotes/metacharacters if supported.

Improve report failure diagnostics enough to distinguish command-shape rejection
from native/ownership/persistence/browser failure using non-secret flags, not raw
protocol/reasoning or fabricated support. Preserve the original failed report and
candidate bytes; never rerun/overwrite the same sample ID or reclassify its guard.

Supervisor is separately observing the first MCP sample in that SAME frozen
candidate under i1-supervised-mcp-20260907t0752. Do not touch that candidate or
dispatch any model call. Read its final report/owned public journal only after
finishedAt/terminal, and incorporate any relevant command-shape evidence without
guessing or claiming it passed. Supervisor owns its active process and cleanup.

Run unchanged sample/ownership/profile/actual-class routes and new negatives,
strict types/diff. Append a handoff with source hashes and precise correction.
Prepare a NEW immutable installed candidate with full baseline and relevant
sample gates; no native capture under this request. Supervisor/opposite review
must inspect corrected code and separately authorize new sample IDs. Do not get
stuck in more architecture-only planning.

Fable is currently correcting T2 all-session revocation in MCP source/tests from
your opposite review. No overlapping MCP edits, no T3 activation, no sibling
engine changes unless a separately demonstrated engine defect requires a handoff.
Preserve startup helper and unrelated dirty work. No root dependency install,
server restart, account/config/binding changes, additional agents, publication,
root/sibling/candidate commits or replay of unknown work.
