# Preserve the three knowledge segments through actual runtime assembly

Continue SAME Fable session after actual end_turn12:42:02.036Z, durable final and
entire docs/G4-adapter-and-replay-opposite-review.md read. Parent records your
bounded source/replay-positive review; full recovery/native acceptance remains
pending. Full goal/count7-next8 unchanged. Parent owns replay helper follow-ups.
Astra still owns native runner/checker/recovery tests. Do not edit those files,
native adapters/providers/evidence, guards or immutable candidates.

Parent live browser audit at1440and390, API writes blocked, no native calls:
.foundry/qa/g6-live-inspection-2026-09-07T12-39-59.394Z/report.json and screenshots.
Turn Context for turn_b086ace9 shows generated thread-knowledge:conventions and
thread-knowledge:memory as DOMAIN-KNOWLEDGE contributions. The distinction is
semantic, not cosmetic: the user explicitly requires instructions, domain
knowledge and thread knowledge separately, from their real origin, not names.
Current code messages.ts:121-128 heuristically labels ordinary content blocks;
runtime.ts:512 creates the generated layer without explicit content segment.

Parent new acceptance/generated-knowledge-segment.test.ts uses actual runtime/
factory-created layers and assembled reading view. No model calls.1pass/1fail:
generated PRIVATE_THREAD_FACT is incorrectly domain-knowledge; other-thread
privacy control passes. Strict fixture types pass. Evidence
.foundry/qa/2026-09-07T12-42-11.119Z-5dbbcf02-3238-4159-b6b9-368491e46bda-G6.

Implement explicit semantic metadata through existing ContextLayer definition/
instance, ContextStack assembly, generated DomainLibrarian/runtime construction,
injection snapshots and relevant persistence/clone/restore boundaries. Use a
bounded typed field or established metadata, not prefix matching or relabeling
only the UI. Configured domain cache remains domain knowledge; layer prompt
remains instructions; thread-private generated state remains thread knowledge.
Keep old artifacts immutable; legacy records lacking metadata must not be silently
rewritten or presented as newly verified provenance. Do not change injected
content, provider capacity, selection or privacy merely to correct the segment.
Inspect existing serialization/schema consumers and keep backward compatibility.

Own this disjoint core/domain metadata path and focused tests only. Source edits
outside it require an explicit recorded reason. Add tests for runtime-created and
standalone domain layers, custom IDs where applicable, clone/restore metadata,
historical snapshot immutability, configured-domain control, and other-thread
privacy. Preserve original tests; run parent2 and focused core/domain/decoration/
knowledge-recovery tests, strict types and diff. Opposite review before acceptance.

Also include a short G6 follow-up proposal grounded in the supplied screenshots:
fresh isolated browser context renders100quota warnings for server-durable
messages, current cache persistence writes full100message records; no artifact
links in selected historical context; long raw text wraps awkwardly in narrow
desktop inspector. Do not hide unsaved transient content, omit history or mutate
server records to silence warnings. Propose a bounded durable-server/optional-cache
and actual artifact-navigation approach; do not implement a new frontend storage
architecture in this segment assignment. The first12:38:55observer read raced
the trace render; only12:39:59explicit-wait report supports historical-view claims.

Output docs/G6-generated-segment-handoff.md with scope/hash/results, migration and
historical behavior, remaining inspection/cache/artifact gates. No model/capture
probe, root install, live restart, credential/binding/account change, commit or
count advance. Native learning/continuity, pooling/models/efforts/latency and full
session/lineage/subsession/artifact/tag goals remain required.
