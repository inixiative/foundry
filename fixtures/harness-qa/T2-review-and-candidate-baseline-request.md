# Astra: opposite T2 review and complete candidate baseline prerequisites

Continue in the SAME Astra Foundry/native session after actual I0/I1 completion
07:28:46.476Z, native task01a07a79-b506-7a52-a600-485bfbef9760. Supervisor read your
complete102-line handoff. Fable now independently reviews that immutable candidate
and new sibling/prewrite/sample contract; do not edit its frozen files. Count6-next8
unchanged until independent/opposite acceptance. No model calls or live rollout.

## Opposite T1/T2 source review

Fable's transport/late evidence/record observer corrections actually completed
07:26:49.955Z. Read full docs/T2-live-transport-handoff.md and current MCP source,
including authenticated per-runtime transport, protected launch file/stdin proxy,
read-only scoped authority, lifecycle reservation/revocation/cleanup, late records
after transport close, shared isolated async record observer and bounded retention.
Supervisor58 tests360assertions/types/diff independently pass at
.foundry/qa/2026-09-07T07-29-20.535Z-312ff080-7f9c-407a-9b97-eff25882de6a-G5/report.json,
stableba81469338cc145592e22d68c837ef0afbc54bda8228cbebfa11699abe076ad7.
There are22 independent cases (7+6+4+2+3), not21 as one handoff label says; total58
is correct. Do not silently count known absent native bridge delivery as proven.

Review only MCP files, no edits to them. Read Fable's concrete T3 interface notes
in docs/lead-startup-capability-review.md and prepared
T3-native-bridge-integration-request.md. Capture operation-start lease associations
without fabricating observed native IDs; bridge lifetime must match runtime/native
process, not a new bridge per user message. Per-record journal outcomes stay beside
frozen observations; no dropped late records or silent context/model/tool weakening.
Deliver docs/T2-opposite-integration-review.md with accept/reopen, concrete source
findings and first implementation contract for T3. No more architecture-only phase
after reviewed readiness; next milestone is actual owned native tool execution.

## Fix the demonstrated isolated full-baseline prerequisites

Own only scripts/prepare-native-adoption.ts, narrowly related staging tests,
packages/foundry/tests/bash-shell.test.ts and your candidate/handoff artifacts for
this implementation. No core/provider/runtime semantics or MCP changes while
Fable reviews the frozen candidate. Two concrete failures in the last candidate:

1. bash-shell.test.ts:80 creates BashShell at process.cwd() and expects a Git HEAD.
   Make this Git behavior test own an isolated disposable test repository and real
   HEAD, instead of requiring the checkout under test to have history. Normal
   synthetic commits INSIDE that new test fixture are explicitly allowed; use
   fixed fixture-only identity, disable signing/hooks, preserve assertions and
   clean only that fixture. No commits, ref changes or copied history in root,
   sibling or review candidates. Do not fabricate candidate source provenance or
   skip the Git behavior assertion to make the baseline pass.
2. session-backed-provider.test.ts imports the excluded954-byte selected-terminal
   fixture fixtures/harness-qa/native/claude-limit-terminal.json. Supervisor reviewed
   its complete contents: selected terminal error fields plus provenance only,
   no credentials/prompts/hidden reasoning. Add ONLY that exact immutable fixture
   path to Foundry staging's explicit allowlist; include its hash in the manifest.
   Keep native tapes, arbitrary other native files and recordings excluded, and
   add positive/negative allowlist coverage. Do not alter the fixture or replace
   its real-error assertions with a synthetic success.

Freeze/install a NEW candidate with the current reviewed source and exact native
archive rules, two scripts-disabled installs and explicit Prisma type generation,
then run its full baseline plus package check and risk-focused candidate routes.
Do not mutate previous candidates to appear current. Retain the old RED/stale
evidence. Root still registry/live unchanged. Missing prerequisites are now
specifically authorized to fix, not reasons to keep a permanently partial build.

Supervisor startup-only helper fix has opposite bounded acceptance and now62
startup/profile/adapter/config tests309assertions/types/diff on the SAME stable
ba814693... fingerprint, report
.foundry/qa/2026-09-07T07-26-33.059Z-9470368d-0e6d-40ca-88a4-823f1f0c369e-G5/report.json.
The fifth case covers signal/clear forwarding; composite capability scope is
commented. Explicit strict script/test types pass. Preserve these edits and include
them in the new candidate; no live restart. This is supervisor work, not an Astra
accepted slice to self-count.

Parent independently ran the current candidate's ACTUAL sample setup failure path
for claude and mcp with a missing browser module and Bun.spawn fully refused:
zero subprocess attempts/writes/spawns, clean teardown, unchanged artifacts and
closed guards. No native work. That narrower proof supplements, not replaces,
Fable's actual runner/source readiness review. After both reviews and a complete
fresh candidate baseline, the supervisor should authorize bounded actual samples
promptly, then T3/I2 and full tool-workbench/recovery/lineage/inspection/latency work.

Update docs/I0-I1-native-integration-handoff.md with a distinct correction section
and exact new artifacts, plus your opposite review. No agents, native probes,
root install, credentials/account/binding changes, live restart or publication.
Only isolated test-fixture commits described above are allowed; never root commits.
