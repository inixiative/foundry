# Opposite source review of native learning settlement

Continue SAME Fable/Foundry session after actual end_turn10:18:33.660Z and durable
final. Parent read your full discovery handoff including ordering appendix; its
corrected ordering/provenance/focused gate now passes tests (types completion
recorded separately). Correction to appendix: parent10:15:34 report was46/3 due
to omitted fixture transport state, not47/2; corrected10:16:33 report establishes
the two admission unlocks. Preserve authored history and cite canonical QA.

Read-only opposite review of Astra-owned I2 current source and
docs/I2-guard-review-and-learning-handoff.md. Astra is STILL ACTIVE, finishing an
immutable candidate; this is a source review, not authorization to accept an
unfinished handoff. Do not interrupt, stage, edit production or run models.
If its final handoff arrives during review, read the full append and verify
relevant candidate bytes. If not, label final candidate review pending.

Review runtime/librarian/provider optional inspectOwnedAdmission and
releaseOwnedAdmission, core evidence/types, durable cleanup/learning recovery and
the new narrow inspector state. Require original job/base/owner/admission/pool/cwd,
nonblocking central dispatch, native/local/RPC/cleanup distinction, irrevocable
expiry and exactly-once eligible CAS/publication. No loss of original local error,
foreign reconstruction, guessed free capacity or duplicate admission.

First rerun unchanged independent acceptance/native-learning-eligibility.test.ts.
Parent2pass/2fail10:02:48 stable report is in canonical readiness QA. Valid native
completed before deadline still cannot publish while RPC pending; transient
undefined inspection closes the original eligibility before a valid timely
snapshot arrives. Controls ordinary completion once and post-hard-expiry refusal
pass. Review whether these express the full goal and identify concrete correction
boundaries; do not merely endorse the source's settled-call requirement. A valid
native terminal can establish candidate completeness without proving free RPC or
physical capacity. Those must remain independently represented and tested.

Run focused unchanged independent learning ownership/closed-capacity/durable/input
and native cleanup cases plus new ordinary I2 tests as relevant, strict types and
before/after hashes. Do not turn a moving whole tree into release acceptance.
No need to restage or rerun every old package/baseline: review bounded evidence.
The production MCP adapter still refuses tools:false; controlled injected adapter
tests must not imply actual restricted MCP reviewer support. User-visible native
learning and latest inspector still require actual/browser evidence after source.

Output docs/I2-native-learning-opposite-review.md with ordered findings, exact
file/line/report/hash evidence, independent versus own-authored boundaries, and
source/candidate/actual-native readiness separated. Same full goal/count6-next8.
No root installs, live restarts, accounts/bindings, agents, commits, shared edits
or independent fixture changes. Use apply_patch for the report.
