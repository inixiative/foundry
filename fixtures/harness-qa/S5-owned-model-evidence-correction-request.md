# S5: immutable native model evidence

Continue SAME Fable Foundry/native session after verified end_turn04:04:16.907Z.
Supervisor read Correction3 and your completed binding-provenance response.
Construction correction has12 self-verified independent passes, not acceptance.
Astra completed opposite-model review of the prior revision at04:02:08.130Z;
read `docs/S5-review-and-G3-learning-diagnostic.md` Task A, second Medium finding.

Supervisor independently reproduced it plus an external observer variant with
the actual provider, Claude adapter and installed registry parser over synthetic
streams, no OS processes:
`fixtures/harness-qa/acceptance/native-model-evidence-ownership.test.ts`.
Stable RED `.foundry/qa/2026-09-07T04-02-43.392Z-G5/report.json`, two failures;
standalone strict types pass. Earlier04:02:06 run was stale and the observer case
hit the runner5s timeout; current fixture has a100ms explicit provider deadline
so that failed path closes its fake streams deterministically. Behavior unchanged.

- Caller mutates a returned result's nested raw object into a fake own-binding
  system/init. Second completion reports FABRICATED_CLIENT_MODEL from the mutated
  session history, despite no model/configuration label ever being emitted.
- External session observer mutates the next result object before the provider
  reads it; the installed parser can even lose completion and hit its deadline.

Own narrow provider/adapter evidence boundary plus focused tests and your
dedicated S5 handoff. Preserve current construction provenance and all independent
assertions. Do not edit Astra's active domain/runtime/journal learning code, sibling
engine/recorder, shared ledgers, independent acceptance, dependencies or live state.

Own immutable configuration evidence at the first reliable observation boundary,
and isolate returned nested event/raw data from retained facts and other observers.
Capture/copy each new fact once, not deep-copy all history per event. The installed
engine lacks sibling S1 immutability, so you cannot claim it already protects this.
Keep first-init model unknown when not retained; don't manufacture an ACK from
our request. Preserve matching supported positive envelopes, foreign/unknown
negative cases, native errors, genuine completion, and original observer errors.
Use existing adapter subscription/ownership patterns. A narrow compatibility
boundary is appropriate pending I adoption; no new session engine or dependency
change. Test both engines with controlled transports and caller/observer mutation.

Run all FOUR independent model files (14 cases), focused adapter/provider tests,
strict types and full baseline. Reports must retain stable fingerprints; another
agent's concurrent change is not your edit but still makes the combined report
stale. Do not change assertions for a pass. Give a concise dedicated correction
handoff with exact results and limitations, then await Astra re-review/supervisor
acceptance. No native probes, restarts, bindings/profiles/credentials, commits,
publication or detached agents. Use apply_patch for manual source edits, not shell
heredocs/Python write tricks. Full goal/count5-next6 unchanged. Astra first reviews
your recorder latch read-only, then implements owned causal learning separately.
