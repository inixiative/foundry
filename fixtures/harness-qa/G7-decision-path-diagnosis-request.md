# Diagnose decision latency on the real dependency path

Continue SAME Astra session after actual G6 correction task_complete13:47:42.334Z,
native01a07c19-fc09-7de1-9f5c-287eac39f919, matching durable final and full appended
G6 review read. Parent accepts the bounded G6 configuration correction after its
own46focused tests, original7scratch and your4production route tests rerun. Count
remains EIGHT (G6 is Fable-authored). Your legacy config-test typing caveat remains.

Parent corrected preflight TEST portability only while you reviewed G6. Fable now
opposite-reviews it under turn_d57089db-621d-4447-9e1b-c3d6d9f4a301, not implementing
UI yet. Runner6419617e unchanged; root full baseline/types/diff now pass. Parent
will freeze the one integrated candidate after the test review. Preserve production
source during this assignment: scratch measurements/QA and handoff only, no edits.

Use this slot for the next real slowness issue rather than another candidate plan.
Read G7-live-decision-latency-request.md and its existing timing report, then trace
current classifier/router/middleware/provider/capacity code and actual dependencies.
Existing historical live evidence shows repeated ~30s pre-executor work, two serial
15s decisions with fallback. It does not establish current code timing, true cause
for every fallback or account/quota identity. Do not infer those facts.

Inspect only non-secret current profile fields and owned public completed traces
where needed. Use existing measurement helper for one bounded read-only sample
of a recent completed turn in each preserved work thread. Do not dump configuration,
environment, credentials, native reasoning or unrelated session tapes. No native
CLI probes, model calls, retries, new bindings or actor dispatch are authorized.

Build scratch controlled-provider timing/failure tests through the actual production
decision/runtime path. Establish which dependencies are genuinely serial, what
current cached/direct routing policies exist, whether capacity-unavailable work
still waits again, what fallback metadata is recorded, and which layer preparation
can actually proceed concurrently. Use repeatable short controlled delays, not
30second sleeps. Read configured memory/decorator eligibility and prove no required
instructions/domain/owned committed knowledge disappears in any proposed fast path.
Distinguish controlled timing from actual model latency and unknown native work
from available capacity. Never shorten timeout/model effort or drop tools/context
merely to satisfy a target, and do not label a heuristic as model acknowledgment.

Output docs/G7-decision-path-diagnosis.md with exact source/trace evidence, executable
scratch results, the smallest justified implementation boundary and real matched
benchmark protocol for later authorization. This is concrete diagnosis to enable
the next source change, not permission for production/native activation here.
Full original scope including native recovery/learning, history/artifact UI,
models/efforts/pooling and session lineage/subsessions remains unchanged. No install,
candidate mutation, live restart, account/credential/binding change or commit.
