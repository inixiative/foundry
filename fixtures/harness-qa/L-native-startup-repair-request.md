# P02c: clean real composed startup and honest observability

Continue the full Foundry goal in this existing Astra4407 session. Parent has now
independently accepted bounded LW after one additional parent observer-history
marker correction. Read docs/L-live-watch-parent-acceptance.md and the current
protected parent durable/terminal tests. Source97/530 and separate actual browser
observer55/23 and sender62/12 pass. No installed/native/live adoption is claimed.

You own the next source/test/browser slot; Fable remains spend-limited and parent
will be read-only. P02c is next in CORE-006. Read the complete
docs/L-native-browser-startup-parent-investigation.md and original pilot02 handoff.

Reproduce startup through the ACTUAL domain-loop composition with explicit existing
controlled owned native adapters, its viewer/server/WebSocket and owned Chromium.
Do not substitute the unrelated already-green LW/phase fixtures. Preserve initial
named HTTP/API failure evidence before correction. The old pilot has anonymized
API400s whose exact route names cannot be reconstructed; current source suggests
missing analytics/token dependencies, but reproduce rather than retroactively
claiming those old routes. Diagnose any404 separately.

Make diagnostic labels a finite allowlist of safe static local paths. Unknown
paths stay generic; queries, credentials, headers, bodies, arbitrary URLs and
private thread/project names must not enter diagnostic receipts. Preserve existing
redaction. Add secret/unknown-path controls. Separate connection/render readiness
from a clean startup verdict: a connected page with required API/module failures
must fail clean QA. Test an actual failing response, not just synthetic report data.

Repair real composition dependencies and lifecycle based on the reproduction.
Full harness token/cost/tool observability is required: do not delete analytics
requests, blanket-suppress HTTP errors or fabricate zeros/acknowledgments to pass.
Use normal startup's existing TokenTracker/AnalyticsStore/event wiring and owned
persistence where appropriate. Prove actual controlled usage reaches the UI/store,
survives intended reopen and releases resources, while unavailable native usage
or model-price facts remain explicitly unavailable. Do not add a new reporting
framework or replace the native path with a mock-only viewer.

Retain each attempt in an exclusive directory, exact argv/PID/source/exit evidence,
required desktop/mobile actual PNGs, browser errors and acquired-handle cleanup.
Inspect the screenshots. Run focused composition/startup/redaction/observability
tests, strict/package types and unchanged relevant native/LW/P02a/P02b contracts.
No default timeout increases, old assertion weakening or broad LS retry matrix.
The old combined32/7/4skip remains failed unless its real scope is independently
repaired; a clean startup check is not its substitute.

No native model experiment, package installation, candidate mutation, preserved
server4400/4407 restart, account/model/binding change, commit or unrelated process
signal. Do not retry locked CUA; existing owned Playwright fixtures are authorized.
Maintain every controlled original through its real settlement/cleanup handles.

Write docs/L-native-startup-repair-handoff.md: observed cause versus inference,
source changes and ownership/lifecycle, RED/GREEN evidence, actual requests/rendered
usage and reopen proof, cleanup/final hashes, unresolved failures and next graph
node. Propose the shortest remaining reviewed path to a fresh immutable installed
candidate and actual visible native work, but do not execute adoption or pilot03.
Then finish this original task for independent parent verification. Full goal stays.
