# Implement exact-owned discovery accounting

Continue SAME Fable/Foundry session after actual end_turn10:05:08.405Z and durable
final. Supervisor read entire docs/T3-actual-retrieval-diagnosis.md. Same full
goal/count6-next8. Both actual captures stay FAILED and immutable. No new models.

Own ONLY scripts/native-retrieval-guard.ts and its focused guard tests, plus your
handoff. Astra still owns I2 runtime/provider/core types and subsequent sibling
integration. No edits to core native types, provider projection, session adapter,
sibling, runner, frozen candidates or independent fixtures in this assignment.

Implement an explicit, inspectable discovery category with exact call/result
accounting. Do not broadly ignore ToolSearch, filter arbitrary native calls or
relax the15 independent provenance cases. The expected tool is derived from the
validated original bridge identity, never supplied by the discovery call.
Exact ToolSearch input contains only query:select:<owned foundry_memory tool>.
Require original owner/admission/call identity, one begin/one result, no tool error,
no duplicate/unmatched results, and discovery before its dependent memory calls.
At most one discovery is a bounded sample rule, NOT a production tool restriction.
SDK receipts remain bijectively joined only to actual memory calls.

Correction to the diagnosis proposal: a block-type label alone cannot establish
which tool references were returned. Define the narrow expected optional public
projection contract locally in the guard, without editing shared types yet:
toolReferences?: readonly string[] on tool_result. Require a nonempty list naming
ONLY the exact owned expected tool, no duplicates. No unrelated text, receipt,
error or unknown omitted payload can count as valid discovery. Coordinate any
additional omission/type metadata needed explicitly in your handoff. An old empty
string without observed typed references remains insufficient, not retroactively
accepted. Known projected references need not expose raw envelopes or private data.
The sibling/core/provider projection will be a separately owned integration step
after Astra's current work; do not claim that path exists yet or fabricate capture.

RED-to-GREEN focused matrix: exact owned reference positive, missing/empty/foreign
reference, wrong server/tool, free-text or multi-select query, extra input keys,
duplicate begin/result/reference/discovery, failed discovery, receipt/text payload,
late/foreign owner or admission, discovery after dependent tool, unaccounted Bash,
discovery without memory operations. Existing15 independent provenance cases and
sentinel policy remain unchanged and must still pass. Report explicit diagnostics
for discovery and memory accounting rather than silently changing counts.

Output docs/T3-owned-discovery-handoff.md with exact test reports/source hashes,
public projection contract and implementation limits. No self-acceptance/staging,
root install, live restart, account/config/binding changes, agents or commits.
Use apply_patch for manual edits. Broader MCP per-call config diagnosis is retained
for Astra's subsequent integration, not part of your owned files now. The claim
that every future Claude turn necessarily uses ToolSearch is too strong: discovery
depends on deferred tool state. Account for observed discovery when present.
