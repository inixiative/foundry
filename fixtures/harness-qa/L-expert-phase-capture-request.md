# Capture and inspect historical expert advice inputs

Your opposite review d52d4085 ended authoritatively: native end_turn08:18:15.004Z,
durable1788855495055 and empty buffer. Parent read the FULL final handoff. Astra's
08:18:56 public update confirms it observed that terminal and has started the separate
local-expiry admission regression. Do not wait on or interrupt that task.

Continue the full CORE-006 goal. You own the LI advice-phase input capture increment;
Astra owns ONLY scripts/domain-loop-composition.ts plus its new admission test/handoff.
Parent owns independent fixtures, graph/ledger and final integrated verification.
No source edit to composition/providers/native engines/factory/runtime/journal schema,
protected tests/checkers, candidates, captured runs, installed packages or live state.

Parent added protected packages/foundry/tests/expert-phase-provenance.test.ts. This
uses the real FlowOrchestrator/DomainLibrarian, captures the actual controlled provider
request, derives the protocol FROM THAT request, serializes plan/decoration, changes
the warm cache, verifies the three historical parts remain exact, then looks for the
protocol anywhere in the historical record. RED0pass/1fail/6assertions on four stable
hashes at .foundry/qa/parent-expert-phase-provenance-Iv3TDc/report.json; strict types pass.
Leave its assertions and bytes unchanged. This is a missing historical record, not
an invitation to synthesize old requests from today's ADVICE_RESPONSE_PROTOCOL.

Implement narrowly in packages/foundry/src/agents/domain-librarian.ts,
packages/foundry/src/agents/flow-orchestrator.ts, the existing core message types only
if required, viewer/ui/inspector-data.js and detail-drawer.js, new focused author tests,
and docs/L-expert-phase-capture-handoff.md. Preserve the three owned segments unchanged.
Use the existing plan/decoration/durable dispatch artifact and existing inspector;
no fourth domain-memory layer, parallel journal, global last-request cache or new
middleware framework. Prefer an invocation-scoped observation of the actual input
supplied to the provider over re-running a formatter later. A shared mutable last-input
field would misattribute concurrent calls and is not acceptable.

Retain the exact historical advice messages including protocol and wrapper/common
evidence, attributable to that participant/turn and frozen at the actual boundary.
Capture it even when the call errors, times out or later settles; a late result cannot
rewrite the sealed delivered record. Cold/not-started/excluded cases must not acquire
invented request evidence. Preserve requested/prepared/local outcome versus native
admission/receipt distinctions: recording provider input is NOT proof the native model
received it. Never serialize credentials, opaque transport/session functions or
unrelated experts' private state. If optional metadata is useful, keep it bounded and
explicitly requested, not falsely observed. Keep raw request evidence out of the
central agent's injected prose: it belongs in inspection metadata, not duplicated
domain advice or instructions.

Expose the recorded advice request in the existing historical expert inspector,
beside (not replacing or merging) its three owned parts. Older records lacking it
must say not recorded, never borrow current source/cache/protocol. Keep the current
state view distinct from selected-turn state. Persist/serialize the evidence through
the existing artifact path; test reload and old-record absence, pending/late/error,
concurrent expert attribution, exact equality with captured provider input and no
new central context injection. Preserve fresh domain interpretations from the parent's
protocol sentence; do not revert to verbatim-excerpt-only advice.

This increment is advice capture, not acceptance of every phase: inspect and identify
the corresponding post-review/guard protocol capture gaps explicitly in the handoff,
without broadening into those lifecycles or claiming the full LI node is complete.
Do not alter model/effort/tools/deadlines/capacity policies. No actual native model
pilot, installation, accounts, bindings, restart, commit or process kill. Browser
execution is deferred while Astra measures the admission tests; do controlled UI-data
and persistence assertions now, state that rendered desktop/mobile QA is still needed.
Parent will coordinate that after both source owners are terminal. Ordinary focused
source tests are allowed; record scoped hashes and later drift, and do not present
concurrent-source results as a final integrated freeze.

Review note: parent confirmed that the failing fixture parents were born07:58:29/34,
project-b and journals only07:58:42. However copied project-a files preserve source
birth/mtime from September7. Treat the cp/mkdir setup interval as evidence pointing
strongly to I/O, not direct awaited-boundary instrumentation or a proven filesystem
mechanism. 'Row-for-row identical' should be qualified as comparable behavior unless
canonicalized row comparisons actually support it. Three reruns pass54/4skips with
stable scoped fingerprints; prior failure remains open. Parent96test gate had8files,
not9. Do not repeat stronger causal claims than these observations warrant.

Finish a concise handoff with exact source hashes, unchanged parent RED-to-GREEN,
focused runtime/decoration/inspector/persistence tests, strict types, resource state,
legacy/pending semantics and remaining browser/post/guard gaps. Full L/E/Herald,
native learning, both-engine recovery/lineage/subsessions/artifacts/tags and measured
fidelity/latency remain required. No release acceptance or count advance.
