# Continue leading: recorder verdict, then owned causal learning

Continue in the SAME Astra Foundry/native session after verified
task_complete04:02:08.130Z. Supervisor read your complete
`docs/S5-review-and-G3-learning-diagnostic.md`. Count5/next6 and the entire
CORE-002/003/004/005/AS-001..005 goal remain unchanged. Implement meaningful
causal learning now; preserve the native-integration path, not just source checks.

Ownership: Fable is actively correcting provider/adapter construction provenance
in `S5-authoritative-binding-correction-request.md`. Supervisor has also reproduced
your mutable-event finding plus an observer variant in independent
`native-model-evidence-ownership.test.ts`: two failures, stable report
`.foundry/qa/2026-09-07T04-02-43.392Z-G5/report.json`. Fable will receive that
follow-up after its actual terminal. Do not edit providers/session-backed.ts,
session-adapter.ts, their tests, sibling source/recorder, independent fixtures or
shared ledgers. Provider changes are not yet accepted or deployed.

## Task A: finish the opposite-model recorder review first

Read sibling `docs/S1-continuation-ordering-correction.md`, including Correction2,
actual scripts/s0/continuation.ts/continuation-record.ts/continuation-plan.ts and
lifecycle allowlist, focused tests and the unchanged independent ordering file.
Supervisor all17 native cases across five files pass with stable cross-repository
evidence: sibling `.qa/s0-cross-2026-09-07T03-53-31.502Z/report.json`, Foundry
`.foundry/qa/2026-09-07T03-53-31.517Z-G5/report.json`. The runner now latches closed
before any post-deadline await, late completion permits cleanup only, pin1.3.14
is explicit, and unknown occupancy stays attached. Parent inspected exact runner
and both plan/execute commands; no capture has been authorized or run.

Give a bounded accept/reopen verdict, source evidence and any exact blockers.
Record this completed Task A verdict in a dedicated handoff and public progress
message BEFORE moving into Task B, so supervisor can independently decide whether
to authorize the disposable captures while you implement learning. Do not edit or
capture the recorder yourself here. Do not self-approve your earlier preparation;
review Fable's corrective boundaries and all prerequisites honestly.

## Task B: implement the owned learning lifecycle you proposed

Use your diagnostic as the architecture starting point. Own existing domain
reviewer, thread-runtime, knowledge-persistence/local-session-store interfaces,
related core knowledge/layer interfaces and focused tests as needed. Add narrow
role/phase settings/resolution and startup/factory wiring only if essential, using
existing patterns and without touching Fable's provider interface. Keep a concise
dedicated handoff with decisions, evidence, dependencies and remaining gaps.

Required behavior, not just a plan:

- Freeze identified review input with owner/project/thread, runtime generation,
  originating evidence/message, base knowledge revision/hash and explicit three
  parts: reviewer instructions, configured domain knowledge, COMPLETE bounded
  committed thread knowledge. Do not silently truncate half the knowledge being
  replaced. Keep evidence truncation explicit and mandatory knowledge intact.
- Separate background review identity/occupancy from interactive advice/guard;
  preserve existing bindings as history. Keep one owned review per domain/phase
  and retain queued evidence. Do not silently choose a weaker central model.
  Resolve explicit review options faithfully; expose requested-but-unenforced
  native token/effort budgets rather than claiming support the adapter lacks.
- Soft observation expiry means pending/delayed, not permanent discard. A valid
  result before the bounded hard eligibility deadline may commit if ownership and
  base are still current. Hard expiry is an irreversible latch. A late terminal
  can reconcile occupancy/evidence, never reopen commit or launch replacement work.
  Use explicit timeout0 only on the owned background completion if needed to keep
  the native result observable; never release its lease on local observation
  timeout. I/S1 integration is still required for full native safety claims.
- Build and validate a candidate without changing published knowledge. Use the
  existing journal for an atomic compare-and-set against owner/generation/base
  revision/hash and identified evidence, then publish only committed state.
  Stale, duplicate, expired, disposed, foreign or invalid results must have honest
  outcomes. Write failure preserves previous revision and completed executor
  output. Durable success followed by publication failure is reconciliation-needed,
  not falsely unsaved and never an excuse to replay central/native work.
- No added serial central wait. Prepare from latest committed state and expose
  pending review honestly. Immediate next requests can use the older revision;
  first request after commit automatically receives the new fact without the user
  repeating it. Historical prepared/delivered artifacts remain immutable.
- Parse unknown JSON safely (null/array/scalar/malformed), bound full response,
  facts/reason and knowledge, and reject over-cap knowledge without clipping or
  an automatic repair/model retry. Preserve application4000-char cap by default.

Tests must exercise production runtime/journal with controlled reviewers: a new
Zephyr rollback fact absent from subsequent prompts, review beyond soft boundary
but within hard deadline, exact fact in next-turn input and journal reconstruction;
immediate request does not wait for review; complete committed knowledge tail and
all three segments supplied; unrelated domain/thread isolation; stale restore,
disposal, hard-deadline/terminal boundary race, duplicate/foreign results, one
review admission while unknown, queued evidence, malformed/oversized output,
real SQL-trigger commit failure and durable-success/publication-failure distinction.
Use short configurable deterministic deadlines in tests, not long sleeps or new
model probes. Do not weaken existing regressions to make changed behavior green;
when intentionally changing timeout semantics, retain the old safety invariant and
explain the new observable outcome.

Run focused tests, full baseline/types/diff and all eight unchanged memory files
(26 cases); model tests are independently changing under Fable ownership, so
coordinate via reports rather than editing them. Stable source fingerprints are
required for acceptance; concurrent changes make a report stale. Supervisor owns
additional independent SQL/browser/native acceptance. No self-acceptance, commits,
dependencies, publication, restarts, live configuration/binding/credential changes,
new agents or native probes. Use apply_patch for manual edits.

After the bounded implementation, propose the next ready I/T/real sample step and
rollout conditions. Full goal includes live observer synchronization (old browser
tab lagged behind server journal), native events/tools/cancel/usage/lineage/
subsessions, artifacts/attachments/tags/right-panel inspection, model effort/
capacity and subscription-independent continuity. Source learning success alone
does not close those requirements or prove zero native latency overhead.
