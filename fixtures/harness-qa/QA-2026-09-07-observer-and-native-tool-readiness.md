# Independent observer QA and native-tool readiness,2026-09-07

Previous goal turn: progress, not a wait. It verified cleanup/learning source,
dispatched R1 on an actually idle preserved binding and updated supervision. This
turn adds independent browser/combined evidence, two executable native acceptance
boundaries and opposite-model review dispatches. Full goal/count5/next6 remains;
no source-only acceptance is deployment or native parity.

## Verified terminal and current assignments

Astra R1 actual task_complete05:48:14.271Z, native
01a07a5e-c074-7550-93bc-7d72fb22598c. Supervisor read the full
docs/R1-configuration-collection-handoff.md. The production change is both frozen
detached observedConfiguration getters plus their contract comment. No lifecycle
or provider completion rewrite; ten focused both-engine ownership cases added.
Positive later envelopes in those new tests are explicitly synthetic engine-event
injections, not proof that registry0.1.0 emits configuration. Its75-case report is
stable internally, but later QA docs changed the whole-tree fingerprint.

Fable UI ordering actual end_turn05:45:05.517Z. Supervisor read the complete
QA-2026-09-07-fable-observer-inspection-handoff.md correction, including all3 actual
store/browser cases and40-case stable run. Both completed implementation bindings
were preserved; no native work was replayed.

Current read-only opposite reviews, both accepted HTTP200/matching start:

- Fable G3-R1-fable-rereview-request.md, Foundry
  turn_b7f43564-c437-4f0e-b64a-888de226c5d8, same native
  da000690-899a-4128-a868-b9a024aeeaa0. Actual public tool05:51:30.187Z.
  Reviews learning correction and R1 separately; may write only its review handoff
  QA-2026-09-07-fable-learning-R1-rereview.md. Conditional count6 scope/leadership
  checkpoint recommendation, not self-acceptance or UI edits.
- Astra G4-astra-observer-review-request.md, Foundry
  turn_b93a6060-15aa-4574-8c5f-81853066d861, native
  01a07a6b-6ee5-7ee3-9d13-ee6022acf139, actual start05:50:56.242Z. Same lead native
  01a07901-d93d-7903-9c9d-09e5e7136573. Reviews Fable observer/history/knowledge
  changes including the newly observed timeout. Only docs/G4-observer-inspection-
  review.md may be written. No source/test changes authorized by this review.

Both native processes43025/95751 were verified live after dispatch. Await actual
terminal/handoff; do not duplicate on empty buffers or observation timeout. Fable's
earlier native error_max_turns is not a model-quality score or unknown observation.

## Independent checks

1. All33 independent learning/model/cleanup cases passed with types/diff:
   .foundry/qa/2026-09-07T05-43-48.458Z-0c6c5d85-608d-436c-9149-f5efd0c7e1cf-G3/report.json.
   Exact scope:16 learning across FIVE acceptance files (including acceptance/
   domain-learning, not the package file),2 cleanup and15 model cases. Before/after
   hash07e8e8cf3450d4dc2864d5332565e0154701ba426bf6f33950d950beac3e0eb8.
   This corrects prior31-case accounting and closes the source-stale16+2 rerun gap.
   No fixture changes or expected-failure relabeling produced the pass.
2. Actual-browser ordering passes on FINAL store hash
   c2ba8d2dbb149a709d255598ded87c986456f05391c1e88db16624d4e1d04988:
   .foundry/qa/knowledge-observer-order/2026-09-07T05-45-31.866Z-124d6671-f08e-4c5d-b6f8-2a82d6bc7143-G4/report.json.
   Revision2 remains after older200, exactly one old/new request, no browser error,
   source stable. Earlier parent05:39:06 also passed but preceded no-store change.
   Screenshot after-older-response.png was inspected on that earlier revision;
   final standalone fixture verifies final visible text/state too.
3. Original8 independent browser checks pass:
   .foundry/qa/owned-learning-visual/2026-09-07T05-39-48.493Z/report.json.
   Three owned WS events; external work appears without reload; historical pending
   and zero wait; old/new prepared inputs at333/390/1440 with no overflow/errors.
   Fable separately reran the unchanged original8 after no-store at05:42:24.087Z.
   These are controlled runtime/HTTP/SQLite/browser tests, not native performance.
4. Parent independent nine-file UI rerun:
   .foundry/qa/2026-09-07T05-46-03.089Z-d93c3f31-eded-4e2c-92d4-af7edb09dd55-G4/report.json.
   **39 pass/1 timeout**,170159ms total; historical pending/current-knowledge test
   hit its120s limit. All3 new ordering browser cases pass. Combined source is
   stale due concurrent QA additions; this is neither a release pass nor proof
   that source movement caused the timeout. Astra review must triage actual awaits,
   fixture cleanup and application behavior, not increase a deadline blindly.
   Failed fixture produced no final observer-inspection report; the parent runner
   did record failure. Test process exited. A subsequent process inventory found
   no remaining standard Chrome test-profile root; no unknown process was killed.
5. Explicit strictES2023 types passed for native-tool-scope and both independent
   browser scripts. New native-turn-budget strict compilation also passed.

## Native acceptance now required for implementation

Actual SDK scope suite now7 cases:2 pass/5 fail at
.foundry/qa/2026-09-07T05-41-56.847Z-de649dae-8d03-48c7-916a-101d833d9e8d-G5/report.json.
New independent case registers a replacement thread object with the same ID while
the old object remains undisposed. Its MCP authority still returns old private
facts. Four existing foreign/unprojected/disposed/reassigned failures remain.
Fixture teardown now disposes all owned thread objects as well as both SDK ends.
No native availability/auth or real private disclosure is claimed from synthetic
markers and in-memory SDK calls. T1-live-scoped-tools-request.md is prepared,
NOT dispatched, including actual scoped retained-memory search/get and generation
revocation. It is not just a safer query denial path.

New native-turn-budget.test.ts:2 pass/1 fail at
.foundry/qa/2026-09-07T05-49-01.014Z-5f69b0fa-14c5-4dda-b00c-7b703567eda0-G5/report.json.
Actual installed session class via production adapter/provider emits
--max-turns25 for central work with no operator cap. Explicit48 reaches native
argv; auxiliary1 and tool restrictions pass. Only controlled streams/argv capture,
no OS model process. This is an executable launch-policy defect matching actual
earlier native cap interruptions, not a live25-tool benchmark. Central unbounded
policy must be explicit/native-supported, never another hidden larger limit.

I0-I1-native-integration-request.md is prepared, NOT dispatched. After opposite
reviews, assign this concrete package/outcome implementation to Astra and disjoint
T1 to Fable. The I delivery includes frozen allowlisted package staging, isolated
candidate/frozen-lock installation and actual package resolution, typed owned
outcomes/events through runtime/journal/UI, no hidden central cap, and a disposable
two-engine Foundry sample runner. Native execution and primary dependency/server
rollout require separately inspected bounded dispatch; no installation occurred.
T2 actual native bridge and I2 learning reconciliation follow, not indefinite
additional planning or packaging-only success. Read the prepared request before
using it; authority is activated only by a later explicit supervisor dispatch.

## Visible work and remaining goal

CUA refreshed existing lead tab17 without touching the native process. It now
visibly contains the learning return and R1 assignment instead of stopping at the
old04:38 review. Scrolled to latest, marked handoff, reset temporary viewport and
requested visible browser. This is an explicit observer reload, not proof that
the old loaded frontend auto-updated. Current per-tool native progress is still
not bridged; the next actual terminal should test no-reload reconciliation after
the refreshed frontend. Preserve user drafts/history and both real bindings.

No commits, publication, native probes, credential/account/binding changes or
primary/lead restart occurred. Foundry still resolves registry agent-session0.1.0;
current source edits are not backend deployment. Full native cancel/events/usage/
retention, model effort/no-loss transitions, parallel three-part causal decoration,
scoped retrieval, attachments/artifacts/tags/right-panel, resume/fork/rewind/
subsessions/lineage and subscription-independent continuity remain required.
Account AS-001..005 identity/lease isolation and truthful unknown capacity remain;
do not copy credentials or silently rebind/replay unknown work. Release needs real
sample workflows on both engines and measured capability/latency evidence.
