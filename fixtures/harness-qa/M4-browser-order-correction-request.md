# Diagnose and correct browser-to-lifecycle test interference

Your E1 review turn ended authoritatively (native end_turn07:02:39.543Z, matching durable1788850959628, empty buffer). Parent read the full review and is assigning F1 to Astra in disjoint configuration files. Continue the unchanged full goal/CORE-006. You own the isolated Chrome slot now; parent will not launch Chrome during this task.

Read docs/M4-owned-driver-integration-parent-review.md and its retained reports. Parent real checker06:57:11-e0ff0994 ran all normal/fault Chrome scenarios and new driver safety controls successfully, but R1/R2 in m4c-runner-review.test.ts each timed out at5s AFTER the actual normal browser test in the same process. All45 fingerprints stable. R1/R2 alone unchanged pass2/42 at06:59:01-b838ad7e. Reduced exact browser-then-R1/R2 sequence in parent-browser-review-order-A1GEXF also fails on6 stable hashes: actualChrome passes, R1 timeout5s, R2 final runtime1548ms vs required<1500. Do not infer a common cause or CPU load from this evidence. Parent also verified actual saved artifacts in2Mm6pp through unchanged copy-only verifier and viewed historical mobile inspector.

Investigate the exact stalled boundary and ownership/lifetime cause. Add bounded public progress if needed; no hidden reasoning or unrelated native tapes. Reproduce the same order with existing assertions unchanged, compare no-browser control and inspect owned resources/awaits. Do not raise deadlines, suppress/skip tests, split processes as a claimed fix, or erase retained failures. A proven external/runtime incompatibility may be isolated explicitly, but give causal evidence and preserve the real combined failure as open unless actually fixed.

Allowed narrow source ownership: scripts/native-domain-loop.ts, scripts/owned-playwright-driver.ts, scripts/owned-browser-host.mjs, scripts/owned-browser.ts, their focused tests and scripts/check-native-domain-loop.ts only if the evidenced correction requires it; docs/M4-browser-order-handoff.md. Existing parent acceptance fixtures, m4c-runner-review.test.ts assertions, check-native-loop-artifacts.ts, all E1 config/factory/runtime/startup/routes files, native providers/journal, tickets and live state remain protected. Add new focused tests rather than weaken them. First checkpoint should state where the original hangs and the minimal proposed source boundary. No native model calls, installs, candidates, server/account/binding/shared-browser changes or commits. Never kill a native work process. Exact owned browser cleanup only; record unconfirmed outcomes as unconfirmed.

This is preparation for installed native L, not a substitute for it. Aim to finish the blocker and return exact source hashes, stable combined gate, opposite-review-ready handoff and resource outcomes. If no source correction is justified, report the precise evidence and remaining blocker without modifying unrelated code or claiming a pass.
