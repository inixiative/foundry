# Lead review and next implementation design: native engine integration

Your owned-learning task completed at 04:35:30.941Z and is entering Fable review. Count stays 5, checkpoint 6. Preserve the full CORE-002/003/004/005 and AS-001..005 goal. This assignment is read-only source review plus a concrete implementation handoff. Do not edit production/tests/shared ledgers, install dependencies, restart servers, probe models, publish or launch agents.

First review Fable's completed sibling compaction correction in ../agent-session/docs/S1-compaction-evidence-correction.md and source. Repeated init is not compaction evidence; explicit boundary is synthetic-only positive evidence. Independently inspect the frozen real two-admission recordings and existing negative cases without rewriting artifacts. Fable reports 21 independent native cases across seven files and 178 sibling tests. Confirm any bounded acceptance and remaining real-protocol gaps. No additional capture is authorized.

Also re-review Fable's latest Foundry provider corrections: authoritative construction binding and immutable owned model/configuration evidence, described in QA-2026-09-07-fable-native-model-profile.md Corrections 3 and 4. Your prior findings were reproduced and corrected; all 14 independent model/profile/binding/evidence cases pass. Requested model is not native acknowledgment; installed registry drops some configuration evidence. Verify source and tests, including event mutation, first subscription timing, resume provenance and legacy behavior. Do not adopt source until supervisor accepts the reviews.

Then design executable I and T integration slices against the current repositories. Foundry still uses registry agent-session 0.1.0, while reviewed sibling identity/terminal behavior is not in its runtime. Read new preregistered native-adoption-boundary.test.ts (typed native failure and identity metadata lost: 2 failures, 1 positive) and actual SDK native-tool-scope.test.ts (cross-project metadata disclosure: 1 failure, 1 positive). The typed raw admission/native/local/terminal outcome contract is additive, not license to call unknown work completed. Extend the failure/cancellation/late-result/journal/provider acceptance matrix explicitly.

Propose a reviewed local dependency mechanism with exact files/commands, reproducibility, package exports/types/runtime compatibility, independent test evidence and rollback preserving all new journal records and bindings. Publication or invented version changes are not required. For T, CLI currently constructs a separate thread: adding IDs does not make it authoritative. Specify actual callable tools bound to the owning live runtime, scoped memory retrieval, project isolation, tool-call/dispatch provenance, transport authentication/lifetime and native launch configuration verified in source. Prefer existing components. Do not remove mandatory-context protection on the strength of a tool name in the prompt.

Include how the owned-learning unknown lease can reconcile from native evidence without replaying possibly running work, how events reach the visible inspector and how to verify no meaningful context/tool/effort loss or serial overhead. Separate safe rollout prerequisites from the finished capability so conservative guards do not become the product. First slices need real in-Foundry work, both-engine smoke and sample projects, not indefinitely more mocked checks.

Write docs/I-native-integration-review-and-plan.md with findings, bounded verdicts and a dependency graph ready for supervisor dispatch. Run checks sequentially to avoid millisecond report-directory collision, record stable hashes and scope. Keep live lead/primary bindings untouched. Return actual terminal summary; no self-acceptance of the learning slice.
