# Prepared: make the proven domain loop work in the live harness

READ-ONLY PREPARATION is now authorized within Astra's8991b702 continuation after
its successful-review audit correction checkpoint, while parent independently
reviews M0 artifacts. ce6da7f7 completed15:11:31.409Z; full handoff read. No separate
dispatch of this file is needed while8991b702 is active. This is not activation
authorization; all installs/restarts/candidates/native captures still need review.
CORE-006 now places verified live local-expert learning before Herald integration.

Parent independently ran scripts/check-live-viewer-observer.ts (read-only isolated
Chromium, no existing profile/storage, no POST/native process/restart) against both
live servers. Report .foundry/qa/live-observer/2026-09-07T15-01-43.693Z/report.json
and4screens were read/viewed. Fresh1440/390 pages show current user admissions and
no horizontal overflow/page exceptions. /history404 falls back to /api/messages200.
Existing in-app tab14 instead showed history ending at the old02:45Z assignment;
cause of the stale view is unproven, and browser-only cache was not touched.

Both live /api/threads/:id/knowledge responses contain only {status,snapshot,history},
without newer learning metadata. New ROOT UI incorrectly labels absence "no owned
runtime"; Fable owns the prepared correction (now assigned with cache evidence work).
Do not infer runtime absence from a legacy response. Source/UI are served from the
working tree but long-running backend code is older.

Actual last6 retained learning decisions on lead4407 are all timeout, reason
"no review within 10000ms", most recent storedAt1788792566523/6528 after your G5
completion. No durable knowledge snapshot exists there. Primary4400 has only
revision1 memory/conventions snapshots updated1788734232826/4088 (previous evening).
Recent primary decisions rejected the two capped turns, then timed out after its
successful index correction at1788793389209/9212. These are actual persisted old
runtime outcomes, NOT proof of what the corrected source will do or why the native
review provider failed to finish.

First deliver a bounded executable activation/checkpoint plan, not more disconnected
fixture work: identify exact source/native package dependencies required by M0-M4,
existing accepted corrections and remaining genuine blockers (including G5 SDK
closure/exit137 evidence). Separate failures of diagnostic fixtures from unproven
production readiness without erasing either. Specify what must be observed/tested
before a reviewed native package/server activation, and the smallest safe first
live loop scenario with exact owner/admission/evidence/commit/next-input checks.

Preserve existing journals, knowledge, native bindings, all native process outcomes
and browser-only rows. Only activate when owned work is terminal and existing
evidence is backed up; never clear a binding, replay unknown work, or silently
replace sessions. Cross-version UI/API capability reporting must be explicit. Keep
full model/effort/context/tools and measure overhead separately from native latency.
No blanket serial post-review barrier or automatic retries. A local10s observation
deadline is not native termination or permission to start another review.

Document docs/M4-live-domain-loop-adoption-handoff.md with exact commands, prerequisites,
rollback and success/failure artifacts. This request is READ-ONLY preparation, not
authorization to install, create a new candidate, restart4400/4407 or capture native
samples. Parent will authorize the bounded activation after review. Keep Fable's
UI/history/cache files read-only. No unrelated SDK/platform expansion or pooling.
