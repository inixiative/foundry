# Wire saved configured experts into the production thread runtime

Your9adab6e2 is terminal (native task_complete2026-09-07T21:24:13.066Z,
matching durable final1788816253088 and empty buffers revalidated September8).
Parent read your full final browser integration handoff, verified source122/1298,
and independently inspected copied-journal and mobile/disconnected artifacts.
That remains bounded source evidence, not installed/native acceptance.

Fable owns the browser driver repair in active same-session turn1a6e076a after a
confirmed terminal DNS error on70426df0. Its files are exclusively
scripts/owned-playwright-driver.ts, scripts/owned-browser-host.mjs,
packages/foundry/tests/owned-playwright-driver.test.ts and its handoff. Do not edit
these or launch Chrome while it works. The parent handles driver verification.

New disjoint product source task: E1 configured expert registration. The user
clarified that each expert owns instructions, domain knowledge and private
per-thread interpretation. Shared thread evidence is not a universal expert;
Herald explicitly publishes eligible cross-thread knowledge later. CORE-006
already specifies this. Do not introduce another middleware/journal framework.

Parent reproduced a real gap using ConfigStore.save/load -> buildLayers ->
buildAgents -> production ThreadFactory/ThreadRuntimeManager, without manually
supplying deps.domains:
fixtures/harness-qa/acceptance/configured-domain-registration.test.ts
.foundry/qa/2026-09-08T06-11-34.073Z-d6149610-0697-408e-8a05-b414878ea28a-G3/report.json
Stable RED0pass/1fail/1assertion: configured compatibility expert produces an EMPTY
domainLibrarians map. Strict types PASS. Aggregate4b1e3f97d6e4441ffb023a471f31713081f55a497326207aa537c7930f8cbc4d.
The test's later assertions require actual controlled pre/post learning and next
Continue consumption, plus no initial knowledge in an unrelated thread. They have
not yet executed because registration fails. Parent owns this fixture unchanged.

Current source: LayerSettingsConfig.domain promises domain discovery, agent
flowRole/domain/ownedLayers describe explicit expert ownership, but buildLayers
does not convey it to runtime, runtime defaults to five hard-coded domains and
start.ts explicitly supplies that default list. Saved custom experts are inert.

First record a concise ownership/resolution contract in docs/E1-configured-expert-handoff.md,
then implement it through existing config/build/runtime/startup APIs. Prefer
explicit enabled domain-advising agent ownership and configured domain layers;
do not turn every passive or generated layer into an extra model agent. Preserve
documented legacy built-in behavior when metadata is absent. Explicit disablement
or ownership conflicts must not silently resurrect a built-in expert. Reject
ambiguous duplicate writers/domain mappings before partial runtime attachment.
Keep configured layer IDs distinct from domain names (the parent fixture uses
compatibility-reference versus compatibility). Preserve isolated generated
thread-knowledge and immutable historical message inputs.

Honor configured instructions and model/provider selection through existing
phase-provider facilities, or explicitly reject unsupported configurations rather
than silently substituting another model/profile. Retain text-only auxiliary
roles and central tools/capacity/effort unchanged. Preserve the validated docs
prompt as an explicit legacy default, not a reason startup discards custom experts.
Use actual resolved project configuration; project-only/override/disabled definitions
must not affect another project's registrations. Read current resolver and factory
ownership before choosing the smallest coherent wiring; report unavoidable scope
dependencies rather than creating a parallel factory.

You own narrowly needed config/config-resolve, thread-factory/thread-runtime,
startup wiring (or an existing import-safe startup helper), their focused tests,
and docs/E1-configured-expert-handoff.md. A small dedicated resolver module is
allowed only if it materially clarifies the existing config boundary. Preserve
all dirty changes. Do not edit driver/helper/runner/composition/checker, native
transport, journal, UI, parent fixtures, tickets or old artifacts. Use apply_patch
for manual edits. No other background agent or request to Fable.

Verify parent fixture unchanged; add negative disabled/passive/ambiguous cases,
project resolution and fresh factory after saved-config reload. Existing per-thread
learning, ownership, lifecycle and provider boundary tests must remain intact.
No manual knowledge seeding to make the next-message assertion pass. Retain exact
commands, RED/GREEN, source hashes and remaining lifecycle gaps in final handoff.

This advances source preparation in parallel, not a change to acceptance order:
L native learning still precedes E live viewer/config mutation/restart/stale-review
acceptance. Hot-edit reconciliation and viewer controls must remain explicit next
E work unless already correctly handled by existing production paths. Do not
claim E or the full goal complete from registration tests alone. Native pilot,
installed candidate and preserved live adoption remain pending. No install,
candidate/model sample, live server/state/account/binding/shared-browser change
or commit. Work visibly inside this existing Foundry session.
