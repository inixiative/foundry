# Make actual running work visible in Foundry

Continue the full Foundry goal in this existing Astra4407 session. Parent has
independently accepted bounded P02b: read docs/L-pool-occupancy-parent-acceptance.md.
You own the next source/test/browser slot; Fable is unavailable on spend limit.
Read docs/L-live-watch-parent-investigation.md and CORE-006. The user explicitly
reported that the work site showed nothing; fixing real watchability is now next,
before P02c startup and the remaining native-loop gates, not instead of them.

Reproduce and repair the production live-watch path end to end. Existing owned
native text forwarding exists in thread-factory; do not assume missing stream()
alone is the cause. Trace owned event normalization/registration, provider projection,
factory callbacks, harness, durable/live routes, and browser reconciliation. Distinguish
root source, preserved live servers and installed native version. Keep private
reasoning excluded. Do not scrape native tapes into the app as a substitute for the
actual native adapter lifecycle.

Required behavior:
1. An already-open observer and a freshly opened browser can watch an externally
   submitted turn without being the POST sender. Show accepted/running state before
   first output, the actual public progress and tool lifecycle as available, and a
   truthful terminal outcome. Missing engine detail stays explicitly unavailable.
2. Refresh/reconnect/thread switching recovers the same active turn. Use a proper
   ordered snapshot/cursor or existing equivalent; preserve exact owner identity,
   deduplicate delta versus snapshot events, avoid stale fetch regression and races
   between live completion and durable history. Do not send/replay a new model call.
3. Preserve final answer and activity as distinct concepts where the native protocol
   distinguishes them. Never concatenate a final snapshot after its own deltas or
   turn every public progress update into a duplicate completed assistant message.
   Retain original provider completion/native/input/phase artifacts and failures.
4. Keep desktop/mobile Chat and relevant inspection useful: compact status/activity,
   no raw-wire dump, clear partial/unconfirmed/persistence-failed distinctions. Scope
   to real repeated work, not explanatory copy or a marketing screen. Do not hide
   failed/unknown work behind an indefinite cursor or claim idle for an active turn.
5. Project/thread isolation, foreign/late/duplicate owner controls, no private
   reasoning/config/auth exposure, no destruction of browser-only unsaved evidence,
   and bounded subscriptions/buffers with owned cleanup and no replay are mandatory.

Drive controlled real SessionBackedProvider/factory/runtime/SQLite/routes, including
representative supported Claude and Codex public events. Capture RED before repair.
Actual owned Playwright QA must use the production app, external HTTP sender, both
an existing observer and a fresh390/1440 browser, held original work before text,
progress/tool updates, reconnect and terminal success/failure. Include an unrelated
thread sentinel and corruption/ordering controls. No seeded fake positive UI states,
DOM bypasses, suppressed unexpected browser/API errors or fabricated native receipts.
The browser should see activity while the exact original call remains held, not
only after completion. Retain screenshots and exact source/command/exit evidence;
inspect representative rendered captures and confirm every acquired child exit.

Use current domain contracts and ownership abstractions. Preserve P02a/P02b tests,
full result evidence, existing deadlines and the unchanged protected runner/R1/R2
tests. Run focused coverage, strict/package types and source-stable actual browser.
Earlier combined32/7/4skip stays failed unless genuinely repaired and independently
verified in its scope. No broad LS retry matrix or timeout increases here.

No native model experiment, account/model/binding change, root installation,
candidate edit, live4400/4407 restart, existing task cancellation or commit. Controlled
owned fresh browser/fixture resources are allowed. CUA is locked: do not retry or
bypass that restriction; use the existing owned Playwright mechanism. Parent stays
read-only during your source/test/browser slot.

Finish with docs/L-live-watch-repair-handoff.md: reproduced cause versus inference,
architecture/ordering/durability semantics, exact RED/GREEN evidence and screenshots,
source hashes, cleanup and remaining gaps. State clearly that controlled source
proof does not mean the preserved live4407 process now runs this correction. Propose
the next reviewed immutable installation/adoption step needed for real visible native
work, without executing it. Then finish this original task for parent review.
