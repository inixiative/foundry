# Make expert resolution decisions explicit before adoption

Your route correction ended authoritatively (task_complete07:00:24.121Z, matching durable1788850824133, empty buffer), full handoff read. Parent independently passed new HTTP/configured/original/gates30tests191assertions on stable source and accepts the narrow two-line route correction. Full goal and native L remain open.

Read docs/E1-fable-opposite-review.md. Address F1 as an actual configuration-observability issue: passive domain/writers metadata on a built-in layer suppresses the legacy expert without a diagnostic. Do NOT fabricate an agent from passive metadata or resurrect an explicitly disabled expert. Domain metadata can legitimately categorize passive knowledge, so do not ban passive domain labels wholesale. Prefer an explicit, bounded resolution diagnostic through the existing runtime warning/reporting path for a legacy expert suppressed by passive metadata; reject genuinely contradictory writer references if that is already the validation contract. The operator needs the layer/domain and actionable cause, without source contents or provider secrets. Add focused controlled tests for both domain and writers cases, enabled explicit expert, disabled declarations, and no extra model admission. Preserve existing private knowledge ownership and original acceptance assertions.

For F2, retain deliberate explicit global learning.review precedence from existing startup/config contract, but record the resolution clearly in this handoff and diagnostic if needed when it overrides an expert profile. Do not silently choose a cheap model or change requested model/effort. F3 configurable custom guard triggers and F4 default model routing are future E concerns, not license for unrelated scope growth; record them against the full goal. User requires capacity/model fidelity, not an automatic cheaper substitute.

Own only agents/configured-experts.ts, narrowly agents/thread-runtime.ts if diagnostic forwarding requires it, focused configured-expert tests or a new configuration-diagnostics test, and docs/E1-configuration-diagnostics-handoff.md. No changes to factory/startup/config schema unless you first demonstrate existing reporting cannot carry this narrowly. Routes and all parent fixtures are frozen. Fable owns browser-order correction in driver/helper/runner/checker files and the exclusive isolated Chrome slot. Parent handles independent verification. Run focused tests/strict/diff sequentially with exact hashes; no browser/native/model admission, installs, candidate freeze, live-server/settings/account/binding/shared-browser changes or commits.

Keep the work bounded so the reviewed configured-expert source can join the next installed candidate after browser integration is resolved. Do not claim hot editing, identity migration, native learning, Herald, full release or count advance from diagnostics.
