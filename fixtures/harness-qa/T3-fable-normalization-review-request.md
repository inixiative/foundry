# Opposite review of Astra's native MCP normalization and late settlement

Continue the same Fable/Foundry session after verified end_turn09:34:52.028Z.
Supervisor read your entire provenance handoff and appended duplicate correction.
The shared-native-pair diagnostic is scoped; Astra owns final integration and
candidate staging. Same full goal/count6-next8, no new architecture-only cycle.

Read-only review of Astra-authored current ../agent-session/src/codex-session.ts,
src/harness-session.ts and related changed normalization/type files; Foundry
packages/foundry/src/providers/native-evidence.ts, session-backed.ts and actual
native-candidate/bridge.test.ts and late-settlement fixtures. Use current source
and exact hashes, not old registry or frozen T3 bytes. No production edits.
Your own retrieval parser/diagnostics and T2 base are NOT opposite-independent;
disclose that boundary. Do not stage, capture models or change any live state.

Check legacy mcp_tool_call_begin/end parsing, original public arguments, result
Ok/Err/tool-level errors, malformed/foreign/duplicate/late events, call pairing,
native versus SDK identities, omission labels and provider projection; preserve
the shell path. Ground encoded shapes in primary protocol source and controlled
tests, explicitly distinct from installed app-server schema and actual legacy
wire evidence (still pending). Verify owner reconciliation uses the ORIGINAL
registered admission, never a current/foreign owner; physical/bridge release
must still preserve unknown outcomes without replay. Inspect both actual engine
class controlled route tests, not just helper fixtures.

Run the unchanged nine-file cross-check or focused independent cases with both
repositories' before/after fingerprints, relevant sibling regressions and strict
types. Parent27case report ../agent-session/.qa/s0-cross-2026-09-07T09-21-54.875Z/report.json
is a bounded positive, not your review verdict or actual model capture.
Watch for concurrent source edits; stable per-boundary hashes qualify the
review, never claim whole-tree acceptance if moving. Astra's current task may
finish during your review; read its updated handoff if available and verify
candidate provenance if complete, but do not race its staging or mutate it.

Output docs/T3-native-normalization-review.md with ordered actionable findings,
file/line and exact check/hash evidence, readiness split into source integration
and actual native retrieval. Do not assign broad release credit. Record actual
terminal time only from evidence, not a guessed future end-time header. No
agents, root install, restart, credential/account/binding changes or commits.
After this review and reviewed combined candidate, actual both-engine omitted-fact
retrieval should proceed promptly, then I2 and the full remaining workflow graph.
