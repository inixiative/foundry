# Continue the lead: final guard review, then native learning settlement

Continue the SAME Astra/Foundry/native session after verified T3 task_complete
09:39:34.948Z and durable final. Supervisor read the complete final T3 handoff.
Same full CORE-002/003/004/005 and AS-001..005 goal, count6-next8. Preserve C-final
adoption-a4f91618-08ad-4c81-afa3-830789f789ce and its native manifest40f042...:
parent independently verified375sourcefiles, both installed packages,1277baseline
and105installed/SDK/browser/provenance checks. These are controlled positives,
not actual native retrieval or rollout. Fable reviews your normalization read-only
now; parent will run actual both-engine retrieval after that verdict is resolved.

## First: complete opposite review of Fable's final guard boundary

Read all docs/T3-retrieval-provenance-handoff.md including09:34 correction and
the final parser/diagnostics in C-final. Disclose your own uniqueness-check
authorship; review Fable's structured receipt/bridge/full-owner/default-argument
and shared-pair diagnostics independently. Parent all15 independent cases pass.
Record docs/I2-guard-review-and-learning-handoff.md with bounded accept/reopen
verdict and exact evidence, and public update BEFORE implementing I2, so parent
can proceed with the frozen retrieval candidate. Do not edit the guard, runner,
MCP parser/transport or any frozen candidate. Any NEW blocker must be reported
concretely rather than silently patched while parent prepares capture.

## Then: implement the remaining I2 source boundary

Own current agents/thread-runtime.ts, domain-librarian.ts, narrow typed provider
owned-admission inspection/lifecycle interface and persistence/UI projection
only as necessary, plus focused tests. No source changes to the frozen T3
capture path. Source-level I2 can advance independently; native I2 acceptance
still depends on actual retrieval and real native learning evidence, not mocks.

Current runtime observer journals native facts but never reconciles native
review state/capacity; LearningState.nativeOutcome is hard-coded unknown.
reconcileReviewCapacity only handles locally-settled expired jobs with capacity
already settled. SessionBackedProvider.inspectOwnedAdmission exists but is not
wired into this lifecycle. Read actual implementation before changing it.

- Retain exact job, thread/project/runtime generation, originating evidence,
  native admission, provider-pool identity and base revision/hash. Original
  registered owner wins; never infer from the current request. Inspection is
  read-only, performs no sends/retries/rebinds and rejects unknown/foreign owners.
  Use a typed optional existing-provider capability, not a new session engine.
- Separate local observation outcome, native terminal, RPC/transport occupancy,
  process cleanup and commit eligibility. A terminal alone must not fabricate
  free capacity while the actual native call/cleanup is unresolved. Support
  exact late native evidence after local observation rejects, not only an
  awaited JavaScript promise finally resolving. Preserve the soft/hard deadlines.
- An eligible original review can publish a valid bounded learned candidate
  exactly once through the existing owner/base compare-and-set and durable
  journal. A hard-expired/stale/disposed/foreign review may settle evidence and
  capacity only; it cannot reopen commit eligibility, drain deferred work or
  launch a replacement. Persist enough exact ownership/closure to survive
  restart without replaying an unknown native admission.
- Do not add a central serial wait, shorten deadlines to hide latency, clear
  occupancy on transport loss, or re-inject an entire transcript as learning.
  Keep instructions/domain knowledge/complete bounded thread knowledge separate.
  First eligible next request after a commit sees the learned convention without
  user repetition; historical input/trace snapshots remain immutable. Preserve
  durable-write failure versus publication reconciliation, requested budgets
  versus enforced capability, and JSON/budget/refusal behavior already tested.

Write RED-to-GREEN production runtime/provider/journal tests: native late result
inside eligibility, after hard expiry, duplicate/foreign job or generation,
native completed while RPC pending, transport loss, no repeated native admission,
exact late inspection with key-order-independent owner fields, disposal/restore,
SQL-trigger write failure and committed-but-publication-failed state. Exercise
both real installed classes over controlled streams in a NEW isolated I2
candidate only when necessary; never edit C-final or install into root. Keep
the existing independent learning/memory/native cases unchanged and run them
explicitly with source fingerprints/types. No model probes or staging merely
for a plan; coordinate any staging only after implementation/source handoff.

Append exact implementation/tests/hashes/limitations and next native-learning
sample requirements to the same handoff. No self-acceptance/count advance.
No root/live restart, credential/account/binding changes, agents, publication,
commits, or edits to independent parent fixtures. Full native artifacts/tags/
inspection/lineage/fork/subsessions/pooling/model-effort/performance remain open.
