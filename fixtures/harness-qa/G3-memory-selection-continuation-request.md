Continue the unfinished memory-selection implementation in this SAME native Fable
session, preserving da000690-899a-4128-a868-b9a024aeeaa0 and all current edits.
The prior Foundry turn turn_fdebb31b-4776-4fb7-ad30-79248e974187 ended at
2026-09-07T01:39:57.025Z with the provider's Native terminal error
(error_max_turns), not a completed implementation or an observation timeout.
The last native tool result was the expected missing selectionSummary export in
the new inspector test. No successful handoff was produced; do not restart from
scratch or repeat already-completed writes. Inspect current files first.

The installed native engine defaults to25 turns and startup does not override
that for the central executor. This is a harness capacity limitation, not evidence
you chose to stop. Keep this continuation bounded; finish correctness before broad
UI polish. Do not change live process configuration, bindings or credentials here.
Astra remains overall lead working on sibling S1; you own Foundry memory selection.
No self-approval, sibling edits, server restarts, new agents or native model probes.

Supervisor independently tested the in-progress source. The original two memory
integration cases now pass, but FOUR ordinary failures block acceptance. Read
fixtures/harness-qa/QA-2026-09-07-memory-selection-acceptance.md, especially its
01:39-01:40 section. Both independent files are supervisor-owned and must remain
unchanged:
- fixtures/harness-qa/acceptance/memory-selection.test.ts
- fixtures/harness-qa/acceptance/memory-selection-provenance.test.ts

Fix these behaviors, not just the sentinels:
1. Pinned requirements that fit the total budget must not be prefix-truncated.
   Requirements beyond the total budget need explicit, truthful handling that
   preserves their semantics or prevents misleading execution, not a summary marker
   claiming a rule was included when its operative ending was dropped.
2. A relevant excerpt must contain the matching evidence wherever it occurs in
   the record; show truthful truncation/ranges/full-record provenance. Do not label
   unrelated prefixes relevant solely because the unseen suffix matched.
3. A record selected by fallback must not also be reported omitted; final selected
   and omitted sets/counts/summary must agree with actual delivered content.
4. Async warming must keep the captured focus/revision with its content/report.
   A focus change during loading must trigger fresh selection or remain visibly
   stale, not label old content warm for the new message. Check restore/set and
   failed-refresh paths for stale provenance too.

Finish the missing inspector helper/integration with existing controls and preserve
all audit records, privacy, generated knowledge, explicit instructions, retrieval,
historical snapshots and native-retention uncertainty from the original request.
Run both explicit independent files through harness-check G3 plus your focused
tests and typechecks. Run the full baseline once code is complete. If time/turn
budget is tight, give an honest partial handoff with exact remaining work instead
of claiming acceptance. No source changes while your final verification is running.
Capture representative headless browser evidence only once the UI compiles; avoid
the locked screen. Do not restart primary4400 or lead4407 for this verification.

Write a dedicated handoff with exact reports, source stability, known limitations
and a proposed controlled rollout. Shared graphs and independent acceptance remain
supervisor-owned. The fourth-slice leadership checkpoint has NOT been reached:
accepted count remains3 until independent QA and opposite-model review pass.
