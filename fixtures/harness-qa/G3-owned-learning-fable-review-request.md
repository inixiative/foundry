# Opposite-model review: owned causal learning

Astra remains provisional lead, accepted count 5, checkpoint 6. The full CORE-002/003/004/005 goal is unchanged. Your S1 compaction correction ended at 04:29:54.561Z and is queued for Astra review. This is a new read-only review, not implementation or authorization for native probes, restarts, dependency changes, credentials, detached agents or publication.

Astra's actual learning task completed at 04:35:30.941Z. Read docs/G3-owned-learning-handoff.md and the implementation in learning-config, domain-librarian, thread-runtime, knowledge-persistence, local-session-store, startup/factory and their focused tests. Review the current revision, not the original six failures. The supervisor's six learning-review-input/learning-owned-runtime cases now pass. Two new learning-durable-boundary cases exercise real HTTP/SQLite; the supervisor is repairing its second-connection SQL trigger setup (exclusive lock), not weakening rollback assertions. Leave all implementation and independent tests unchanged.

Find correctness, capacity, lifecycle and inspection blockers. In particular inspect all three complete reviewer segments; exact frozen owner, dispatch, generation/epoch and base; per-phase provider/options; soft observation versus hard eligibility; durable CAS before publication; deadline crossing in SQL; stale/duplicate/foreign/disposed work; actual layer publication and signal failure; queue retention and native unknown occupancy. No added central wait must not become an excuse for permanently lost learning, misleading pending states or silent background capacity loss. Native terminal reconciliation remains an explicit I dependency, not an implemented feature.

Resolve the intent conflict in fixtures/harness-qa/acceptance/domain-learning.test.ts: its immediate-next-request case assumes a serial 25ms learning barrier. The intended contract is immediate requests use the last committed revision and report pending, and the first request after commit receives the new fact without user repetition. Recommend explicit replacement cases preserving causal-learning coverage; do not edit or silently classify the old failing assertion as expected.

Run focused tests as needed sequentially: harness-check output directories can collide in the same millisecond. Report exact commands, counts, before/after source hashes and what the tests actually prove. No native captures. Write a dedicated fixtures/harness-qa/QA-2026-09-07-fable-owned-learning-review.md with severity/file/line findings, supported or missing user behaviors, contract recommendation, rollout requirements and a bounded accept/reopen verdict. If no blocker remains, include the sixth-slice leadership/scope checkpoint recommendation; supervisor decides acceptance after independent browser/native checks. Do not edit shared ledgers or self-accept your own prior provider/compaction changes.
