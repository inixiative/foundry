# Exercise long-history work before the UI implementation

Continue SAME Fable session after actual end_turn13:32:39.801Z, matching durable
final and complete appended G6 handoff read. Parent reproduced the unchanged
reviewer scratch7pass/0fail and focused config/segment checks are green with
types/diff. G6 correction awaits Astra's opposite review after its current
preflight review. Do not edit those source files or begin UI production edits yet:
the integrated native candidate freeze needs this stable source boundary.

Count correction: EIGHT bounded Astra source slices are accepted, not seven.
See checkpoint-eight. No count change for your G6/preflight work, no installed
or native acceptance, and the full goal remains intact.

Use this available slot for executable G6 product QA, not another prose plan.
Read G6-history-index-and-inspection-request.md (prepared, not implementation
authorization) and QA-2026-09-07-history-payload.md. Reuse existing production
viewer/SQLite and existing fixture/browser helpers in a disposable scratch
directory under .foundry/qa. Read sample-projects/release-notes, documentation and
tool-workbench; choose one recognizable project for the fixture. Seed >300
messages using the actual journal APIs, including two threads, historical traces
with large injection/native-shaped public detail, older unique artifact markers,
and a separate browser-only completed-unsaved result. Simulated native detail is
labelled controlled fixture data and is NOT actual native evidence.

Start only an owned temporary fixture server on an available random loopback port.
No real model processes or dispatch: use controlled handlers, and assert zero
native/model calls. Open fresh Playwright1440/390 browsers using the installed
runtime path /Users/agreenspan/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright.
Use the actual current UI. Count response bytes and visible messages, try to reach
the oldest seeded turn and its historical trace/artifact, inspect prepared context
versus current layers, reload, and exercise quota-denied and offline legacy/unsaved
history. Retain screenshots, structured assertions, exact steps and source hashes.
Inspect screenshots yourself, not just existence. Distinguish absent controls,
missing/partial history and real product failures from timing/observer failures.
Failures are expected baseline evidence, never mark them as passing feature gates.

Only scratch fixture scripts/tests/reports/screenshots and
docs/G6-long-history-browser-baseline.md may be written in this assignment.
Do not edit production, checked-in regression tests, candidates, package/lock,
sibling, existing captures, accounts/credentials/bindings, live4400/4407, or commit.
Close only owned fixture server/browser handles before your actual final. Do not
dispatch to another model. Parent will independently inspect your artifacts and
authorize the real UI implementation promptly after the native candidate freeze.
